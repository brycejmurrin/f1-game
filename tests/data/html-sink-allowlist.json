{
  "//": [
    "Audited HTML sinks in js/ whose markup is NOT a source constant — read by",
    "tools/check/html-sink-lint.mjs and gated by tests/unit/html-sink-lint.test.mjs.",
    "Key: <file>::<receiver source>.<sink> (no line numbers). `count` is how many",
    "sites share the key. `reason` says why every dynamic part is ours, never",
    "player text. A new site should use Dom.el()/textContent or escape, not a row.",
    "Audited 2026-09-25."
  ],
  "sites": {
    "js/game.js::sum.innerHTML": {
      "count": 1,
      "reason": "HUD fold summary: hudProfile / hudMetricsLayout are validated against HUD_PROFILES / HUD_MET_LAYOUTS at load and in the G setters; every other part is a literal."
    },
    "js/perf/metrics-overlay.js::sum.innerHTML": {
      "count": 1,
      "reason": "METRICS fold summary: page() is readPage(), which returns only a PAGES member (store) or a /(gov|car|phys|log)/ query match."
    },
    "js/perf/renderer-picker.js::sum.innerHTML": {
      "count": 1,
      "reason": "RENDERER fold summary: backendLabel() of readBackend() (webgl2|webgpu|three, anything else reset to webgl2) and liveBackend() (constants)."
    },
    "js/ui/aria-state.js::el.innerHTML": {
      "count": 1,
      "reason": "paintOnOff re-wraps a button's textContent; wrapOnOff ESCAPES & < > \" ' before inserting its own spans (the 2026-09-24 stored-XSS fix — custom driver names reach these buttons)."
    },
    "js/ui/dom.js::target.innerHTML": {
      "count": 1,
      "reason": "Dom.paintFold: [kind, label] pairs are the callers' constants or validated enums (audio/panel.js, input/steer-tuning.js) — never user text. Keep it that way: a caller with free text must use Dom.el()."
    },
    "js/ui/loading-screen.js::$(\"ld-flag\").innerHTML": {
      "count": 1,
      "reason": "Flags.svg(country): emits only a built-in RECIPES flag keyed by a known code (unknown input falls back to \"xx\"); the input never reaches the markup as text."
    },
    "js/ui/select-screen.js::fl.innerHTML": {
      "count": 1,
      "reason": "Flags.svg(t.country) — see loading-screen: the markup is a built-in recipe, not the input."
    },
    "js/ui/select-screen.js::flagEl.innerHTML": {
      "count": 1,
      "reason": "Flags.svg(t.country) — see loading-screen: the markup is a built-in recipe, not the input."
    },
    "js/ui/select-screen.js::drsList.innerHTML": {
      "count": 1,
      "reason": "Circuit DRS zones from js/circuits/ data (code, not player input); every interpolation is a Math.round() number or the zone index."
    },
    "js/ui/select-screen.js::list.innerHTML": {
      "count": 1,
      "reason": "Circuit corner list from TrackMaps: c.n is a number, the class comes from the TDC_CLS map, and lbl is TrackMaps' corner class (HAIRPIN/SLOW/MEDIUM/FAST) — circuit data, not player input."
    }
  }
}
