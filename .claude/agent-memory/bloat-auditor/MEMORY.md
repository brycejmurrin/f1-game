# bloat-auditor memory

One line per finding the parent rejected: `file:line — kind — why not`. Never re-propose a listed carve.

An entry is believed only while the code it judged is unchanged: if the cited file has been modified since the entry was recorded, the rejection is re-checked, not trusted — the reason a carve was refused usually lived in the surrounding lines, and those have moved.
