---
description: Apex 26 shared agent layout — point Cursor at the Claude tree and AGENTS.md
alwaysApply: true
---

# Apex 26 — Cursor entry

- **Rules:** read root `AGENTS.md` (canonical). Root `CLAUDE.md` is a stub that
  imports it; do not edit rules into `CLAUDE.md`.
- **Skills / subagents:** `.claude/skills/*/SKILL.md` and `.claude/agents/*.md`
  (shared with Claude Code). Do not add `.cursor/skills/` or `.cursor/agents/`
  unless a file is explicitly Cursor-only.
- **Route:** deploy/`version.json` → `deploy-research`; pre-push → `verify-agent`;
  live canvas → `mcp-probe`. Do not attach `mcp-probe` for a version.json check.
- **MCP:** Keep `apex-tools` in **root `.mcp.json`** so Cloud/Claude/this
  agent can load it. `.cursor/mcp.json` is the Cursor CLI/IDE copy (same four
  servers). If the host catalog is empty, `tools/apex-tools-mcp.sh call`.
  Never run Chrome MCP while Playwright is running; never hand a subagent a
  browser test group.
- **Token hygiene:** one skill per task; prefer `__apex` / `verify-track` /
  `test:tooling-fast` before screenshots or browser groups. Path-scoped rules
  under `.cursor/rules/` (e.g. `render-wgx`, `render-tlx`) attach by globs.
