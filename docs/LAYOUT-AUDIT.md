# Layout audit — moved

> This page moved to [`COMPONENTS.md`](COMPONENTS.md). The screen x viewport grid, what the probe measures and how to read it are now the *Layout axes* section of the UI/component reference.

The stub stays because source files, tests and tools cite this path in comments;
follow the link for the content.
