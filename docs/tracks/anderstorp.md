# Scandinavian Raceway — Visual Design Brief

**Setting:** DAY, green theme (Småland pine and an airfield). ~4.02 km, 8 turns, clockwise.

## 1. Setting

Built around a working airfield in the Småland pine forest. The long back straight IS the runway — wide, flat, and conspicuously wider than the rest of the lap. Six Swedish Grands Prix in the 1970s; nothing since. Flat, fast and strange, with dense pine right up to the edge everywhere except the runway.

## 2. Atmosphere & palette

Northern light: cool, blue-shifted, long shadows even at midday. Pine is dark and desaturated. Sand run-off is pale grey, not golden.

## 3. Elevation

Flat. This is the flattest circuit in the roster — do not add relief.

## 4. Landmarks & surroundings by lap position

| s | Side | Distance | Box description |
|---|---|---|---|
| 0.005 | +1 | 14 | Start/finish. Two low `grandstandEx` blocks on the infield, open-backed, timber decking over a steel frame — nothing taller than a treetop. A squat white-and-grey `building` behind them is race control; a `marshalPost` sits at the line and a `cameraTower` just past it. `groundPatch` of pale grey gravel between stand and armco. |
| 0.020 | -1 | 6 | Outside of the pit straight is the cheap side: a single run of `guardrail` and then pine. `forestEdge` of tall bare-trunked Scots pine set only a few metres back, canopy high enough that the trunks read as a colonnade in the low northern sun. One weathered `sponsorHoarding` breaking the green. |
| 0.045 | -1 | 22 | **Turn 1.** Pale grey sand run-off — not golden, closer to wet concrete — then a `tyreWall` stack and `forestEdge` immediately behind it. `marshalPost` at the apex, `cameraTower` on the outside so the corner can be shot down the straight. |
| 0.095 | +1 | 28 | Infield paddock. Low corrugated `building` rank, single storey, ribbed metal in faded blue and cream, with a cluster of `motorhome` units parked nose-in on a `groundPatch` of grey hardstanding. This is the working part of the site and it looks it. |
| 0.150 | -1 | 5 | Pine right up to the armco. Dense ranks of `pine` behind a continuous `guardrail`, no run-off worth the name, trunks close enough that the shadow banding across the track is the dominant lighting effect here. |
| 0.185 | +1 | 26 | **Turns 2–3, Södra Kurvan.** The south loop opens out: mown grass infield, a low `spectatorHill` (a metre or two of banking, not a real hill — this site is flat), two `marshalPost` positions and a pair of `billboard` panels facing the crowd side. |
| 0.245 | -1 | 9 | Back into the trees. `forestEdge` and `guardrail`, one `marshalPost`, a small wooden `building` serving as a flag/timing hut. Nothing else — the forest does the work. |
| 0.330 | -1 | 20 | **Turn 4, Opel Kurvan.** Named for the sponsor: a `sponsorHoarding` run on the outside, `tyreWall` in front of it, grey sand run-off, then `forestEdge`. Low `spectatorHill` outside the barrier with a `cameraTower` behind it. |
| 0.400 | +1 | 32 | Infield scrub between the loops — `groundPatch` of sandy grass, scattered `tree` and thin `pine` clumps rather than solid forest, one `marshalPost`. Sightlines open up here and you can see the hangars ahead. |
| 0.480 | -1 | 18 | **Turn 5.** Long constant-radius corner, slightly banked. `guardrail` with a `tyreWall` on the exit, `forestEdge` close behind, a `billboard` at turn-in. Grey sand strip between kerb and barrier. |
| 0.556 | +1 | 30 | **Turn 6 — onto the runway.** The track edge steps outward and the surface widens dramatically: a `groundPatch` of aged concrete apron spreads to the infield, painted with faded runway edge markings. The armco pulls back and the whole corridor opens. This is the moment the player should feel. |
| 0.640 | +1 | 40 | Airfield apron proper. Two large corrugated `building` hangars, arched roofs, dull silver-grey, doors facing the runway; a low `building` beside them is the flying club. `groundPatch` of concrete hardstanding links them to the straight. |
| 0.740 | -1 | 55 | Runway's far side. Flat mown grass running a long way out to a distant `forestEdge` — the only place on the lap where the pine is not on top of you. A sparse line of `pine` marks the field boundary; one `marshalPost` mid-straight. |
| 0.820 | +1 | 35 | End of the apron. A third smaller `building`, corrugated and low, with a `sponsorHoarding` on its flank; `groundPatch` of concrete giving way to grass as the runway's width is taken back. |
| 0.860 | -1 | 16 | **Turn 7 — off the runway.** The corridor narrows again: `guardrail` returns on both sides, `tyreWall` on the outside, `forestEdge` of dense `pine` closing back in. `marshalPost` and a `cameraTower` at the braking point. |
| 0.920 | -1 | 24 | **Turn 8, the long final corner onto the pit straight.** A big, slow, banked sweep. `grandstandEx` on the outside — low timber terracing — with a `spectatorHill` extending it, `billboard` panels along the barrier, `tyreWall` and grey sand run-off, `forestEdge` behind the whole thing. |

## 5. Track features

- The runway back straight, noticeably wider than the rest of the lap.
- A long, long final corner onto the pit straight.
- Dense pine everywhere else.

## 6. Modelling notes

- The width change at the runway is the identifying feature. Make it visible.
- Scots pine in dense ranks — tall bare trunks, canopy only at the top.
- Low wooden and corrugated buildings, nothing grand.
