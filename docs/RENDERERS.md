# Renderers — moved

> This page moved to [`ARCHITECTURE.md`](ARCHITECTURE.md). Boot flow, frame pipeline, safety prefs, the parity snapshot and the cross-backend parity workflow are now the *Renderers — GLX / WGX / TLX behind one seam* section of the architecture reference.

The stub stays because source files, tests and tools cite this path in comments;
follow the link for the content.
