# Agent world API — moved

> This page moved to [`DEBUG-HOOKS.md`](DEBUG-HOOKS.md). The agent-facing JSON world view is now the *Agent world API* section of the hook reference.

The stub stays because source files, tests and tools cite this path in comments;
follow the link for the content.
