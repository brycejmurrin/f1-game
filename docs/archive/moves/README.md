# Archived move plans (applied)

Historical `move-tree.mjs` plans from the 2026-09 tree restructure. Not
runnable — `from` paths no longer exist. Recover context via git history
or these JSON files. Live plans stay in `tools/moves/` (spike only).
