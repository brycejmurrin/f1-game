{
 "confirmed": [
  {
   "severity": "HIGH",
   "kind": "UNAPPLIED",
   "file": "tests/specs/agent-view.spec.js",
   "line": 1535,
   "claim": "The agentHelp() spec still asserts the deprecated-aliases note that commit a6fb6ce2 deleted from agentview.js, so the agent test group now fails on assertions the session broke and never re-ran.",
   "evidence": "tests/specs/agent-view.spec.js:1535-1536: `expect(notes).toContain(\"DEPRECATED\"); expect(notes).toMatch(/frame\\(\\).*plan\\(\\).*worldModel\\(\\)|worldModel/);` \u2014 but the session's diff removed the only matching entry from agentview.js's agentHelp notes: `-  \"frame()/plan()/worldModel()/visible() remain as DEPRECATED aliases \u2014 \" + \"prefer render({what}) and scene({visible})\"`. The current notes array (js/game/agentview.js:2447-2453) has 3 entries containing neither \"DEPRECATED\" nor \"worldModel\". The session's own survey predicted this exact pairing: code-survey-tests.md item 10 says these lines \"will need a paired edit when that note is cleaned up\" \u2014 the note was cleaned up, the paired edit was not made.",
   "fix": "Update tests/specs/agent-view.spec.js:1533-1536 to match the post-removal manifest (e.g. drop the DEPRECATED/worldModel assertions or assert the aliases are absent), consistent with the REMOVED pin the session added in tests/unit/agentview-api-contract.test.mjs.",
   "finder": "session-diff",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "HIGH",
   "kind": "BUG",
   "file": "tools/pick-tests.mjs",
   "line": 72,
   "claim": "The session's two extractions were never added to the pick-tests RULES, so an edit to js/game/physics-consts.js \u2014 every immutable driving-model number \u2014 now routes to ZERO physics groups (only the tiny/tooling fallback), and js/game/cam-modes.js routes to no camera group.",
   "evidence": "Ran `node tools/pick-tests.mjs js/game/physics-consts.js js/game/cam-modes.js` \u2192 only `test:tiny` and `test:tooling-fast`. RULES line 72 routes only `/^js\\/game\\.js/` to [\"behaviour\",\"api\",\"circuit\",\"physics\"]; line 73 `/^js\\/game\\/(cameras|cam-tune|cam-tuner)\\.js/` omits cam-modes. The RULES' own comment (lines 65-71) narrates this exact failure mode: \"js/track/space.js ... routed to it while the file that contains the model did not, which is how a change to the FX block's pace normalisation came back 'no physics group needed'\" \u2014 the extraction recreated it for the constants file. CLAUDE.md's testing rule 2 makes pick-tests the authoritative change\u2192test router.",
   "fix": "Add `[/^js\\/game\\/physics-consts\\.js/, [\"behaviour\",\"api\",\"circuit\",\"physics\"], ...]` and add `cam-modes` to the camera rule at line 73; tests/unit/test-groups.test.mjs imports RULES so verify with test:tooling-fast.",
   "finder": "session-diff",
   "verdict": "confirmed",
   "skepticNotes": "Confirmed on tip a187ecb0. Running `node tools/pick-tests.mjs js/game/physics-consts.js js/game/cam-modes.js` yields only test:tiny and test:tooling-fast. tools/pick-tests.mjs:72 routes only /^js\\/game\\.js/ to [\"behaviour\",\"api\",\"circuit\",\"physics\"], which cannot match js/game/physics-consts.js; line 73's camera rule /^js\\/game\\/(cameras|cam-tune|cam-tuner)\\.js/ omits cam-modes. Both files were extracted from game.js in this session (commits a6fb6ce2 and 7c67f9a0) without corresponding RULES updates, and the comment at lines 65-71 documents this exact routing-gap failure mode the rules were supposed to prevent. The finding is accurate, not overstated: the immutable driving-model constants now escape all physics test routing, contradicting CLAUDE.md's rule that pick-tests is the authoritative change-to-test router. | Confirmed, not refuted. Running `node tools/pick-tests.mjs js/game/physics-consts.js js/game/cam-modes.js` on branch tip a187ecb0 yields only test:tiny and test:tooling-fast. tools/pick-tests.mjs line 72 routes /^js\\/game\\.js/ (literal dot \u2014 cannot match the js/game/ directory) to the physics/behaviour/api/circuit groups, and line 73's camera rule /^js\\/game\\/(cameras|cam-tune|cam-tuner)\\.js/ omits cam-modes. Both files were newly created by this session's diff (commits a6fb6ce2 and 7c67f9a0, +149 and +108 lines) and no commit in a162b00a..HEAD updated the RULES. The gap is not deliberate: the RULES' own comment at lines 65-71 describes this exact omission pattern as a previously-shipped bug (\"came back 'no physics group needed'\"), and the header says rules should be biased toward running too much. The extraction of every immutable driving-model number into physics-consts.js recreated the documented failure mode."
  },
  {
   "severity": "HIGH",
   "kind": "UNAPPLIED",
   "file": "js/game/agentview.js",
   "line": 2310,
   "claim": "objective().units ships the WRONG curvature sign to agents \u2014 '+k is a right-hand turn' \u2014 contradicting the file's own CONVENTIONS string ('+k = LEFT-hand turn') and the measured corners() note; the triage marked this JS-BATCH and the session never applied it.",
   "evidence": "js/game/agentview.js:2309-2310: `units: \"metres, m/s, seconds, degrees where a key says Deg. +x is right \" + \"of the centreline; +k is a right-hand turn.\"` versus line 31: `\"+x = right of centreline; +k = LEFT-hand turn; \"` and lines 294-297: `// POSITIVE CURVATURE IS A LEFT-HAND TURN. The comment on spline.js's curvatureRaw says \"+ = right\" ... both were wrong`. The file header (line 16) repeats the wrong sign: `+k is a right-hand turn`. code-survey-triage.md line 31-32 dispositioned exactly this (\"objective().units + header say '+k right' (should be LEFT) ... JS-BATCH (comments)\") but no commit touched these lines.",
   "fix": "Flip the sign statement in objective().units and the header comment to '+k = LEFT-hand turn' to match CONVENTIONS; also the apex.js twins at js/game/apex.js:316, 318, 340 ('+=right') per the same triage item.",
   "finder": "session-diff",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "INCOHERENCE",
   "file": "js/net/rendezvous.js",
   "line": 174,
   "claim": "The session rewrote the file header to say seal()/open() ARE wired, but left the inner comment 60 lines below still declaring the opposite \u2014 the same file now asserts both states.",
   "evidence": "Session-rewritten header at js/net/rendezvous.js:44-45: `seal()/open() below implement the fix and ARE now wired on the public room-code path (NetNostr.directExchange calls them on every exchange)` \u2014 true: js/net/nostr.js:258 `const sealed = await NetRendezvous.seal(code, text);` and :276 `text = await NetRendezvous.open(code, bytes);`. But line 174, untouched by the diff, still reads: `// NOT WIRED UP. seal/open/topicFor have no production caller: swap() posts // through httpPut(), which sends plain JSON.`",
   "fix": "Rewrite the line-174 block to scope the 'not wired' claim to topicFor and the private httpPut worker path only, matching the header.",
   "finder": "session-diff",
   "verdict": "confirmed",
   "skepticNotes": "Confirmed. The session's diff (a162b00a..HEAD) rewrote the header at js/net/rendezvous.js:44-47 to say seal()/open() \"ARE now wired on the public room-code path (NetNostr.directExchange calls them on every exchange)\" \u2014 which is true: js/net/nostr.js:258 calls NetRendezvous.seal(code, text) and :276 calls NetRendezvous.open(code, bytes). But the untouched inner comment at js/net/rendezvous.js:174-175 still reads \"NOT WIRED UP. seal/open/topicFor have no production caller: swap() posts through httpPut(), which sends plain JSON.\" The file therefore asserts both states simultaneously; the line-174 claim is factually false for seal/open today. LOW severity is appropriate (comment-only, no runtime effect), and the proposed fix \u2014 scope the line-174 note to topicFor and the httpPut worker path \u2014 matches the actual state of the code."
  },
  {
   "severity": "LOW",
   "kind": "UNAPPLIED",
   "file": "tests/unit/scenery-api-contract.test.mjs",
   "line": 3,
   "claim": "A cluster of the session's own triaged JS-BATCH fixes never landed: the 938aed50 comment batch covered roughly half the list, leaving false statements the survey had already pinpointed.",
   "evidence": "All still present and all explicitly dispositioned JS-BATCH in code-survey-triage.md: tests/unit/scenery-api-contract.test.mjs:3 `(24 consumer files in js/tracks/)` (the dir is js/circuits/ with 40 files; triage line 81); js/game/quali.js:70 `Re-measure with artifacts/tmp/quali-calibrate.mjs` \u2014 a gitignored artifacts path that does not exist (triage line 49); js/data/telemetry.js:1284 `view._delta = series[0];` write-only line with its false comment (triage line 20, D3); js/game/apex.js:316/318/340 `+=right` curvature comments contradicting DEBUG-HOOKS (triage line 31).",
   "fix": "Land the remaining JS-BATCH comment/dead-line batch (comment-only, no behavior) with one ?v bump, or move the items into the ARCHITECTURE-REVIEW backlog so the triage file's claim of disposition stays honest.",
   "finder": "session-diff",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "DOC-DRIFT",
   "file": "spike/three-spike.html",
   "line": 33,
   "claim": "The spike page claims its script list mirrors manifest TRACK_VM but omits js/log.js and js/track/graph.js, which TRACK_VM includes \u2014 the page cannot build a track (TrackGraph/Log undefined at eval), and the session's triage flagged the fix but never applied it.",
   "evidence": "spike/three-spike.html:33 `<!-- Real engine scripts, order mirroring tools/manifest.cjs TRACK_VM -->` followed by a list starting at `../js/track/geo-paths.js` with no log.js and no graph.js; `require('./tools/manifest.cjs').TRACK_VM` begins `['js/log.js', 'js/track/geo-paths.js', ..., 'js/track/graph.js', ...]`. code-survey-triage.md line 86: \"spike three-spike.html TRACK_VM missing log.js+graph.js (broken spike page) \u2014 JS-BATCH (comments) + spike fix\"; the session diff touched only spike/*.md.",
   "fix": "Add `<script src=\"../js/log.js\">` first and `<script src=\"../js/track/graph.js\">` after models.js (the TRACK_VM position), or drop the 'mirrors TRACK_VM' claim and mark the page known-broken like the deleted babylon spike.",
   "finder": "session-diff",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "HIGH",
   "kind": "UNAPPLIED",
   "file": "js/render/three/tsl-lit.js",
   "line": 258,
   "claim": "The entire 13-item render DRIFT cluster triaged JS-BATCH (triage line 73-74) was never applied \u2014 no js/render/* file appears in `git diff --name-only a162b00a..HEAD`.",
   "evidence": "tsl-lit.js:258 still reads `/* \u2500\u2500 BRDF leaves (lit.js:150-168) \u2014 plain node composition, inlined \u2500\u2500\u2500\u2500\u2500\u2500 */` (actual: shaders/lit.js:183-201), and the file still carries 57 `lit.js:` citations, most shifted ~160-200 lines. Also unapplied: js/render/shaders/fx.js:63 `// uDecalGlow lifts bright marks` (the uniform is uGlow), js/render/webgpu/wgx.js:1391-1392 `same 64 fallback as GLX uShadowRange` three lines above `d[91] = ... : 80.0; // same fallback as GLX` (wgx.js:1395), js/render/shaders/chunks.js:108 dangling trailing comment `// Lift-gamma-gain colour grade: very mild S-curve per channel.` inside the tonemap chunk, js/render/glx.js:2-4 header `One standard lit shader (hemisphere ambient + lambert sun + exp2 fog)... and a blob-shadow quad` for what is now a PBR GGX pass with 13 programs.",
   "fix": "Apply the exact OLD\u2192NEW list in code-survey-render.md D1-D13 (~40 comment lines across tsl-lit.js, tsl-sky.js:90/122, tsl-fx.js:179/216, tlx.js:512/533/594/934/949, tlx-shadow.js:28, wgx.js:1391, shaders/fx.js:63, shaders/chunks.js:108, wgsl-chunks.js:44, glx.js:1-5) + ?v bump.",
   "finder": "triage-completeness",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "HIGH",
   "kind": "UNAPPLIED",
   "file": "js/game/setup-ui.js",
   "line": 3,
   "claim": "Four survey reports (code-survey-game-ui.md, -track.md, -tests.md, -tools.md) never got triage sections \u2014 triage line 55 still says 'Pending reports: gamejs, game-ui, render, track, circuits, shell-css-config, tests, tools' but only four of those eight got sections \u2014 so their comment-drift items were never queued; the batch fixed the 8\u219212 parts-category drift in parts.js/game.js/agentview.js but missed the untriaged game-ui instance.",
   "evidence": "setup-ui.js:2-3: `the tab column (TEAM & DRIVER,\\n   the 8 parts categories, LIVERY)` \u2014 Parts.CATALOG has 12 categories, and `grep -rn \"8 parts categories\" js/` shows this is the only surviving site of the drift class 938aed50 fixed elsewhere. Also never triaged/applied: track survey DRIFT-1 (mesh.js:28-29 findCorners header states the refuted '+k = right' sign convention), DRIFT-2 (geom.js:21 FLAG comment points at the wrong file), DRIFT-3 (scenery-data.js:44-48 orphaned CROWD_DAY emitter comment), and game-ui DRIFT-3..8 (tuner.js:1-3 header claims RESET/COPY VALUES live there, ariastate.js:5 '17 menu screens', setup-ui.js:566 'three colour wells' vs 12 rows, etc.).",
   "fix": "Change setup-ui.js:3 to 'the 12 parts categories'; add triage sections for the four pending reports and land their JS-BATCH-class comment fixes from the per-file OLD\u2192NEW specs.",
   "finder": "triage-completeness",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "HIGH",
   "kind": "UNAPPLIED",
   "file": "spike/three-spike.html",
   "line": 34,
   "claim": "The triaged 'spike fix' (triage line 86) for three-spike.html's drifted TRACK_VM mirror was not applied: the page still lacks js/log.js and js/track/graph.js and cannot build any circuit as committed.",
   "evidence": "The script list under `<!-- Real engine scripts, order mirroring tools/manifest.cjs TRACK_VM -->` starts at `<script src=\"../js/track/geo-paths.js\"></script>` (line 34) and contains neither `../js/log.js` nor `../js/track/graph.js` (grep confirms zero hits), while tools/manifest.cjs:200-209 TRACK_VM lists `\"js/log.js\"` first and `\"js/track/graph.js\"` after models.js. tracks.js calls `TrackGraph.create({ raw: RAW })` unconditionally inside Tracks.build() and scenery-city.js reads `TrackGraph.NODE_COLOR` at eval, so the page throws ReferenceError before rendering.",
   "fix": "Add `<script src=\"../js/log.js\"></script>` before spike-data.js and `<script src=\"../js/track/graph.js\"></script>` after ../js/track/models.js, matching TRACK_VM order (no ?v needed \u2014 spike/ is not shipped).",
   "finder": "triage-completeness",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "HIGH",
   "kind": "UNAPPLIED",
   "file": "js/game/apex.js",
   "line": 137,
   "claim": "Three agent-surface JS-BATCH items (triage lines 36-38) were skipped even though both target files WERE edited by the batch: the sky() doc block describes pre-fix geometry, world()'s penalties comment cites game.js ~2178, and agentHelp().cli omits two live agent.mjs commands.",
   "evidence": "apex.js:136-138: `// gradient are clearly visible. Eye sits 7 m above track; target is 25 m ahead\\n  // and 14 m higher \u2014 giving ~24\u00b0 upward tilt, centred in the FOV-75 frustum.` while the code below sets `smp.p[1] + 3.5`, target `smp.p[1] + 34` over 20 m (`~58\u00b0 up`) and `G.skyViewOverride = { ..., fov: 78 }` then snaps `G.camFov = 75` (the 78-vs-75 mismatch also survives). agentview.js:919: `// on, every cut costs +5 s (js/game.js ~2178).` \u2014 the cut/penalty block is now game.js:3652-3659 (`if (c.cuts >= 4) { c.penalty += 5;`). agentview.js:2444-2445 cli string ends `...survey|rollout> [flags]` while tools/agent.mjs:38,49 also serves `objective` and `model`.",
   "fix": "Rewrite the sky() doc block to the +3.5 m eye / +34 m target / fov-78 geometry (and note or fix the camFov=75 snap), update the cite to js/game.js ~3652, and add objective|model to the agentHelp().cli string; ?v bump.",
   "finder": "triage-completeness",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "HIGH",
   "kind": "UNAPPLIED",
   "file": "index.html",
   "line": 980,
   "claim": "The user-facing HOW TO PLAY camera line still teaches 4 modes (and even the wrong prefix order \u2014 DRIFT sits between FAR and COCKPIT), and the three stale z-index comments triaged JS-BATCH (triage line 86) all survive verbatim.",
   "evidence": "index.html:980: `press <span class=\"key\">C</span> to cycle CHASE &rarr; FAR &rarr; COCKPIT &rarr; HOOD` while CAM_MODES (js/game/tables.js:32-46) is 13 modes with drift third; same fossil at apex.js:160 `// Get or set the player camera mode (CHASE / FAR / COCKPIT / HOOD)`. z-index comments: css/overlays.css:640-644 `Above the main menu (#overlay, z-index 20)... and above the team picker (36)` (no #vsfriend z-index rule exists; it is a top-layer dialog), css/menus.css:391-392 `z-index clears #carsetup (35, css/carsetup.css)` above a rule that sets only `--sheet-w`, css/career.css:198 `30 matches #results/#standings` and :350 `#career at the same z-index as the history sheet` \u2014 all four cite z-index rules deleted in the dialog migration.",
   "fix": "Reword index.html:980 to the real cycle (13 modes, or 'cycle the 13 camera modes / tap CAM for the picker'), fix apex.js:160, and delete/reword the four stale z-index sentences; fold into the next ?v bump. (The track-detail 'real <dialog>' comment pair was consciously re-dispositioned to REVIEW in eea3a737 / ARCHITECTURE-REVIEW.md:297-305 \u2014 not a miss.)",
   "finder": "triage-completeness",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "UNAPPLIED",
   "file": "tests/unit/scenery-api-contract.test.mjs",
   "line": 3,
   "claim": "The circuits-survey JS-BATCH items (triage lines 80-81) were all skipped: the contract test header cites a nonexistent directory and wrong count, and the three circuit elevation comments are still wrong.",
   "evidence": "tests/unit/scenery-api-contract.test.mjs:3: `// scenery callback (24 consumer files in js/tracks/).` \u2014 the directory is js/circuits/ and holds 40 files (`ls js/circuits | wc -l` = 40); doubly flagged (circuits survey + track survey DRIFT-4). Circuits: js/circuits/hungaroring.js:35 `// Undulating amphitheatre (~36 m)` (computed span 38 m), monaco.js:46-47 `// tunnel toward the harbour (~42 m top-to-bottom)` (computed 40 m), portimao.js:44-45 `// ...the biggest authored elevation deltas of any circuit in this batch.` (its batch-sibling Kyalami's 27 m single rise is larger per the survey).",
   "fix": "Header \u2192 '40 consumer files in js/circuits/'; apply the three OLD\u2192NEW comment edits from code-survey-circuits.md (test-file edit needs no ?v; the circuit files do).",
   "finder": "triage-completeness",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "UNAPPLIED",
   "file": "js/game/debrisworld.js",
   "line": 869,
   "claim": "The one partially/wrongly-landed JS-BATCH item: the debrisworld 'caution lives in game.js' fix was triaged as x2 (triage line 47) but the batch fixed only one of the two sites.",
   "evidence": "debrisworld.js:96 was corrected to `// js/game/racecontrol.js and reads`, but debrisworld.js:868-869 still reads `// \u2500\u2500 B1: hazard query \u2014 settled bodies resting ON the racing surface \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\\n// Deterministic read of the side-world consumed by the game.js caution state\\n// machine.` \u2014 the caution state machine was extracted to js/game/racecontrol.js (game.js only delegates via raceCtl).",
   "fix": "At :869, `the game.js caution state` \u2192 `the racecontrol.js caution state`; fold into the next ?v bump.",
   "finder": "triage-completeness",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "UNAPPLIED",
   "file": "js/game/bodyattitude.js",
   "line": 55,
   "claim": "The residual small JS-BATCH/borderline singles were not landed, and the LAT_MAX mirror comment was actually made MORE wrong by this session's own physics-consts extraction (a6fb6ce2).",
   "evidence": "bodyattitude.js:55: `const LAT_MAX     = 22;                // m/s\u00b2 cornering grip (mirrors game.js)` \u2014 LAT_MAX now lives in js/game/physics-consts.js:18 (`LAT_MAX: 22,`) and game.js merely destructures it (game.js:283), so the mirror pointer is stale and the drift risk the triage flagged (line 53) is now cross-file. Also still unapplied: game.js:5767 `// shadow \u2014 only the body mesh is skipped. Bumper hides everything as before.` (no bumper mode exists; its sibling '14 cameras' WAS fixed and now lives at cam-modes.js:40 as '13 cameras'); game.js:4290 write-only `c.wheelAngle`, :821 `if (session !== \"quali\" && !isQuali()) return;` (!A && !A), :4375 unreachable finished-undo; js/data/telemetry.js:1284 `view._delta = series[0];    // gauge scrub uses the primary compare series` (write-only, gauge recomputes live); js/net/rendezvous.js:120-122 normalise `so O->0->? is not a dead end` self-cancelling claim; js/game/store.js:12 `NON-NULL once a write has failed` (noteBroken also fires from get()'s catch at :25); js/game/quali.js:70 `Re-measure with artifacts/tmp/quali-calibrate.mjs` (file does not exist \u2014 artifacts/ is gitignored); js/game/audio.js:606-607 startRain's drizzle-gain param has no caller passing gain (game.js:2349, 2911 call `startRain()`).",
   "fix": "Read `PhysicsConsts.LAT_MAX` in bodyattitude (or at minimum re-point the comment at js/game/physics-consts.js), then land the enumerated one-liners from the surveys' OLD\u2192NEW specs in one batch + single ?v bump. (Spot-checks of what DID land found no wrong applications: all four `state !== \"select\"` conjuncts are gone, #standings really is a `<dialog>` at index.html:723 so the new track-detail.css comment is truthful, and the 6\u21928-speed / FLYING-lap / _ltBase / env-probe game.js comment fixes match the code.)",
   "finder": "triage-completeness",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "HIGH",
   "kind": "DOC-DRIFT",
   "file": "docs/ARCHITECTURE-REVIEW.md",
   "line": 340,
   "claim": "The \u00a78 backlog still lists \"cam modes ~168\" as an open game.js extraction candidate, but commit 7c67f9a0 (this session, after the register was written) extracted it into js/game/cam-modes.js \u2014 the commit message itself calls it \"First of the review's backlogged game.js extractions\", yet the commit updated only CLAUDE.md, not the review.",
   "evidence": "docs/ARCHITECTURE-REVIEW.md:338-340: \"**game.js extraction candidates**, ranked by boundary crossings (\u00a74): garage live preview ~415 ln (blocked on a car-drawing seam), camera disclosure ~324, pre-race screens ~261, cam modes ~168, liveries ~161, sky state ~107.\" \u2014 but js/game/cam-modes.js exists (108 lines, global CamModes) and git show 7c67f9a0 --stat touches CLAUDE.md, js/game/cam-modes.js, tools/manifest.cjs, tests/unit/module-size.test.mjs but no docs/ file.",
   "fix": "OLD: \"camera disclosure ~324, pre-race screens ~261, cam modes ~168, liveries ~161, sky state ~107.\" NEW: \"camera disclosure ~324, pre-race screens ~261, liveries ~161, sky state ~107. (Cam modes ~168 was taken in 2026-08 \u2014 now js/game/cam-modes.js, CamModes.create(G).)\"",
   "finder": "docs-vs-code",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "HIGH",
   "kind": "DOC-DRIFT",
   "file": "docs/ARCHITECTURE.md",
   "line": 630,
   "claim": "The module-contract doc has zero mention of js/game/cam-modes.js \u2014 the newest shipped module (extracted 7c67f9a0, which updated CLAUDE.md only). Its \"js/game/ \u2014 the extracted game modules\" table also has no entry anywhere in the doc for 11 other shipped modules: cam-tune.js, cam-tuner.js, light-presets.js, bodyattitude.js, debrisworld.js, incidentsim.js, agentview.js, agentview-raster.js, music-lib.js, spotify.js, uilayers.js.",
   "evidence": "grep -c per module over docs/ARCHITECTURE.md returns 0 for cam-modes, cam-tune, cam-tuner, light-presets, bodyattitude, debrisworld, incidentsim, agentview, music-lib, spotify, uilayers; the table at lines 621-646 jumps from \"| `cameras.js` | `GameCams` | the 13 player camera modes + the `__apex.view` debug free-cam framing |\" (line 630) to store/career/... with no CamModes row, while js/game/ on disk holds 44 files.",
   "fix": "OLD: table row sequence ending \"| `cameras.js` | `GameCams` | the 13 player camera modes + the `__apex.view` debug free-cam framing |\" with no cam-modes entry. NEW: add \"| `cam-modes.js` | `CamModes` | the CAM button / picker-grid / C-key camera-mode switch UI \u2014 broadcast-only, mutates `camMode`/`camCutT` through `G`; eval-destructures `GameTables` (a `HARD_EDGES` entry in tools/manifest.cjs) |\" plus rows (or a one-line pointer to CLAUDE.md's map) for the 11 other undocumented js/game modules.",
   "finder": "docs-vs-code",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "HIGH",
   "kind": "DOC-DRIFT",
   "file": "docs/ARCHITECTURE-REVIEW.md",
   "line": 208,
   "claim": "The \u00a77 open-defect register's \"A19 residue\" entry lists dead CSS tokens --btn-col and --ped/--act as open, but commit 938aed50 (this session, after the register was written) deleted all of them (plus --btn-edge) from css/overlays.css \u2014 fixed-but-listed-open.",
   "evidence": "docs/ARCHITECTURE-REVIEW.md:208-210: \"**A19 residue.** Dead CSS tokens `--btn-col` (`css/overlays.css:32`) and `--ped`/`--act` (`:465`) have no consumer anywhere;\" \u2014 but grep -n '\\-\\-btn-col\\|\\-\\-ped\\|\\-\\-act\\b' css/overlays.css returns nothing, and 938aed50's message says \"Removed ... the four dead custom properties `--btn-col`/`--btn-edge`/`--ped`/`--act` (zero var() consumers)\" while its diffstat shows css/overlays.css changed and no docs/ file.",
   "fix": "OLD: \"**A19 residue.** Dead CSS tokens `--btn-col` (`css/overlays.css:32`) and `--ped`/`--act` (`:465`) have no consumer anywhere; `css/overlays.css` still carries mutually inconsistent measured cluster widths in comments (the \\\"467px\\\" family) of which at least two are stale.\" NEW: \"**A19 residue.** `css/overlays.css` still carries mutually inconsistent measured cluster widths in comments (the \\\"467px\\\" family) of which at least two are stale (the dead `--btn-col`/`--btn-edge`/`--ped`/`--act` tokens were removed in the 2026-08 cleanup batch).\" \u2014 the 467px comments do survive at css/overlays.css:182,322,460.",
   "finder": "docs-vs-code",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "DOC-DRIFT",
   "file": "docs/ARCHITECTURE.md",
   "line": 658,
   "claim": "The states paragraph says vacuously-true `state !== \"select\"` guards survive in game.js, but commit 938aed50 removed all four; none remain.",
   "evidence": "docs/ARCHITECTURE.md:658-659: \"A few vacuously-true `state !== \\\"select\\\"` guards survive in game.js.\" \u2014 grep -n 'state !== \"select\"' js/game.js returns nothing, and 938aed50's message says \"Dead code: the four always-true `state !== \\\"select\\\"` conjuncts in game.js (nothing assigns \\\"select\\\" \u2014 removing them is behaviour-preserving)\".",
   "fix": "OLD: \"A few vacuously-true `state !== \\\"select\\\"` guards survive in game.js.\" NEW: delete the sentence (or: \"The vacuously-true `state !== \\\"select\\\"` guards that used to survive in game.js were removed in the 2026-08 cleanup.\").",
   "finder": "docs-vs-code",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "DOC-DRIFT",
   "file": "docs/ARCHITECTURE.md",
   "line": 59,
   "claim": "The Reorg section says game.js \"is back over 8,000\" lines \"because no guard bounds the file\" \u2014 both halves are now false: the file is 7,956 lines after this session's physics-consts + cam-modes extractions, and tests/unit/module-size.test.mjs ceilings it at 7,970, a guard the same doc describes ~65 lines later, so the file contradicts itself.",
   "evidence": "docs/ARCHITECTURE.md:58-61: \"`game.js` is back over 8,000 \u2014 extraction moved code out once and nothing stopped it accumulating again, because no guard bounds the file.\" vs wc -l js/game.js = 7956, tests/unit/module-size.test.mjs:40: '\"js/game.js\": 7970,' and docs/ARCHITECTURE.md:127: \"`tests/unit/module-size.test.mjs` is the guard that makes this stick: a per-file line ceiling you LOWER when you extract.\"",
   "fix": "OLD: \"`game.js` is back over 8,000 \u2014 extraction moved code out once and nothing stopped it accumulating again, because no guard bounds the file.\" NEW: \"`game.js` grew back over 8,000 before anything bounded it \u2014 extraction moved code out once and nothing stopped it accumulating again until `tests/unit/module-size.test.mjs` gave every extracted-from file a ratcheted ceiling (see the Deferred follow-ups below); the 2026-08 extractions brought it back under 8,000.\"",
   "finder": "docs-vs-code",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "DOC-DRIFT",
   "file": "docs/ARCHITECTURE.md",
   "line": 82,
   "claim": "The \"game.js pass 2\" follow-up bullet's present-tense inventory \"Four modules are out\" is stale: this session also moved physics-consts.js (a6fb6ce2) and cam-modes.js (7c67f9a0) out of game.js, and its \"next candidates\" list (quali networking block, collisions) no longer matches the crossings-ranked backlog in ARCHITECTURE-REVIEW \u00a78.",
   "evidence": "docs/ARCHITECTURE.md:82-86: \"Four modules are out (`js/game/aerozones.js`, `js/game/skidmarks.js`, `js/game/light-store.js`, `js/game/racecontrol.js`) \u2014 8,178 \u2192 8,009 lines at the time of that pass; ... the next candidates are the quali networking block and collisions.\" \u2014 but js/game/physics-consts.js and js/game/cam-modes.js now exist (both extracted from game.js this session), and docs/ARCHITECTURE-REVIEW.md:338-341 ranks the candidates as garage preview / camera disclosure / pre-race screens / liveries / sky state.",
   "fix": "OLD: \"Four modules are out (`js/game/aerozones.js`, `js/game/skidmarks.js`, `js/game/light-store.js`, `js/game/racecontrol.js`) \u2014 8,178 \u2192 8,009 lines at the time of that pass; ... the next candidates are the quali networking block and collisions.\" NEW: \"Four modules came out in that pass (`js/game/aerozones.js`, `js/game/skidmarks.js`, `js/game/light-store.js`, `js/game/racecontrol.js`) \u2014 8,178 \u2192 8,009 lines then \u2014 and the 2026-08 cleanup added `js/game/physics-consts.js` and `js/game/cam-modes.js`; the remaining candidates are ranked by boundary crossings in ARCHITECTURE-REVIEW.md \u00a78.\"",
   "finder": "docs-vs-code",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "HIGH",
   "kind": "INCOHERENCE",
   "file": "tools/pick-tests.mjs",
   "line": 73,
   "claim": "The session extracted js/game/cam-modes.js (commit 7c67f9a0) but never added it to the pick-tests routing table, so an edit to the CAM picker UI is told to run only tiny+tooling-fast \u2014 never test:camera or test:ui \u2014 and the meta-guard cannot notice.",
   "evidence": "tools/pick-tests.mjs:73 routes `[/^js\\/game\\/(cameras|cam-tune|cam-tuner)\\.js/, [\"camera\"], \"\"]` \u2014 \"cam-modes\" matches no alternative, and no other js/game rule names it, so only the catch-alls at lines 39-41 fire. The file is real and eagerly loaded: tools/manifest.cjs:141 `\"js/game/cam-modes.js\"` and index.html:1548. The meta-guard tests/unit/test-groups.test.mjs:44-55 probes only `${d}/probe.js` per DIRECTORY (\"individual files fall through to their dir's rule\"), so a per-file routing gap inside js/game/ stays green forever.",
   "fix": "Extend the rule at tools/pick-tests.mjs:73 to `(cameras|cam-tune|cam-tuner|cam-modes)` (and consider adding \"ui\" \u2014 cam-modes is a DOM picker grid, exercised by tests/specs/camera.spec.js and menu specs).",
   "finder": "guard-health",
   "verdict": "confirmed",
   "skepticNotes": "CONFIRMED by direct execution: `node tools/pick-tests.mjs js/game/cam-modes.js` returns only tiny+tooling-fast \u2014 never test:camera. The rule at tools/pick-tests.mjs:73 is `(cameras|cam-tune|cam-tuner)` with no cam-modes alternative; the file was extracted in session commit 7c67f9a0 (inside a162b00a..HEAD), is eagerly loaded (tools/manifest.cjs:141, index.html:1548), and its behavior is directly covered by tests/specs/camera.spec.js (\"the CAM button cycles through every mode and wraps\", line 25) which test:camera runs. The meta-guard tests/unit/test-groups.test.mjs:44-51 explicitly checks only directory-level probes (\"individual files fall through to their dir's rule\"), so this per-file gap stays green indefinitely. The finding's evidence, mechanism, and fix all hold on this branch today. | Confirmed by direct reproduction. Running `node tools/pick-tests.mjs js/game/cam-modes.js` yields only tiny + tooling-fast \u2014 no camera or ui group. The rule at tools/pick-tests.mjs:73 is `(cameras|cam-tune|cam-tuner)` with no cam-modes alternative, and no other js/game rule names it. The file was created by in-range commit 7c67f9a0, is eagerly loaded (tools/manifest.cjs:141, index.html:1548), and the meta-guard tests/unit/test-groups.test.mjs:44-55 only probes `${d}/probe.js` per directory, so this per-file gap stays green. The extraction moved code from js/game.js (routed to behaviour/api/circuit/physics) into a file routed to nothing topical \u2014 a genuine regression in test routing coverage introduced by this session."
  },
  {
   "severity": "HIGH",
   "kind": "BUG",
   "file": "tests/specs/ui-button-touch.spec.js",
   "line": 460,
   "claim": "The test \"throttle button visible in button mode\" cannot fail for the reason its name states: the only expect is inside an existence guard, so removing/renaming #btn-throttle makes the test pass on a screenshot alone.",
   "evidence": "tests/specs/ui-button-touch.spec.js:459-463: `const throttleBtn = page.locator(\"#btn-throttle\"); if (await throttleBtn.count() > 0) { await expect(throttleBtn).toBeVisible(); } await page.screenshot(...)`. The comment above it (line 458) states the intent this shape defeats: \"Button mode exposes an explicit GAS button for manual throttle control\". Same never-fail family confirmed by the survey: tests/specs/camera-driving-hooks.spec.js:188-207 computes before/after positions around spin and never asserts them; tests/specs/headless-api.spec.js:290-303 accepts both `r === false` and `typeof r === \"object\"`.",
   "fix": "Delete the `if` and assert unconditionally: `await expect(throttleBtn).toBeVisible();` (Playwright's expect auto-waits; a missing element then fails instead of skipping).",
   "finder": "guard-health",
   "verdict": "confirmed",
   "skepticNotes": "Confirmed on this branch. tests/specs/ui-button-touch.spec.js:459-463 reads exactly as claimed: `const throttleBtn = page.locator(\"#btn-throttle\"); if (await throttleBtn.count() > 0) { await expect(throttleBtn).toBeVisible(); } await page.screenshot(...)`. The only assertion after entering button mode is wrapped in an existence guard, so if #btn-throttle were removed or renamed the count() is 0, the expect is skipped, and the test passes on the screenshot alone \u2014 defeating the stated intent in the comment at line 458 (\"Button mode exposes an explicit GAS button for manual throttle control\"). The element does exist today (index.html:184, `<button id=\"btn-throttle\" ... hidden>`) and is `hidden` by default, so the unguarded `expect(...).toBeVisible()` fix is both viable and meaningful (it would verify button mode actually un-hides it). No skip annotation, TODO, or documented reason for the guard exists. Nearby coverage does not compensate: tests/specs/hud-audit.spec.js:43 asserts `#btn-steer-left, #btn-throttle` with .first(), an OR that #btn-steer-left alone satisfies. One caveat on scope: the test is not fully inert \u2014 if the element exists but stays hidden in button mode, the guarded expect does fire and fail; the never-fail hole applies only to the removed/renamed-element case, exactly as the finding states. Severity HIGH is arguable for a test-quality gap, but the factual claim is accurate. | The cited code exists verbatim today at tests/specs/ui-button-touch.spec.js:459-463: `const throttleBtn = page.locator(\"#btn-throttle\"); if (await throttleBtn.count() > 0) { await expect(throttleBtn).toBeVisible(); } await page.screenshot(...)`. The guard is real and the stated failure mode reproduces: if #btn-throttle were removed or renamed (it is a single static element, index.html:184 `<button id=\"btn-throttle\" class=\"touchbtn pedal\" hidden ...>`), count() would be 0, the expect would be skipped, and the test would pass on the screenshot alone \u2014 silently green through exactly the regression it is named for. git log -L shows the guard has had this shape since the file was created (commit e5a72480); it is not new in the cleanup diff a162b00a..HEAD (that range does not touch this file), and nothing in the file or docs documents the guard as deliberate. One caveat trims the headline slightly: because the element is static DOM, count() is >0 today, so the expect DOES currently run and the test CAN fail if the button exists but is not visible in button mode (e.g. `hidden` not cleared). So \"cannot fail for the reason its name states\" is overstated for the visibility case \u2014 the never-fail hole is specifically the removal/rename case, which the finding's own evidence sentence states correctly. The proposed fix (unconditional `await expect(throttleBtn).toBeVisible()`) is correct and strictly stronger. Core finding stands; severity arguably MEDIUM rather than HIGH given the assertion does execute today."
  },
  {
   "severity": "HIGH",
   "kind": "BUG",
   "file": "tests/unit/coplanar-faces.test.mjs",
   "line": 58,
   "claim": "The coplanar sweep's roster floor is still the pre-fix `>= 24`, so the sweep can silently drop up to 16 of the 40 circuits \u2014 the exact defect its sibling prop-clipping.test.mjs was already fixed for, with a comment naming this failure.",
   "evidence": "tests/unit/coplanar-faces.test.mjs:58: `assert.ok(results.length >= 24, \\`expected >= 24 circuits, got ${results.length}\\`);` \u2014 and a circuit absent from `results` is simply never compared against BASELINE (the loop at lines 61-64 only iterates results). The fixed sibling tests/unit/prop-clipping.test.mjs:64-67 says why: \"The floor is the ROSTER, not a number typed once: `>= 24` kept passing after the roster reached 40\" and asserts `assert.equal(results.length, roster)` with roster = `manifest.cjs`.CIRCUITS.length.",
   "fix": "Copy the sibling's fix verbatim: require tools/manifest.cjs via createRequire and assert `results.length === MANIFEST.CIRCUITS.length` at tests/unit/coplanar-faces.test.mjs:58.",
   "finder": "guard-health",
   "verdict": "confirmed",
   "skepticNotes": "Confirmed. tests/unit/coplanar-faces.test.mjs:58 still asserts `results.length >= 24` while the loop at lines 61-64 only compares circuits present in results, so a sweep that dropped circuits would pass silently. The sibling tests/unit/prop-clipping.test.mjs:64-67 was fixed for exactly this (\"`>= 24` kept passing after the roster reached 40\") and asserts equality against manifest.cjs CIRCUITS.length; coplanar-faces claims deliberately matched semantics but was not given the same fix. Minor mitigations: coplanar-audit.cjs --all iterates env.Tracks.LIST (line 283-284) so today it emits all 40, and the stale-baseline test catches missing baselined circuits \u2014 the defect is a latent guard weakness rather than an actively failing gate, which the finding's wording (\"can silently drop\") states accurately. | The cited code is real \u2014 tests/unit/coplanar-faces.test.mjs:58 does still read `assert.ok(results.length >= 24, ...)` while the fixed sibling tests/unit/prop-clipping.test.mjs:64-67 asserts `results.length === manifest.CIRCUITS.length` (roster = 40). But the finding's load-bearing claim \u2014 that the sweep \"can silently drop up to 16 of the 40 circuits\" \u2014 does not reproduce on this branch. tools/coplanar-baseline.json currently names ALL 40 manifest circuits (verified: 40 keys, zero keys outside the manifest, including 5 zero-valued entries), and the second test in the same file (coplanar-faces.test.mjs:68-77) iterates every BASELINE entry and asserts `assert.notEqual(now, undefined, `BASELINE names unknown circuit \"${id}\"`)` against the sweep's measured map. So if the sweep dropped any circuit today, the suite fails loudly in that second test \u2014 the drop is not silent. The weakness is real but latent/contingent: it only becomes exploitable if zero-cap entries are ever pruned from the baseline (which the file's \"circuit NOT in BASELINE must read 0\" design invites) AND the sweep then drops one of those circuits. Copying the sibling's roster assertion is a reasonable small hardening, but as stated \u2014 HIGH-severity BUG with silent 16-circuit coverage loss today \u2014 the claim is overstated and refuted."
  },
  {
   "severity": "HIGH",
   "kind": "MESS",
   "file": "tools/fixture-consumer-audit.mjs",
   "line": 26,
   "claim": "The fixtures-adoption ratchet sits at FLOOR = 31 while real adoption is 54 (measured with the tool's own IMPORTS_FIXTURES regex), and unlike every sibling ratchet it carries no slack assertion \u2014 so 23 specs could silently migrate OFF the shared fixture before anything fires.",
   "evidence": "tools/fixture-consumer-audit.mjs:26 `export const FLOOR = 31;` vs 54 specs matching `from\\s+[\"']\\./fixtures\\.js[\"']` in tests/*.spec.js today. Its own header (lines 17-18) says \"Raise FLOOR whenever you migrate specs\", and the repo idiom it cites elsewhere carries a two-sided check: tests/unit/module-size.test.mjs:60-71 (\"a ceiling is not left far above the file it guards\", 400-line slack) and tests/unit/comment-citations.test.mjs:131-135 (CITATION_SLACK 15) \u2014 both of which I verified currently pass. This is the one ratchet in the suite sitting far from its real value with no guard on the gap.",
   "fix": "Raise FLOOR to 54 and add a slack assertion in tests/unit/fixture-consumer-audit.test.mjs (fail when adoption - FLOOR exceeds ~5), mirroring module-size's second test.",
   "finder": "guard-health",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "HIGH",
   "kind": "MESS",
   "file": "tests/unit/test-groups.test.mjs",
   "line": 83,
   "claim": "A tests/ directory split (specs/unit/helpers/data) requires coordinated edits to nine guard sites; worse, three guards go silently VACUOUS rather than red because their regexes forbid `/` after `tests/`, so subdir'd script paths simply stop being checked.",
   "evidence": "Flat readdirs that would see zero files: tests/unit/test-groups.test.mjs:83 `fs.readdirSync(path.join(ROOT, \"tests\"))` (coverage-table check \u2014 missing=[] on empty input, passes vacuously), :92-93 (suite counts \u2014 fails loudly), :125 (RENDER_SPECS on-disk \u2014 fails loudly); tests/unit/docs-integrity.test.mjs:23 `ls = (dir, re) => fs.readdirSync(path.join(ROOT, dir))...` used at :236-237 (counts; note :237 matches only `.test.mjs`, so the one `.test.cjs` is already invisible to both count guards) and :347 (glob resolution); tests/unit/test-coverage-audit.test.mjs:60-63 flat readdir + `testFiles.length > 100` (loud tripwire); tools/test-coverage-audit.mjs:39-41 flat readdir; tools/fixture-consumer-audit.mjs:52-55 `readSpecs()` flat readdir. Silently-vacuous regexes: tests/unit/docs-integrity.test.mjs:343 `/tests\\/[A-Za-z0-9_*-]+\\.(?:spec\\.js|test\\.(?:mjs|cjs))/` and tests/unit/test-groups.test.mjs:154 `/tests\\/([a-z0-9-]+)\\.spec\\.js/` \u2014 neither character class admits `/`, so `tests/specs/foo.spec.js` in a package.json script matches nothing and the spec-exists and project-pin guards check nothing. Also to update: every `tests/foo.spec.js` literal in package.json:11-49; playwright.config.js:81 `globalSetup: './tests/helpers/global-setup.js'` (testDir:74 and the `**/name.spec.js` RENDER_SPECS globs at :63 are already subdir-safe, as is testIgnore `**/manual/**`); test-groups.test.mjs:113-114 literal `tests/manual/README.md`; the `..`-relative ROOT/imports in all ~49 unit suites (e.g. test-groups.test.mjs:20 `from \"../tools/pick-tests.mjs\"`, :22 `path.resolve(..., \"..\")`); and tests/specs/menu-baseline.spec.js-snapshots/ which resolves relative to its spec file.",
   "fix": "Before any split: replace the flat readdirs with a shared recursive walker, widen the two script-path regexes to admit `/`, rewrite package.json paths, and move globalSetup/snapshots with their files \u2014 otherwise the split lands with the coverage-table, script-path and project-pin guards passing on empty input.",
   "finder": "guard-health",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "MESS",
   "file": "tests/unit/docs-integrity.test.mjs",
   "line": 399,
   "claim": "A tools/ subdirectory reorganization degrades the tools-index guards silently and breaks five guard suites' hardcoded tool paths loudly; the exact sites are enumerable.",
   "evidence": "Silent degradation: tests/unit/docs-integrity.test.mjs:399 `fs.readdirSync(path.join(ROOT, \"tools\"))` (\"the tools index lists every tool\") checks TOP LEVEL only \u2014 a tool moved into a subdir is no longer required to be listed in tools/README.md, only its directory name is; and :385-388 (\"every tool named ... exists on disk\") walks exactly ONE subdir level and matches by bare basename, so two-level nesting or basename collisions go unchecked. Loud breakage on move: tests/unit/load-order.test.mjs:24 `require(\"../tools/manifest.cjs\")` and :149 `tools/carview.html`; tests/unit/scenery-api-contract.test.mjs:20 `require(\"../tools/verify-track.cjs\")`; tests/unit/vstd-invariant.test.mjs:40 `from \"../tools/vstd-lint.mjs\"`; tests/unit/prop-clipping.test.mjs:51,58 `tools/clip-baseline.json` + `tools/clip-audit.cjs`; tests/unit/coplanar-faces.test.mjs:45,52 same pair for coplanar; tests/unit/test-coverage-audit.test.mjs:31 and tests/unit/test-groups.test.mjs:20 import their tools; tools/pick-tests.mjs:93 `/^tools\\/manifest\\.cjs/`; and every `node tools/<name>` in package.json scripts (run-playwright, graph-parity, float-audit, clip-audit, test-coverage-audit, pick-tests, test-bg, rtc-e2e*, ab-lighting). Stale `tools/...` references in docs and source comments WILL be caught (PATH_RE at docs-integrity:69 admits `/`), so docs drift is covered \u2014 the guard gaps are only the two index tests.",
   "fix": "If tools/ is split: make docs-integrity:395-402 walk recursively (excluding `_`-prefixed scratch per .gitignore) and make :385-388 record repo-relative paths instead of bare basenames; update the hardcoded paths in the five guard suites and package.json in the same commit.",
   "finder": "guard-health",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "DOC-DRIFT",
   "file": "tests/unit/scenery-api-contract.test.mjs",
   "line": 3,
   "claim": "Guard files' own headers drifted and nothing can catch them: scenery-api-contract still says the contract serves \"24 consumer files in js/tracks/\" (it is 40 files in js/circuits/; js/tracks/ holds the engine), and test-groups still claims \"98 specs sit flat in tests/\" against 111 on disk.",
   "evidence": "tests/unit/scenery-api-contract.test.mjs:3 `// scenery callback (24 consumer files in js/tracks/).` and :11 `// removals/renames break circuit files and need a sweep of js/tracks/*.js).` \u2014 invisible to docs-integrity because PATH_RE (docs-integrity.test.mjs:69) requires a file extension, and the `*` form is skipped at :74. tests/unit/test-groups.test.mjs:6-7 `98 specs sit flat in tests/` \u2014 present-tense claim, 111 specs on disk (`ls tests/*.spec.js | wc -l` = 111). Both were flagged in the survey (code-survey-tests.md DRIFT 6 and 8) and the session's cleanup batch (938aed50) did not touch them.",
   "fix": "Two comment edits: \"40 consumer files in js/circuits/\" (both lines) and \"111 specs sit flat\" \u2014 or drop the number from test-groups' header since the file itself asserts the real count.",
   "finder": "guard-health",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "DOC-DRIFT",
   "file": "playwright.config.js",
   "line": 54,
   "claim": "The RENDER_SPECS comment claims \"a coverage-audit npm script asserts every spec lands in exactly one project\" \u2014 no such assertion exists; the real guards check only the two narrow directions, so a new GL-heavy spec left out of RENDER_SPECS silently runs with headless-level concurrency (the exact SwiftShader-thrash reintroduction the list exists to prevent).",
   "evidence": "playwright.config.js:53-54: \"Keep this list exhaustive against tests/*.spec.js (a coverage-audit npm script asserts every spec lands in exactly one project).\" tools/test-coverage-audit.mjs never reads playwright.config.js (it only maps test files to test:* scripts, lines 21-35). What actually exists: tests/unit/test-groups.test.mjs:117-130 catches a RENDER_SPECS name with no file, and :132-163 catches a pinned --project script naming a spec outside its project \u2014 neither classifies an unlisted screenshot spec. By construction (headless testIgnore: RENDER_SPECS at config:103) every spec lands in exactly one project; \"exhaustive\" is the unenforced part.",
   "fix": "Rewrite the comment to name the two real guards in tests/unit/test-groups.test.mjs and state that render-spec classification is manual \u2014 or add a heuristic guard (spec contains `screenshot(`/`toHaveScreenshot` but is not in RENDER_SPECS) to test-groups.",
   "finder": "guard-health",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "HIGH",
   "kind": "DOC-DRIFT",
   "file": "js/render/gfx.js",
   "line": 12,
   "claim": "The 86-line gfx.js header still describes the retired WebGPU-Phase-0 era: it claims a script-tag load order that no longer exists, frames the seam as WebGPU-only, and never mentions TLX \u2014 and the prior render survey certified this file 'CLEAN \u2014 seam contract doc is accurate', so it is unitemized.",
   "evidence": "gfx.js:2 titles the file 'the renderer backend seam (WebGPU migration, Phase 0)'; gfx.js:12-13 claims 'Load order in index.html:\\n *     wgsl-chunks.js -> wgx.js -> gfx.js -> ... -> game.js' \u2014 but index.html has NO wgsl-*/wgx tags (grep confirms; tools/manifest.cjs:66 says 'js/render/webgpu/* and js/render/three/* are NOT here'), and the backends now load AFTER gfx.js via game.js loadBackendScripts(). gfx.js:4-5 says the seam is used 'only when apex26.gfxBackend=webgpu explicitly opts into WebGPU', and the create() docblock (line 95) says it 'Resolves to a ready WebGPU backend' \u2014 yet the code at lines 115-119 handles pref === \"three\" -> TLX.create(), the now-primary alternate backend the header never mentions.",
   "fix": "Rewrite the header's opening 15 lines and the create() docblock to the current three-backend reality: GLX default, TLX via gfxBackend=\"three\", WGX (frozen) via \"webgpu\", both deferred-injected by game.js with no script tags. Keep the interface-contract section, noting TLX also implements it.",
   "finder": "mess-hunter",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "INCOHERENCE",
   "file": "js/game.js",
   "line": 39,
   "claim": "The first prose a cold reader meets after game.js's DOM map \u2014 the renderer-selection comment \u2014 describes a two-backend world (WebGPU else WebGL2) that the code six lines below contradicts.",
   "evidence": "game.js:39-41: '// Renderer selection. WebGPU is OPT-IN: only tried when the user set // apex26.gfxBackend=webgpu AND the browser exposes WebGPU. Anything else \u2014 and // ANY WebGPU init failure \u2014 uses the WebGL2 backend (GLX) exactly as before'. Directly below, BACKEND_FILES (line 49) contains a `three:` group, and line 104 reads `const optIn = !phone && (pref === \"three\" || (pref === \"webgpu\" && navigator.gpu));`. The 3-line file header has the same era problem: game.js:3 says 'optionally Gfx/WGX for WebGPU' with no TLX, and line 1 still lists 'HUD' as this file's job though the HUD lives in js/game/hud.js.",
   "fix": "Rewrite game.js:39-44 to name both opt-in backends (TLX via \"three\", WGX via \"webgpu\") and update the line-1..3 file header (drop 'HUD', add TLX).",
   "finder": "mess-hunter",
   "verdict": "confirmed",
   "skepticNotes": "Confirmed on this branch. js/game.js:39-41 says renderer selection is WebGPU-else-GLX (\"Anything else ... uses the WebGL2 backend (GLX)\"), but line 104 reads `const optIn = !phone && (pref === \"three\" || (pref === \"webgpu\" && navigator.gpu));` and BACKEND_FILES (lines 50-68) includes a nine-file `three:` group \u2014 TLX is a second opt-in backend the lead comment omits, and the adjacent comment at lines 102-103 documents it, making the block internally contradictory. The file header at line 3 (\"optionally Gfx/WGX for WebGPU\") has the same staleness, and line 1 still lists \"HUD\" though the HUD lives in js/game/hud.js. Stale comment, not a documented deliberate choice; severity LOW is proportionate."
  },
  {
   "severity": "LOW",
   "kind": "MESS",
   "file": "js/render/three/tlx.js",
   "line": 33,
   "claim": "tlx.js opens with a 117-line milestone changelog (M1, M3-M8 STATUS paragraphs) whose earliest entry states the opposite of the later ones \u2014 the header is a stack of contradictory eras, not a description of the file.",
   "evidence": "tlx.js:1 titles the file '(migration milestone 1)'. tlx.js:33-38 (M1 STATUS): 'every other contract member is present as a SAFE no-op so game.js can issue the full frame protocol without crashing. M2+ replace the no-ops subsystem by subsystem.' Then tlx.js:39 (M3 STATUS): 'the TSL lit core is live', and tlx.js:100 (M8 STATUS): 'the post-processing chain is live' \u2014 i.e. nothing is a no-op any more; a top-to-bottom reader is told two incompatible states of the world. The prior render survey flagged only stale glx.js:N citations in this file, not the header structure.",
   "fix": "Collapse the M1-M8 log into one current-state architecture paragraph (everything is live except PCSS/MSAA); move the milestone history to spike/ADOPTION-PLAN.md or docs/archive where the migration record lives.",
   "finder": "mess-hunter",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "MESS",
   "file": "js/game/cam-modes.js",
   "line": 9,
   "claim": "js/game/ now has a three-way module-creation idiom split (`const X = (function(){`, `const X = (() => {`, `window.X = (function(){`) plus two header styles, and both files this cleanup session created adopted the minority shape \u2014 including a create(G) module whose direct siblings use the majority shape.",
   "evidence": "Session-new cam-modes.js:9 is `window.CamModes = (function () {` with a bare `\"use strict\"` first line, while its look-alike-cluster siblings are cam-tuner.js:15 `const CamTunerPanel = (function () {` and cam-tune.js:25 `const CamTune = (function () {`, both with `/* Apex 26 \u2014 */` headers. racecontrol.js:32 and light-store.js:27 use a third shape, `const RaceControl = (() => {`. 11 of ~50 js/game files use the window.X/bare-strict style (ariastate, cam-modes, light-presets, menunav, music-lib, physics-consts, scrollfade, sheetshape, spotify, topmodal, uilayers); js/data splits the same way (lastrace/live/schedule/standings have no header comment at all). CLAUDE.md's only rule ('a \"use strict\" IIFE assigning one global') can't tell a contributor which shape is house style.",
   "fix": "Pick one canonical shape (the window.X form is the safer bet \u2014 a top-level const is a global binding but NOT a window property, which matters for `typeof window.X` probes and test monkey-patching) and document it in CLAUDE.md's new-file checklist; converge stragglers opportunistically.",
   "finder": "mess-hunter",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "MESS",
   "file": "js/render/glx.js",
   "line": 26,
   "claim": "The 'is this a phone' expression is copy-pasted byte-for-byte in four files, and a fifth module defines mobile a different way \u2014 five definitions of one concept with no shared helper, outside the known clamp/lerp/wrapDelta backlog.",
   "evidence": "The identical two-line expression `/iPhone|iPad|iPod|Android/i.test(navigator.userAgent) || (navigator.maxTouchPoints > 1 && /Mac/.test(navigator.userAgent))` appears at glx.js:26-27 (IS_MOBILE render caps), game.js:100-101 (the backend phone gate), wgx.js:91-92, and liverytex.js:18-19 (texture resolution). input.js:1074-1084 answers the same question with a semantically different instrument: `window.matchMedia(\"(pointer: coarse)\")`.",
   "fix": "Hoist one `isPhoneUA()` helper (js/log.js or js/mat4.js load first, or a tiny js/env.js with a manifest entry) and have the four UA-sniff sites read it; leave input.js's pointer-coarse check, which is intentionally live-updating and about input, not hardware tier.",
   "finder": "mess-hunter",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "MESS",
   "file": "js/game.js",
   "line": 2712,
   "claim": "The codebase accretes 'tombstone' comments that narrate removed code or previous comments rather than describing anything present \u2014 and this cleanup session added another one, so every future cleanup grows game.js's comment mass while shrinking its code, eating the module-size ratchet the repo uses to force extraction.",
   "evidence": "game.js:2711-2712 (added this session in 395f8bd6): '// (setLightTune is a hoisted function, exposed as a plain shorthand below \u2014 // the deferred-arrow copy that used to sit here was a dead duplicate key.)'. Same genus already in place: game.js:7042-7046 'This banner used to read \"Photo mode (free-fly camera)\" and had done since photo mode was extracted...' (5 lines about an old banner), game.js:2438 'The old comment here claimed...', agentview.js:2234-2236 '(frame()/plan()/worldModel()/visible() were exported as deprecated aliases for a while; every caller has been migrated...)'. tests/unit/module-size.test.mjs ratchets game.js by line count, which these paragraphs consume.",
   "fix": "House rule for the next pass: when deleting dead code or fixing a stale comment, write the present tense only \u2014 the removal's story belongs in the commit message, not a tombstone. Sweep the existing 'used to sit here / old comment here claimed / banner used to read' instances.",
   "finder": "mess-hunter",
   "verdict": "accepted-unverified"
  },
  {
   "severity": "LOW",
   "kind": "UNAPPLIED",
   "file": "js/game/bodyattitude.js",
   "line": 55,
   "claim": "This session moved LAT_MAX out of game.js into the new PhysicsConsts global but left bodyattitude.js's hand-mirrored copy pointing at the old home \u2014 the extraction created fresh drift in the one file that duplicates the constant.",
   "evidence": "bodyattitude.js:55: `const LAT_MAX     = 22;                // m/s\u00b2 cornering grip (mirrors game.js)` \u2014 but the value now lives at physics-consts.js:18 (`LAT_MAX: 22,`), moved there by commit a6fb6ce2 of this session; game.js no longer defines it (game.js:283 destructures it FROM PhysicsConsts). physics-consts.js:114 even cross-references bodyattitude ('chassis cornering-lean cap now lives in js/game/bodyattitude.js as ROLL_MAX'), so the session touched this exact seam without closing it. If PhysicsConsts.LAT_MAX is ever retuned, the visual roll model silently stays calibrated to 22 and the comment sends the maintainer to a file that no longer holds the number.",
   "fix": "Read the global: `const LAT_MAX = PhysicsConsts.LAT_MAX;` (physics-consts.js loads before bodyattitude.js per tools/manifest.cjs), or at minimum repoint the comment to js/game/physics-consts.js. The hand-mirror was known backlog; the stale pointer is new.",
   "finder": "mess-hunter",
   "verdict": "accepted-unverified"
  }
 ],
 "clusters": [
  {
   "name": "1. tests/ split \u2014 tests/specs/ (111) + tests/unit/ (49) + tests/helpers/ (7) \u2014 GO",
   "rationale": "160 test files sit flat; every tool that navigates them re-derives the spec/unit/helper axis by regex (test-groups.test.mjs:83-84 'readdirSync(...tests).filter(/\\.(spec\\.js|test\\.(mjs|cjs))$/)', coverage-audit:39-40 same, docs-integrity:236-237 'ls(\"tests\", /\\.spec\\.js$/)'). Counts verified on disk: 111 *.spec.js, 48 *.test.mjs, 1 *.test.cjs (shared-track-foundation-characterization \u2014 goes to unit/), 7 helpers (fixtures.js, track-helpers.js, f1-api-mock.js, qr-camera.js, output-paths.js, global-setup.js, live-reporter.js). tests/data/ and tests/manual/ STAY. Playwright mechanics survive: testDir './tests' discovers recursively (playwright.config.js:74), RENDER_SPECS are already '**/${n}.spec.js' globs (config:63) so they keep matching under tests/specs/, testIgnore '**/manual/**' unaffected, and snapshots default to spec-adjacent so menu-baseline.spec.js-snapshots/ moves WITH its spec into tests/specs/ (tracks-visual computes its own snapshots dir from its file path, tests/specs/tracks-visual.spec.js:26 \u2014 moves free). This is the change most worth doing in a full restructure and the guard net makes it completable: docs-integrity's PATH_RE (docs-integrity.test.mjs:69) fails on every stale tests/<file> reference in live docs, skills, and js/tools/tests source comments \u2014 478 reference lines across 139 files, all mechanically flagged.",
   "moves": [
    {
     "from": "tests/*.spec.js (all 111)",
     "to": "tests/specs/"
    },
    {
     "from": "tests/specs/menu-baseline.spec.js-snapshots/",
     "to": "tests/specs/menu-baseline.spec.js-snapshots/ (Playwright spec-adjacent convention)"
    },
    {
     "from": "tests/*.test.mjs (48) + tests/unit/shared-track-foundation-characterization.test.cjs",
     "to": "tests/unit/"
    },
    {
     "from": "tests/{fixtures,track-helpers,f1-api-mock,qr-camera,output-paths,global-setup,live-reporter}.js",
     "to": "tests/helpers/"
    },
    {
     "from": "tests/data/, tests/manual/",
     "to": "(stay \u2014 no move)"
    }
   ],
   "lockstep": [
    "package.json \u2014 every tests/<name> path in the ~28 test:* scripts (lines 11-53) becomes tests/specs/ or tests/unit/; globs like tests/physics-*.spec.js \u2192 tests/specs/physics-*.spec.js",
    "playwright.config.js:81 globalSetup './tests/helpers/global-setup.js' \u2192 './tests/helpers/global-setup.js'; :137 reporter './tests/helpers/live-reporter.js' \u2192 './tests/helpers/live-reporter.js'; testDir/testMatch/RENDER_SPECS/testIgnore need NO change (verified globs)",
    "66 spec files: import '../helpers/fixtures.js' etc. (was './fixtures.js' \u2014 counted via grep); 2 specs + ~38 unit suites re-derive ROOT '..' \u2192 '../..'; tests/helpers/track-helpers.js:45 require('../tools/manifest.cjs') \u2192 '../../tools/manifest.cjs'; tests/unit/prop-clipping.test.mjs:51+:66 and coplanar-faces.test.mjs:45 ROOT/tools paths",
    "tools/test-coverage-audit.mjs:39 flat readdirSync(tests) \u2192 walk tests/specs + tests/unit; its token regex :25 already matches subdir paths",
    "tools/fixture-consumer-audit.mjs:37 IMPORTS_FIXTURES /\\.\\/fixtures\\.js/ \u2192 /\\.\\.\\/helpers\\/fixtures\\.js/ and :52 readSpecs dir \u2192 tests/specs (else the FLOOR=31 ratchet reads adoption as 0 and goes red)",
    "tests/unit/test-groups.test.mjs \u2014 readdirs :83, :92-93, :125-126 \u2192 new dirs; VACUOUS-PASS TRAP at :154 /tests\\/([a-z0-9-]+)\\.spec\\.js/ stops matching tests/specs/... so the project-pin guard silently passes empty \u2014 must gain the specs/ segment; import '../tools/pick-tests.mjs' \u2192 '../../' after the file itself moves to unit/",
    "tests/unit/docs-integrity.test.mjs \u2014 :236-237 ls('tests',...) \u2192 specs/unit; VACUOUS-PASS TRAP at :343 /tests\\/[A-Za-z0-9_*-]+\\./ has no '/' in its char class, so the 'every npm test:* group names specs that exist' guard matches nothing after the move \u2014 extend it; :99-100 SOURCE_EXEMPT fake paths and :52 PLACEHOLDERS stay as placeholders",
    ".github/workflows/ci.yml \u2014 UNGUARDED: sweeps filter regex 'tests/(prop-clipping|road-under-floor|coplanar-faces|shared-track-foundation)' stops matching tests/unit/..., so sweep-suite edits silently stop triggering sweeps (the exact fail-open the file warns against) \u2192 tests/unit/(...)",
    "docs/TESTING.md \u2014 24 tests/ path lines + the '(\\d+) root Playwright spec' count phrasing (test-groups:96); coverage-table rows are BARE filenames checked by doc.includes(f) (test-groups:85) so they survive unchanged",
    "CLAUDE.md + .claude/skills/*/SKILL.md \u2014 20 distinct tests/ paths in skills (verified list incl. tests/unit/load-order.test.mjs, tests/specs/career.spec.js); docs-integrity 'skills reference only files that exist' (:208) and 'source comments...' (:173) enforce completion across the 478 reference lines",
    "tools/pick-tests.mjs RULES \u2014 NO change: only /^tests\\// (:96) matches tests, still true; tools/test-bg.mjs \u2014 NO change (groups resolve via npm scripts only, verified)"
   ],
   "risk": "Large but mechanically bounded. The real dangers are the three guards that fail VACUOUS instead of red \u2014 test-groups:154 project-pin check, docs-integrity:343 npm-group existence check, and ci.yml's sweeps filter \u2014 plus package.json tools of habit; everything else that can be missed turns a guard red (docs-integrity PATH_RE, load-order untouched, coverage audit, fixture ratchet). Land as ONE commit (git mv for history; git log --follow afterwards), never while a background Playwright run is in flight (the test server serves the working tree \u2014 CLAUDE.md hard edge), and no ?v bump is needed since js/css are untouched. Verification without browsers: node --check on moved helpers, then npm run test:tooling-fast + npm run test:audit locally cover every renamed-path guard; the browser groups only confirm what those already prove.",
   "feasibility": {
    "feasible": true,
    "gaps": [
     "SILENT FAIL-OPEN, not in lockstep: tools/fit-audit.mjs:27 and tools/menu-fit.mjs:32 both do `try { ({ setupApiMocks } = await import(\"../tests/helpers/f1-api-mock.js\")); } catch { /* hub degrades to live/offline data */ }`. After f1-api-mock.js moves to tests/helpers/, the import throws, the catch swallows it, and both audit tools silently run without API mocks. docs-integrity's PATH_RE cannot flag it: its lookbehind (?<![A-Za-z0-9_./-]) rejects matches preceded by '/', and the reference is written as '../tests/helpers/f1-api-mock.js'. No guard turns red \u2014 must be added to the lockstep explicitly.",
     "SILENT SYMMETRIC DRIFT, not in lockstep: tests/helpers/output-paths.js:7 `const REPO_ROOT = resolve(import.meta.dirname, \"..\")` \u2014 after moving to tests/helpers/, '..' resolves to tests/, so every gallery lands in tests/artifacts-<port>/ instead of <repo>/artifacts/. Its own guard spec moves in lockstep with the bug: tests/specs/output-paths.spec.js:12 asserts `resolve(import.meta.dirname, \"..\", \"artifacts\", ...)` \u2014 after that spec moves to tests/specs/, ITS '..' is also tests/, so the spec stays GREEN while the artifacts/ output contract (CLAUDE.md 'Output dirs') is violated. Both the helper's REPO_ROOT and the spec's expectation must go to '../..' together. Same ROOT fix needed in tests/helpers/qr-camera.js:30 (`ROOT = path.resolve(..., \"..\")`).",
     "tests/manual/ stays but imports moved helpers \u2014 not in lockstep: tests/manual/inspect.spec.js:20 `import { galleryPath, galleryUrl } from \"../output-paths.js\"` must become '../helpers/output-paths.js', and tests/manual/galleries/{track-lap-audit,track-trace,all-tracks-buildings}.spec.js import '../../output-paths.js' \u2192 '../../helpers/output-paths.js'. Manual suites are testIgnore'd (playwright.config.js:80) and never run in CI, so these break silently until a human runs them. Also tests/manual/README.md:8-9 says the suite lives 'one directory up in tests/*.spec.js' \u2014 the '*' keeps it out of PATH_RE (char class has no '*'), so that prose goes stale unflagged.",
     "tests/specs/f1-track-accuracy.spec.js:18 `new URL(\"./data/f1-circuit-reference.geojson\", import.meta.url)` \u2014 tests/data/ stays put while the spec moves to tests/specs/, so this must become '../data/...'. Not in the lockstep, and NOT covered by the cluster's 'tooling-fast + test:audit prove everything' verification claim: it only fails inside the f1-track-accuracy render spec, i.e. a browser group. Same class as the fixture imports but a data path, so a grep for '../helpers' migration will not find it.",
     "Hard-coded cross-file reads inside guard tests, not in the lockstep enumeration (fail red in tooling-fast, so loud, but must be edited or the ONE-commit landing is red): tests/unit/span-kinds.test.mjs:46 `read(\"tests/specs/agent-view.spec.js\")` \u2192 tests/specs/...; tests/unit/docs-integrity.test.mjs:217 `read(\"tests/unit/scenery-api-contract.test.mjs\")` \u2192 tests/unit/...; tests/unit/test-coverage-audit.test.mjs:60-63 flat `readdirSync(path.join(ROOT, \"tests\"))` with the anti-vacuous `assert.ok(testFiles.length > 100)` (the cluster listed only the tool, tools/test-coverage-audit.mjs:39 \u2014 the test duplicates the scan and must be walked to specs/+unit/ as well).",
     "tests/unit/fixture-consumer-audit.test.mjs synthetic fixtures encode the OLD import shape (e.g. :15 `[\"smoke.spec.js\", 'import { test } from \"./fixtures.js\";']`) \u2014 when IMPORTS_FIXTURES in tools/fixture-consumer-audit.mjs:37 changes to match '../helpers/fixtures.js', these unit-test inputs must change with it or the audit's own unit test goes red. Loud, but missing from the lockstep.",
     "docs/TESTING.md count-phrase coupling is tighter than stated: test-groups.test.mjs:92-93 counts via flat readdirSync(tests) and matches the doc phrases '(\\d+) root Playwright spec' and '(\\d+) `node --test` unit suites' (:96-97), and docs-integrity.test.mjs:236-249 does the same for CLAUDE.md/README.md ('(\\d+) Playwright specs'). After the split the word 'root' is wrong and the readdirs return 0 \u2014 the assertion `claimed.length` at test-groups:100 means DELETING the phrase also fails. The rewrite must keep a count phrase the (updated) regexes still match, in docs/TESTING.md, CLAUDE.md AND README.md simultaneously.",
     "Minor but worth pinning in the plan: tools/import-circuit-path.mjs:46 and ~15 other tools/ files carry prose references to flat tests/<name> paths (tools/rtc-e2e.mjs:6-8, tools/tune-sweep.mjs:22-39, tools/clip-audit.cjs:249, tools/ab-lighting.mjs:18, tools/assets.mjs:51, tools/vstd-lint.mjs:208, tools/measure-props-over-road.mjs:9, tools/output-paths.mjs:2, tools/README.md rows) \u2014 all ARE caught red by docs-integrity's 'source comments reference only files that exist' (:173) and PATH_RE, so they are covered, but they are part of the '478 reference lines' bulk and confirm the one-commit landing must include tools/ prose, not just docs/ and skills."
    ],
    "notes": "Verdict: GO stands \u2014 the move is mechanically completable and the guard net catches most misses red rather than silently. Verified correct in the cluster: recursive testDir + glob RENDER_SPECS (playwright.config.js:55-63,74), globalSetup/reporter paths (:81,:137), both vacuous-pass traps (test-groups.test.mjs:154, docs-integrity.test.mjs:343 \u2014 confirmed neither regex admits a '/' after tests/), ci.yml:211 sweeps filter fail-open, pick-tests.mjs:96 and test-bg.mjs needing no change, PLACEHOLDERS/SOURCE_EXEMPT staying, and the fixture-audit FLOOR=31 ratchet reading 0 without the readSpecs dir change. The two findings that matter most are the SILENT ones the lockstep missed: (1) tools/fit-audit.mjs:27 + tools/menu-fit.mjs:32 try/catch dynamic imports of ../tests/helpers/f1-api-mock.js (invisible to PATH_RE because of the leading '../'), and (2) the tests/helpers/output-paths.js REPO_ROOT + output-paths.spec.js pair that drifts symmetrically green while writing galleries inside tests/. Also correct the cluster's verification claim: tooling-fast + test:audit do NOT prove everything \u2014 the f1-track-accuracy data-path fix and the manual/ helper imports are only exercised by browser/manual runs, so those specific edits need eyeball/grep verification (grep for '\\\\.\\\\./tests/', './data/', './output-paths', './fixtures', './qr-camera', './f1-api-mock', './track-helpers' across tests/ and tools/ after the move). Nothing found makes the cluster a bad idea outright."
   }
  },
  {
   "name": "2. game.js extraction \u2014 MUSIC & SOUND panel \u2192 js/game/audio-panel.js \u2014 GO",
   "rationale": "The one genuinely low-crossing block left. js/game.js:6535-6720 (setSound, setMusic, setSfx, setMusicSrc, spotifyReady, syncMusicSrcRow, syncAudioPanel, audioTransport, all 'as-*'/soundbtn/pm-audio wiring, the musicVol/sfxVol/sfxOn/musicSrc lets) plus the boot restore at :7928-7934 ('setSound(soundOn); setMusic(musicEnabled); ... applySrc'). Verified crossings: every inbound need but ONE already exists on G \u2014 $/els (G:2541), state (:2548), trackIdx (:2633), soundOn (:2669 'get soundOn() { return soundOn; }, set soundOn(v) { soundOn = v; }'), store + tickUi (:2678); the single new fa\u00e7ade pair is musicEnabled (grep confirms no getter). Outbound: grep over the whole file shows the ONLY callers of setSound/setMusic/syncAudioPanel/musicSrc outside the block are the boot lines :7928-7934, which move into the module's init \u2014 soundOn's 25+ race-path readers keep reading the game.js closure, the module writes through the existing G setter. spotify.js/menus/hud reach none of it (verified empty grep). Contrast: garage/setup-preview is KNOWN blocked, sky-state is inside render(), pre-race screens are many-crossing \u2014 this block is the exception. ~190 lines off the monolith.",
   "moves": [
    {
     "from": "js/game.js:6535-6720 (setSound...audioTransport + as-*/soundbtn/pm-audio wiring + musicVol/sfxVol/sfxOn/musicSrc state)",
     "to": "js/game/audio-panel.js \u2014 AudioPanel.create(G), hyphenated per the js/game/ naming rule"
    },
    {
     "from": "js/game.js:7928-7934 boot restore (setSound/setMusic/applySrc)",
     "to": "js/game/audio-panel.js init path"
    }
   ],
   "lockstep": [
    "js/game.js \u2014 add G pair: get/set musicEnabled (the only new fa\u00e7ade member); wire AudioPanel.create(G) after the fa\u00e7ade like SkidMarks/RaceControl",
    "tools/manifest.cjs \u2014 FULL entry (slot after js/game/spotify.js:97, well before game.js) + HARD_EDGES pair ['js/game/audio-panel.js','js/game.js'] (game.js calls create(G) at eval \u2014 same pattern as :299 ['js/game/aerozones.js','js/game.js'])",
    "index.html \u2014 <script> tag at the matching manifest position (tests/unit/load-order.test.mjs:41 asserts sequence == MANIFEST.FULL; :63 asserts every js/**/*.js has a manifest entry)",
    "sw.js \u2014 NO edit (precache parses index.html tags; only DEFERRED files need seeding, load-order:119)",
    "tests/unit/module-size.test.mjs:40 \u2014 lower 'js/game.js': 7970 \u2192 ~7790 (the :60 slack guard goes red if the ceiling is left >400 above the file)",
    "CLAUDE.md file layout \u2014 add the audio-panel.js row (docs-integrity:309 'every eagerly-loaded js/game module is named in CLAUDE.md's file layout' is a hard guard)",
    "tools/pick-tests.mjs:78 \u2014 extend /^js\\/game\\/(audio|music-lib|spotify)\\.js/ \u2192 (audio|audio-panel|music-lib|spotify) so an audio-panel edit routes to test:audio",
    "Cache bump LAST: every ?v=N in index.html to max+1 AND version.json to the same N (.claude/skills/bump-cache; load-order:49 asserts they agree)",
    "Run after: npm run test:tiny, test:tooling-fast, then test:audio via node tools/test-bg.mjs (pick-tests routing)"
   ],
   "risk": "Low \u2014 the block is DOM-wiring executed once at eval, not per-frame; handlers are assign-once onclick/oninput so behavior is identical through G getters. The two real hazards: a missed closure reference (node --check cannot see it; it surfaces as a boot ReferenceError which test:tiny catches immediately, and the HARD_EDGES pair prevents the eval-order variant), and firstGesture()/tickUi() at :6493/:6521 which read soundOn/musicEnabled \u2014 leave them in game.js so the closure stays the single owner and the module only writes through G. Do not bump ?v mid-test-run. This is the smallest-risk item in the whole map; do it first if minimizing exposure, or second after the tests split if sequencing by headline value.",
   "feasibility": {
    "feasible": true,
    "gaps": [
     "Boot-restore sequencing is underspecified: the lockstep says the js/game.js:7928-7937 restore 'moves into the module's init' AND that AudioPanel.create(G) is wired 'after the facade like SkidMarks/RaceControl' (~game.js:2798). Those are ~5,100 lines apart today. If setSound(soundOn)/setMusic(musicEnabled)/applySrc run at create time (~2800) they execute before CamModes.create (:7710), syncPointerKind (:7916), DataHub.init (:7924) and loadTrack (:7938) instead of after. Probably benign (GameAudio.init only runs on first gesture, so startMusic no-ops: js/game/audio.js:875 'if (!ctx || ...) return'), but the safe shape is the existing netLobby.wire() pattern \u2014 create(G) wires the DOM, returns an init()/restore() that game.js calls at the old :7928 position. The cluster must pick this explicitly.",
     "The rationale's claim 'spotify.js/menus/hud reach none of it (verified empty grep)' is false: js/game/spotify.js:268 reads it \u2014 'if (typeof syncAudioPanel === \"function\") syncAudioPanel();' inside reopenPanel(). It is functionally inert today (game.js is an IIFE ending '})();' at :7956, so syncAudioPanel is closure-scoped and spotify.js always sees undefined) and stays inert after the move \u2014 but the extractor must NOT 'helpfully' expose syncAudioPanel as a window global or on the AudioPanel global under that bare name, because that would flip a dead branch live and change post-Spotify-redirect behavior. Note it in the module header or fix the spotify call deliberately as a separate decision.",
     "pick-tests routing gap: the panel's DOM is exercised by UI-group specs the lockstep's regex extension does not reach \u2014 tests/specs/menu-survey.spec.js:92-94 clicks #pm-audio then #as-music-off, tests/specs/ui-scale.spec.js:54 lists \"audioset\", tests/specs/ui-button-touch.spec.js:376 lists \"pm-audio\", tests/specs/menu-keyboard.spec.js:214 walks #audioset. Routing js/game/audio-panel.js only to test:audio (audio-smoke + music-library, package.json:28) means a broken handler that only these specs catch is never suggested. Add audio-panel to the ui rule at tools/pick-tests.mjs:75 as well as the audio rule at :78 (RULES are 'deliberately biased toward running too much').",
     "Off-by-one on the stated range: js/game.js:6534 'els.soundbtn.hidden = false;' sits one line BEFORE the 6535 start and belongs to the block (its counterparts :2340/:2871 stay in game.js race-flow code). Trivial, but the extractor working strictly from 'lines 6535-6720' would leave a stray statement or a dangling reference.",
     "The module-size claim in the lockstep is wrong in a way that could confuse the executor: game.js is 7,956 lines, ceiling 7970 (tests/unit/module-size.test.mjs:40). After removing ~190 lines (~7,766), slack is ~204 \u2014 UNDER the 400-line slack trip at :67 ('if (ceiling - n > 400)'), so the guard does NOT 'go red if the ceiling is left' \u2014 nothing forces the lowering. Lower it anyway (to ~7790) because that is the ratchet's stated contract (:14 'you LOWER it when you extract'), but know it is a convention, not a failing test.",
     "New closure-reference risk not on G: the block's eval-time statements need, beyond the one new musicEnabled pair, only globals (GameAudio, MusicLib, SpotifyMusic \u2014 all load before audio-panel's manifest slot after spotify.js at manifest.cjs:97) and existing G members ($, els at G:2541; state :2548; trackIdx :2633; soundOn :2669; store/tickUi :2678) \u2014 verified complete. But the handlers call GameAudio.uiTick() gated on bare `soundOn` ~15 times (:6680-6718); each must become G.soundOn (or the module's own throughG read), and node --check cannot catch a missed one \u2014 only test:tiny boot does, and only for eval-time refs; a click-time miss needs test:audio + the ui specs above."
    ],
    "notes": "Verified against real sources. Lockstep items that check out exactly: (1) load-order guard \u2014 tests/unit/load-order.test.mjs:41 asserts script sequence == MANIFEST.FULL, :63-80 asserts every js/**/*.js has a manifest entry, :129-136 asserts HARD_EDGES ordering, so manifest entry + index.html tag + ['js/game/audio-panel.js','js/game.js'] edge (same pattern as manifest.cjs:299 aerozones) are all required and sufficient; (2) sw.js needs NO edit \u2014 load-order:119-127 only asserts DEFERRED files are seeded, and the SW discovers tagged files by parsing index.html; (3) docs-integrity.test.mjs:309-324 hard-requires 'audio-panel.js' to appear in CLAUDE.md ('for m of manifest.matchAll(/\\\"(js\\\\/game\\\\/[a-z0-9-]+\\\\.js)\\\"/g) ... if (!claude.includes(base)) missing.push'), so the CLAUDE.md layout row is a real guard, not hygiene; the hyphenated name matches that regex; (4) cache bump \u2014 load-order:49-56 asserts uniform ?v= and version.json.build equality; (5) no stale-citation breakage: the file:NNN guard (docs-integrity:103-171) only fails citations PAST EOF, and the highest game.js citation in the repo is js/render/webgpu/wgx.js:2426 citing js/game.js:5429, far below the post-move ~7,766; (6) boot restore confirmed at js/game.js:7928-7937 and is genuinely the only out-of-block caller of setSound/setMusic/syncMusicSrcRow; musicVol/sfxVol/sfxOn/musicSrc have zero readers outside block+restore; soundOn's ~40 race-path readers (e.g. :2348, :3620) and firstGesture/tickUi (:6493, :6521-6527) stay closure-owned as the cluster prescribes, and G:2669 already has the soundOn get/set pair while musicEnabled (declared :261-262) has none \u2014 the single-new-pair claim is correct; (7) playwright.config.js references no js/ source paths, package.json needs no change (test:audio exists at :28), and tests/test-groups assertions are unaffected since no group name changes. Nothing makes the cluster a bad idea; the six gaps above are all fixable within the same commit."
   }
  },
  {
   "name": "3. tools/ subdirectories by README taxonomy \u2014 CONDITIONAL GO, reduced form, LAST",
   "rationale": "tools/ holds ~65 flat files and the README already carries the taxonomy in prose. But the reference surface is the largest in the repo: 768 lines across 196 files name a tools/<file> path (verify-track.cjs alone: 102 refs; manifest.cjs 70; assets.mjs 56; agent.mjs 43; test-bg.mjs 42), and unlike the tests split, part of the breakage is RUNTIME PATH MATH, not greppable strings: nearly every tool derives ROOT via path.resolve(...,'..') (verified in 14+ by pattern, more by variant) and requires siblings relatively (clip-audit.cjs:252 require('./clip-baseline.json'), track-build-vm/harness/output-paths consumers). Honest verdict: a WHOLESALE move is poor value per unit risk; the reduced form is defensible \u2014 move the self-contained families (net/, car-render/, lighting/, capture/), keep the high-fanout infra flat. Deliberate placements the prompt asked about: carview.html STAYS at tools/ root (its 17 '../js/**' script srcs plus tests/unit/load-order.test.mjs:151's replace(/^\\.\\.\\//,'') strip and 26 external refs all assume one level of depth); manifest.cjs STAYS (70 refs; it IS the load-order SSoT); clip-baseline.json + coplanar-baseline.json move WITH their audits to tools/track/ \u2014 their complete reader set is clip-audit.cjs:252, coplanar-audit.cjs:360, tests/unit/prop-clipping.test.mjs:51, tests/unit/coplanar-faces.test.mjs:45 (verified; tools/README.md:49 row too).",
   "moves": [
    {
     "from": "tools/{rtc-e2e,rtc-e2e-3p,rtc-e2e-room}.mjs, tools/{nostr-local,nostr-probe,turn-local}.cjs (nostr-probe is .mjs)",
     "to": "tools/net/"
    },
    {
     "from": "tools/{render-car,audit-parts,audit-aero}.mjs",
     "to": "tools/car/ (carview.html itself STAYS at tools/ root)"
    },
    {
     "from": "tools/{ab-lighting,ssr-probe}.mjs + tools/lighting-campaign/",
     "to": "tools/lighting/ (lighting-campaign becomes tools/lighting/campaign/ ONLY if the one-level guard walk is deepened \u2014 see lockstep; otherwise keep lighting-campaign/ as a sibling)"
    },
    {
     "from": "tools/{apex-capture,motion-capture,carshot,aerial-survey,survey-track}.mjs",
     "to": "tools/capture/"
    },
    {
     "from": "tools/{verify-track,float-audit,clip-audit,coplanar-audit,graph-parity,track-build-vm}.cjs + clip-baseline.json + coplanar-baseline.json + {bake-elevation,import-circuit-path,track-accuracy-validator,refresh-f1-circuit-reference,measure-props-over-road}.mjs",
     "to": "tools/track/ (verify-track's 102 refs make this the most expensive single move \u2014 defer it to the final commit or drop it)"
    },
    {
     "from": "tools/{run-playwright,pick-tests,test-bg,test-coverage-audit,fixture-consumer-audit}.mjs + test-shards.sh",
     "to": "tools/test-infra/ \u2014 WEAKEST CASE: ~40 package.json script lines reference run-playwright and NOTHING guards package.json tools paths (docs-integrity:338 checks only tests/ refs); every miss is a loudly broken npm script, but consider keeping these flat"
    },
    {
     "from": "tools/{menu-fit,fit-audit,ui-scale-axis,layout-audit}.mjs",
     "to": "tools/ui/"
    },
    {
     "from": "tools/manifest.cjs, tools/carview.html, tools/harness.mjs, tools/output-paths.mjs, tools/agent.mjs, tools/apex-eval.mjs, tools/assets.mjs, tools/quick-validate.mjs, tools/vstd-lint.mjs, ...",
     "to": "(stay at tools/ root \u2014 shared infra with the widest fan-in)"
    }
   ],
   "lockstep": [
    "EVERY moved tool: re-derive ROOT ('..' \u2192 '../..') and fix sibling requires/imports \u2014 per-file code audit, node --check catches syntax only, NOT wrong path math",
    "package.json \u2014 test:graph-parity/test:float/test:clip/test:audit/test:pick/test:bg/ab:light/rtc:* lines, plus ALL ~30 run-playwright lines if test-infra/ moves",
    ".github/workflows/ci.yml \u2014 'node tools/verify-track.cjs --all' (sweeps job); the sweeps filter's 'tools/' prefix still matches subdirs (regex is ^tools/)",
    "tests importing tools: test-groups.test.mjs:20 '../tools/pick-tests.mjs'; test-coverage-audit.test.mjs, fixture-consumer-audit consumers; lighting-campaign.test.mjs imports ../tools/lighting-campaign/{config,metrics,io,tune}.mjs; prop-clipping:51/:66, coplanar-faces:45, load-order:24 require('../tools/manifest.cjs') + :149-152 carview read",
    "tests/unit/docs-integrity.test.mjs guards: 'every tool named in tools/README.md exists on disk' (:386-388) walks exactly ONE subdir level \u2014 a two-deep tools/lighting/campaign/ makes its files invisible to the ghost check; 'the tools index lists every tool' (:399) is a top-level readdir, so each new subdir NAME must appear in tools/README.md; PATH_RE (:69) enforces every md/source tools/<file> ref \u2014 the 768-line sweep",
    "tools/README.md \u2014 full re-index (rows keep bare **name** format which the ghost check matches by basename, so rows survive; the prose paths do not); tools/README's own intra-doc paths",
    "CLAUDE.md 'Key commands' block (verify-track, pick-tests, test-bg, assets.mjs) + file-layout tools/ row; docs/TESTING.md; .claude/skills \u2014 22 distinct tool paths across SKILL.md files (docs-integrity:208 enforces)",
    "tools/pick-tests.mjs RULES :93-94 \u2014 /^tools\\/manifest\\.cjs/ and /^tools\\// still match (prefix regexes) \u2014 no change IF manifest stays at root",
    "tools/test-bg.mjs \u2014 no path deps (verified: resolves groups via npm scripts); tools/run-playwright.mjs \u2014 verify its own ROOT if moved"
   ],
   "risk": "Highest-churn, weakest-guard cluster: the docs/skills/source-comment refs are guard-enforced, but the runtime breakage class (ROOT math, sibling requires, hardcoded /opt chromium paths already flagged in the tools survey) has NO structural guard \u2014 a moved capture tool that computes ROOT one level short writes into tools/scratch/ or reads a missing baseline and may exit 0 (the survey's BUG 6 class: several of these tools exit 0 on failure, so a broken move can look green). Sequence it LAST, one family per commit, run npm run test:tooling-fast after each (covers load-order, docs-integrity, test-groups, lighting-campaign imports), and manually smoke each moved tool with a no-op invocation. If appetite is limited, the honest cut line is: do net/, car/, capture/, lighting/ (low fan-in, self-contained), keep track/ and test-infra/ flat \u2014 that captures most of the navigability win at a third of the risk.",
   "feasibility": {
    "feasible": true,
    "gaps": [
     "GUARD-INVISIBLE SPEC IMPORT: tests/specs/lighting-ab.spec.js:15 `import { KNOBS, FREEZE_FLICKER, FREEZE_FLICKER_FILE } from \"../tools/ab-lighting.mjs\"` \u2014 the cluster moves ab-lighting.mjs to tools/lighting/ but the lockstep list never names this spec. It is not in test:tooling-fast, not in CI (only smoke/sweeps/guards run there), and docs-integrity's PATH_RE cannot see it (see the PATH_RE gap below). Breakage surfaces only when someone runs test:webgl/test:ab \u2014 exactly the runs the migration plan forbids during the reorg.",
     "SAME CLASS, SECOND FILE: tests/specs/f1-track-accuracy.spec.js:14 and tests/unit/track-accuracy-validator.test.mjs:7 both `import ... from \"../tools/track-accuracy-validator.mjs\"` \u2014 the cluster moves track-accuracy-validator.mjs to tools/track/ with no lockstep entry. The unit test fails loudly in test:tooling-fast; the Playwright spec (test:scenery group) fails silently until that group is next run.",
     "tests/unit/road-under-floor.test.mjs:29 `require(path.join(ROOT, \"tools\", \"track-build-vm.cjs\"))` \u2014 track-build-vm.cjs is in the tools/track/ move list but this consumer is absent from the lockstep list. It lives in test:sweeps (not tooling-fast), so the cluster's per-commit `npm run test:tooling-fast` validation will NOT catch it; it only fails in CI's sweeps job after push.",
     "tests/unit/scenery-api-contract.test.mjs:20 `require(\"../tools/verify-track.cjs\")` and tests/unit/circuit-def-fields.test.mjs:37 `require(path.join(ROOT, \"tools/verify-track.cjs\"))` \u2014 two verify-track consumers missing from the lockstep list (it names only ci.yml:223 for verify-track). Both are in test:tooling-fast so they fail loudly, but they are edits the plan must schedule with the verify-track move.",
     "STRUCTURAL BLIND SPOT the cluster's risk model understates: docs-integrity.test.mjs:69 PATH_RE has lookbehind (?<![A-Za-z0-9_./-]), so `../tools/x.mjs` import specifiers (preceding char is `/`) and `./sibling.cjs` requires inside tools/ NEVER match. The '768-ref sweep' guard covers prose paths only \u2014 every relative import/require broken by a move is unguarded by any structural test and fails only at execution. This converts several 'guard-enforced' lockstep items into runtime-only breakage.",
     "EXEC LINES half-covered: tests/unit/prop-clipping.test.mjs:58 spawns `path.join(ROOT, \"tools\", \"clip-audit.cjs\")` and tests/unit/coplanar-faces.test.mjs:52 spawns `path.join(ROOT, \"tools\", \"coplanar-audit.cjs\")` \u2014 the lockstep names only the baseline-read lines (prop-clipping:51, coplanar-faces:45). Both spawn paths must move to tools/track/ in the same commit or the sweeps exec a missing file.",
     "INTERNAL PATH LITERALS survive a naive ROOT fix: tools/coplanar-audit.cjs:360 builds its baseline path as `path.join(ROOT, \"tools\", \"coplanar-baseline.json\")` \u2014 after moving to tools/track/ and correcting ROOT to '../..', the literal \"tools\" segment still points at the old location. Same idiom exists in the tests above. `node --check` passes; only execution reveals it (the survey's exit-0 class).",
     "GUARD EROSION, not breakage: docs-integrity.test.mjs:395-402 ('the tools index lists every tool') readdirs the tools/ TOP LEVEL only \u2014 every file moved into a subdir permanently exits the 'must be indexed in tools/README.md' requirement (only the README\u2192disk ghost direction at :386-391 checks one level deep). The reorg silently shrinks index coverage for every moved tool; if the taxonomy lands, this test should be deepened to walk subdirs in the same commit."
    ],
    "notes": "Verdict: CONDITIONAL GO in the reduced form, as proposed \u2014 net/, car/, capture/, lighting/ are genuinely self-contained (rtc-e2e.mjs has no repo-path deps beyond spawn; capture tools' only siblings are harness.mjs/output-paths.mjs which stay at root and need a one-line '../' fix each, all with one-level ROOT derivation confirmed: apex-capture.mjs:37, carshot.mjs:20, ab-lighting.mjs:27). Keeping manifest.cjs, carview.html, harness.mjs, output-paths.mjs, run-playwright at root is correct \u2014 25 test files require ../tools/manifest.cjs and load-order.test.mjs:149-152 hardcodes both tools/carview.html and its one-level ../ strip. The cluster's own honest cut line (drop tools/track/ and test-infra/) is the right call, and my findings sharpen it: tools/track/ carries FIVE unlisted consumers (gaps 1-4 include three track-family ones) plus the internal path-literal class (gap 7), while ci.yml's ^tools/ filter and pick-tests' /^tools\\// prefix rules survive any subdir move unchanged (verified at ci.yml:211, pick-tests.mjs:93-94). The single most important correction to the plan: 'run test:tooling-fast after each family' is NOT sufficient validation \u2014 lighting-ab.spec.js, f1-track-accuracy.spec.js, and road-under-floor.test.mjs all break outside that suite (gaps 1-3), so each family commit needs a targeted `node --test` / import-smoke of the specific consumers named here, and the lighting/ family in particular must fix lighting-ab.spec.js:15 in the same commit. Skill docs (22 tool paths across .claude/skills/*/SKILL.md, verified by grep \u2014 heaviest: car-viewer 12x render-car.mjs, motion-capture 5x, scenery-dress measure-props-over-road) are genuinely guard-enforced by docs-integrity:208-214, so those fail loudly as claimed."
   }
  },
  {
   "name": "4. js/game/ squashed\u2192hyphenated rename (debrisworld\u2192debris-world etc.) \u2014 NO-GO",
   "rationale": "Evaluated honestly: NO-GO. The 14 squashed names (debrisworld, incidentsim, carmesh, bodyattitude, uilayers, topmodal, sheetshape, scrollfade, menunav, ariastate, aerozones, skidmarks, racecontrol, photomode; manifest.cjs:115-147) carry 175 references across md/js/mjs/cjs/html repo-wide (measured grep). The payoff is purely cosmetic \u2014 the globals (DebrisWorld, IncidentSim, ...) don't change, no module boundary moves, no line of behavior changes \u2014 and the repo's own convention EXPLICITLY forbids it: CLAUDE.md Critical conventions, 'the older squashed names (debrisworld.js) are grandfathered \u2014 do not churn them.' A restructure that starts by violating the project's written naming policy to satisfy an aesthetic is churn, not structure. The full lockstep is listed below only to document what was evaluated; the moves list shows the shape of what is being declined.",
   "moves": [
    {
     "from": "js/game/debrisworld.js, js/game/incidentsim.js, js/game/carmesh.js, js/game/bodyattitude.js, js/game/uilayers.js, js/game/topmodal.js, js/game/sheetshape.js, js/game/scrollfade.js, js/game/menunav.js, js/game/ariastate.js, js/game/aerozones.js, js/game/skidmarks.js, js/game/racecontrol.js, js/game/photomode.js",
     "to": "(declined) js/game/debris-world.js ... js/game/photo-mode.js"
    }
   ],
   "lockstep": [
    "(what it WOULD have cost \u2014 the reason for the NO-GO) tools/manifest.cjs FULL entries :115-147 + HARD_EDGES pairs :299-301 (['js/game/aerozones.js','js/game.js'] etc.)",
    "index.html \u2014 14 <script> tags + full ?v=N bump + version.json (load-order:41/:49); sw.js precache follows tags automatically",
    "tools/pick-tests.mjs RULES \u2014 at least :75 (scrollfade|menunav|ariastate|topmodal|uilayers), :81 (debrisworld|incidentsim), :82 (carmesh|bodyattitude|photomode), plus aerozones/skidmarks/racecontrol rows",
    "CLAUDE.md file layout + naming-convention paragraph itself (it names debrisworld.js as the grandfathered example); docs-integrity:309 js/game layout guard; docs/ references within the 175-line sweep; comment citations by bare basename (docs-integrity:147 byBase) silently STOP resolving rather than fail \u2014 a quiet loss of guard coverage",
    "git history: 14 renames force git log --follow forever on the most-touched gameplay modules"
   ],
   "risk": "NO-GO. Zero functional payoff against 175 reference edits, a mandatory cache-bump commit, permanently degraded blame/log ergonomics on hot files, one class of guard (bare-basename line citations) that degrades silently rather than failing, and a direct contradiction of CLAUDE.md's written rule. If naming consistency matters, the enforceable version already exists and costs nothing: the convention that NEW js/game files are hyphenated is stated in CLAUDE.md and observed (light-store.js, cam-tune.js, steer-tuning.js); optionally add a tiny unit guard asserting any FUTURE js/game file matches /^[a-z0-9]+(-[a-z0-9]+)*\\.js$/ with the 14 grandfathered names allow-listed \u2014 that freezes the problem at its current size without churning a single reference.",
   "feasibility": {
    "feasible": false,
    "gaps": [
     "Hard-coded VM loader missed: tests/unit/race-control.test.mjs:41-42 loads the file by literal path \u2014 `vm.runInContext(readFileSync(join(ROOT, \"js/game/racecontrol.js\"), \"utf8\"), ctx, { filename: \"js/game/racecontrol.js\" })`. A rename makes this unit suite throw ENOENT; it is in no lockstep row.",
     "CARVIEW manifest subset + tools/carview.html missed: tools/manifest.cjs:194 lists \"js/game/carmesh.js\" in CARVIEW, and tools/carview.html:74 has `<script src=\"../js/game/carmesh.js\">`; tests/unit/load-order.test.mjs:148-153 asserts carview.html tags equal MANIFEST.CARVIEW. The lockstep covered only FULL:115-147 and HARD_EDGES:299-301, not CARVIEW.",
     "Skill files are a GUARDED surface, not just docs: tests/unit/docs-integrity.test.mjs:208-214 (\"skills reference only files that exist\") fails on .claude/skills/ui-menu-a11y/SKILL.md:34-45 (uilayers/topmodal/menunav/sheetshape/ariastate/scrollfade paths), .claude/skills/race-incidents-control/SKILL.md:35-37,75-76 (debrisworld/incidentsim/racecontrol), .claude/skills/game-feel/SKILL.md:28-30,67-68 (skidmarks/bodyattitude). Lockstep named docs/ and CLAUDE.md but not the skills guard or these three SKILL.md files.",
     "CSS comment references degrade SILENTLY: css/tokens.css:83, css/menus.css:275,343, css/components.css:237,303,653,783, css/responsive.css:26, css/track-detail.css:18 cite js/game/scrollfade.js, sheetshape.js, topmodal.js by path. docs-integrity's source-comment existence test (:173-206) walks only js/, tools/, tests/ \u2014 CSS is never scanned, so these become permanent stale pointers with no failing test. A second silent-degradation class beyond the bare-basename byBase citations (docs-integrity.test.mjs:147-160, `if (!target) continue`) the cluster already identified.",
     "Unguarded historical docs: spike/ADOPTION-PLAN.md:5-7, spike/physics/README.md:4-5, spike/physics/DEEP-DIVE.md:3-4, spike/PACKAGE-RESEARCH.md:7 and docs/archive/* name the old paths. LIVE_DOCS deliberately excludes archive/research (docs-integrity.test.mjs:33-41) and never walks spike/, so a rename leaves permanently wrong provenance pointers \u2014 or forces churning archived docs, violating the archive policy stated at docs-integrity.test.mjs:28-31.",
     "pick-tests.mjs coverage is wider than the lockstep row: besides :75, :81, :82, line 74 routes uilayers to the `steering` group (`/^js\\/game\\/(input|steer-tuning|uilayers)\\.js/`) and lines 132-135 territory (aerozones/skidmarks/racecontrol/photomode) is covered by :82 only for photomode \u2014 aerozones/skidmarks/racecontrol have NO pick-tests rule today, meaning a rename would also silently leave them unrouted rather than fixable in one row.",
     "Confirmed non-gaps: package.json and playwright.config.js contain zero references to the 14 basenames (grep verified); sw.js derives precache from index.html tags so it follows automatically, as the cluster stated; docs-integrity:318 regex `\"(js\\/game\\/[a-z0-9-]+\\.js)\"` accepts hyphenated names so the CLAUDE.md layout guard (:309-324) fails loudly until CLAUDE.md is edited \u2014 but that edit would delete the very sentence (CLAUDE.md:274 'the older squashed names (`debrisworld.js`) are grandfathered \u2014 do not churn them') that forbids the change."
    ],
    "notes": "Verdict: the NO-GO stands and should be final. The rename is explicitly prohibited by CLAUDE.md:274 (checked into the repo as project policy), delivers zero functional change (globals DebrisWorld/IncidentSim/etc. are untouched, no module boundary moves), and my check found the documented lockstep UNDERCOUNTED the blast radius: it missed a hard-coded test loader (race-control.test.mjs:41), the CARVIEW manifest subset + tools/carview.html tag asserted by load-order.test.mjs:148, the skills-integrity guard over three SKILL.md files, and two silent-degradation surfaces (CSS comment paths and spike/archive provenance docs) that no test would ever flag. That an 11-agent survey plus this feasibility pass still keeps finding new edges is itself the strongest evidence: the reference graph on these 14 files is broader than any one enumeration, and the payoff is purely cosmetic. The cheap alternative in the cluster's risk note is sound and verified compatible: docs-integrity.test.mjs:318 already matches both naming styles, so a small unit guard freezing the grandfathered set (allow-list of the 14 names, hyphenated pattern required for new js/game files) can be added without touching a single existing reference."
   }
  }
 ],
 "refutedCount": 0
}