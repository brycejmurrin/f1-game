{
 "design": "Everything below is grounded in the files read this session: `package.json` test scripts, `docs/TESTING.md` \u00a71\u20132, `tools/pick-tests.mjs` (RULES + `--since`), `.github/workflows/ci.yml` (full header), `tests/unit/test-groups.test.mjs`, `tests/unit/test-coverage-audit.test.mjs`, `playwright.config.js`, and `docs/research/AUDIT-SYNTHESIS-2026-08.md` \u00a7R2.\n\n# Test taxonomy, tests/ split map, and change-aware CI design\n\n## 1. Group taxonomy\n\nGroup **names stay stable** (19 docs cite them; `tests/unit/test-groups.test.mjs:66-74` asserts the docs table \u2194 `package.json` in lockstep, and `RULES` in `tools/pick-tests.mjs` must keep resolving \u2014 its own guard at test-groups:35-42 enforces that). Two new groups are added: `foundation` and `gallery`. Every membership change below is a coordinated 3-file edit: `package.json` + `docs/TESTING.md` \u00a72 table + (where routing changes) `tools/pick-tests.mjs` RULES.\n\n### 1a. Files that move groups (regroup verdicts)\n\n**The 16 circuit-foundation specs leave `test:physics`.** They contain zero driving-model physics; the `physics-` filename prefix exists only to be caught by `test:physics`'s `tests/physics-*.spec.js` glob (package.json:15), so today every driving-model edit pays ~16 circuit builds it cannot break, while `js/circuits/` edits (routed by pick-tests to `circuit`+`scenery`, RULES line 56) never run them \u2014 misgrouped in **both** directions.\n\n- Rename `physics-<circuit>-foundation.spec.js` \u2192 `<circuit>-foundation.spec.js` (abudhabi, albert-park, bahrain, cota, hungaroring, imola, monaco, montreal, monza, qatar, redbull, spa, suzuka, vegas, zandvoort) and `physics-interlagos-migration.spec.js` \u2192 `interlagos-foundation.spec.js`.\n- New group: `\"test:foundation\": \"node tools/run-playwright.mjs tests/*-foundation.spec.js\"` (after the tests/ split: `tests/specs/*-foundation.spec.js`). All 16 land here and nowhere else.\n- `test:physics`'s glob now matches exactly `physics-characterization` + `physics-fixes` \u2014 the two genuine physics files \u2014 so the glob can stay.\n- pick-tests RULES: add `foundation` to the `js/circuits/` rule (line 56 \u2192 `[\"circuit\", \"scenery\", \"foundation\"]`) and to the track-engine rule (line 52-53 \u2192 `[\"circuit\", \"physics\", \"sweeps\", \"foundation\"]`). Remove nothing \u2014 RULES are deliberately over-wide by design (header lines 16-18).\n- `docs/TESTING.md` \u00a72 gains a `foundation` row under \"Track & scenery\"; the \u00a75 family glob row (`physics-*` per test-groups:81-82) is re-pointed at `*-foundation.spec.js`.\n\n**Gallery specs leave `test:ui`.** `ui-audit.spec.js` (63 shots, 0 expects, 13\u2013108 s each \u2014 it dominates test:ui's wall time) moves to a new on-demand `test:gallery` group, absorbing `ui-desktop.spec.js` (see merges). `test:ui` becomes: `ui-button-touch`, `ui-scale`, `hud-layout`, `menu-keyboard`, `menu-survey` (menu-survey stays only because its verdict is strengthen, not regroup \u2014 see 1c#8). `test:gallery` names explicit files, so `test:audit` / `test-coverage-audit.test.mjs` still sees topical coverage and nothing goes orphan. `test:gallery` is **not** added to any pick-tests rule \u2014 galleries are run on purpose, like `tests/manual/`.\n\n**De-duplication of double-billed specs** (each currently executes twice when its two groups co-run, which pick-tests makes routine \u2014 `js/game.js` selects `behaviour`+`api`+`circuit`+`physics` at RULES:72):\n\n| Spec | Today | After |\n|---|---|---|\n| `world-physics.spec.js` | physics + behaviour | **behaviour** only |\n| `active-aero.spec.js` | physics + behaviour | **behaviour** only (pairs with aero-zones, whose trade tests it partially duplicates) |\n| `physics-fixes.spec.js` | physics glob + behaviour | **physics** only (glob keeps it) |\n| `elevation-tracks.spec.js` (heaviest file in the batch, ~24 s \u00d7 40 circuits) | physics + circuit | **circuit** only |\n| `new-hooks.spec.js` | api + hooks | **hooks** only (post-split, see 1b) |\n| `collision-ai-fixes.spec.js` | collision glob + behaviour glob + barriers | collision + barriers only \u2014 `test:behaviour` drops its `collision*.spec.js`/drift/offtrack members entirely |\n\nResulting `test:behaviour` = `world-physics, physics-fixes\u2192(no, physics), active-aero, aero-zones` \u2192 final: `world-physics.spec.js active-aero.spec.js aero-zones.spec.js`. Coverage is unchanged for every pick-tests selection: game.js edits still select behaviour **and** physics **and** circuit, so the union is identical \u2014 each spec just runs once. `test:barriers` keeps `tracks-walls + collision-ai-fixes` unchanged (no pick-tests rule routes to it; it is a manual convenience group).\n\n### 1b. Splits\n\n1. **`agent-view.spec.js`** (1857 lines, ~90 tests, 30+ min monolith) \u2192 4 files, all staying in `test:agent`: `agent-view-contract.spec.js` (world/trackInfo/agentHelp + size budgets), `agent-view-render.spec.js` (rasters), `agent-view-scene.spec.js` (scene/describe/query/carView), `agent-view-survey.spec.js` (survey/rollout/atmosphere).\n2. **`career.spec.js`** (2049 lines, ~80 tests, documented 120 s-budget fights) \u2192 `career-core.spec.js` (save versioning/migration, GP\u2194career isolation, slots), `career-economy.spec.js` (garage/R&D/objectives/rollover/sponsors/reliability/settlement), `career-ui.spec.js` (hub, MY TEAM roster, modes screen, guides). All three in `test:career` and `test:modes`.\n3. **`new-hooks.spec.js`** lines 509-877 (the Silverstone/COTA/Miami/Jeddah/Singapore/Madrid/Shanghai foundation-diagnostics half) \u2192 `track-foundation-diagnostics.spec.js`, into **`test:foundation`**. The remaining hooks-contract half stays as `new-hooks.spec.js` in `test:hooks` only (dropped from `test:api`, closing the api+hooks double-run). Both new files switch their import from `@playwright/test` to `./fixtures.js` while being touched.\n4. **`parts-physics.spec.js`** (1481 lines, ~70% geometry under a \"physics\" name that misleads pick-tests) \u2192 `parts-geometry.spec.js` (Car3D/CarMesh geometry contracts, visual recipes, shader-parity strings); the final ERS-battery test (lines 1415-1481, 3 monza rebuilds) moves into `parts-ers.spec.js`; catalog/getMods/getCost/resolveSetup/statMult stay in `parts-physics.spec.js`. The `parts-*` glob (package.json:20) absorbs the new name automatically; `test:fast`'s explicit `parts-physics.spec.js` reference still resolves.\n5. **`steering.spec.js`** lines 292-405 (keyboard/pedal input-latch block \u2014 `js/game/input.js` territory, no race build needed) \u2192 moved into `touch-steer.spec.js` (its verdicted natural home: same module, same page-boot-only cost, same fixtures import). No group change; both are in `test:steering`.\n\n### 1c. Strengthen list (weak/vacuous asserts, file:line + one-line fix)\n\nFixes 1\u20132 are flagged DEFER in AUDIT-SYNTHESIS (\"per-test design decision, not a mechanical fix\") \u2014 carry them as design tickets, not batch edits:\n\n1. `tests/specs/camera-driving-hooks.spec.js:191-193` \u2014 spin computes before/after headings, asserts neither: assert `after !== before` and sign against the spin argument.\n2. `tests/specs/headless-api.spec.js:290-303` \u2014 \"obs() returns false when player not initialised\" accepts both outcomes: pick one contract and assert it.\n3. `tests/specs/camera-driving-hooks.spec.js:209-217` \u2014 title claims \"zeroes vLat and yawRate\", only vLat asserted: add the yawRate expect.\n4. `tests/specs/camera-driving-hooks.spec.js:12-17` (and camera-hooks:9-16, camera-tuner:11-16) \u2014 fixed 3 s sleeps: replace with `waitForFunction` on `__apex.info().track`; also import `./fixtures.js` instead of `@playwright/test`.\n5. `tests/specs/camera-hooks.spec.js:19-33` \u2014 dolly never checks the eye landed at the requested (frac, lateral, height): assert eye position against `Tracks`-projected expectation within a band.\n6. `tests/specs/collision-ai-fixes.spec.js:131` \u2014 \"speed is scrubbed against Monza barrier\" passes on coasting drag alone: add a no-wall control run and assert scrubbed < control \u2212 margin.\n7. `tests/unit/css-layers.test.mjs:63` \u2014 a file that loses its opening `@layer` wrapper is silently skipped: add a roster test that every `css/` file except `tokens.css` opens with `@layer`.\n8. `tests/specs/menu-survey.spec.js:57-165` \u2014 tests 30/33-36/40-43 have zero expects: one-line DOM assert per captured state (cycleTo landed on TILT/MANUAL, sound/music toggles reflect state), and delete the `.catch(() => {})` at :162 that green-lights a broken campicker.\n9. `tests/unit/fixture-consumer-audit.test.mjs:5-19` \u2014 the FLOOR=31 adoption ratchet is dead code from CI's perspective: add `assert.deepEqual(fixtureImportViolations(readSpecs()), [])` and `adoption(readSpecs()).uses >= FLOOR` so the guarantee actually runs in `test:tooling-fast`.\n10. `tests/specs/map-hooks.spec.js:32-33, 43` \u2014 north-up claimed but only span asserted, and `orbit()` has no assertion after it: pin a known-frac landmark's normalized map position; assert `camState` changed after orbit.\n11. `tests/specs/parts-livery-contrast.spec.js:29-51, 240-254` \u2014 the ink half tests its own inline mirror of buildAtlas: export the atlas's real lum/ink helpers and assert against those.\n12. `tests/specs/props-over-road.spec.js:57-59` \u2014 \"Shanghai uses the default baseline\" asserts a literal in the same file: delete it (the real check runs inline at :144-145).\n13. `tests/specs/scenery-kits.spec.js:82-88` \u2014 \"emits validated street kit facilities\" never asserts the theme: return the resolved theme from the evaluate and assert it; make the vertex bound fail on a missing `vertices` field.\n14. `tests/specs/terrain-over-road.spec.js:246` \u2014 road faces >1.5 m over the line pass everywhere: gate the exemption on `CROSSOVER_TRACKS` (the :203 frac-distance filter already handles the three known tracks).\n15. `tests/specs/tracks-walls.spec.js:37-53, 89` \u2014 test 2 builds all ~40 circuits to assert 5 street circuits: build just those 5; and make `if (r.skip) continue` a failure \u2014 a track whose `race()` failed must not vanish.\n16. `tests/specs/webgl-probes.spec.js:27-34, 79-98` \u2014 typeof-only `hdrMode()`/`lightState()` shape tests: fold into the adjacent behavioural tests (night>day, shadow-transform).\n17. `tests/unit/coplanar-faces.test.mjs:58` \u2014 the `>= 24` roster floor against 40 circuits: assert against the `js/circuits/` file count (the fix `prop-clipping.test.mjs:65-67` already made).\n18. `tests/specs/parts-budget.spec.js:40-41` \u2014 duplicated `toContain(\"600\")`: make the second assert spent-vs-remaining.\n19. `tests/specs/smoke.spec.js:212-219` \u2014 minimap \"content\" is a width>0 check: sample pixels via getImageData (copy telemetry-compare:88-93's pattern).\n20. `tests/unit/race-control.test.mjs:116-126` \u2014 the 90 s cap it names is never exercised: add the cap-expiry test (flag drops after the cap once the picture clears).\n21. `tests/specs/carview-parts.spec.js:5` \u2014 stale \"eight parts categories\" title vs the twelve asserted: fix the title.\n\n### 1d. Merges (merge verdicts + merge candidates)\n\n**Do now (merge BEFORE the tests/ move \u2014 see \u00a72):**\n- `hud-audit.spec.js` \u2192 **into `hud-layout.spec.js`**: its assertHud (:39-45) is a strict subset of hud-layout's real geometry checks; keep the 8 screenshots as an extra shot per existing mode/orientation combo. File deleted; leaves test:ui shorter.\n- `ui-desktop.spec.js` \u2192 **into `ui-audit.spec.js`** as two more viewport rows (strict subset of ui-audit's screens; 0 expects). File deleted; rides along into `test:gallery`.\n- `tests/manual/inspect.spec.js` \u2192 **into `tests/manual/blank-scan.spec.js`**: byte-identical capture loop; one pass asserts blanks AND writes the contact sheet, halving the 40-circuit run. Update `tests/manual/README.md`.\n\n**Merge candidates (flagged, not scheduled):**\n- `active-aero.spec.js:214-261` trade tests vs `aero-zones.spec.js`'s \"downforce traded for top speed\" block \u2014 same trade against the same constants; collapse into aero-zones when either is next touched.\n- `physics-monaco-foundation` test 2 (:140-220) \u2014 hand-copied `props-over-road` scan with an identical 1.4 cap to props-over-road:52; **drop it** in the foundation rename commit (one of the two is dead weight, per verdict).\n- `physics-spa-foundation` test 3 (:52-147) and `physics-zandvoort-foundation`'s ~170-line `propClearance` helper \u2014 third and fourth hand copies of the shared corridor scan: extract to `track-helpers.js` or drop in favour of the all-circuit audit.\n- `albert-park-foundation.test.mjs` + `baku-migration.test.mjs` \u2014 parameterise into one shared circuit-characterization suite when a third circuit joins the pattern (verdict: two files don't yet justify churn).\n- `parts-ers.spec.js` absorbing parts-physics's ERS test is already scheduled under splits (1b#4); the four ERS sweeps in parts-ers should share one page load while touched.\n\n## 2. tests/ split map\n\nBase plan is AUDIT-SYNTHESIS \u00a7R2 verbatim \u2014 `tests/specs/` + `tests/unit/` + `tests/helpers/`; `tests/data/`, `tests/manual/` stay; **one commit via `git mv`, never with a background run in flight, no ?v bump** \u2014 including all 13 R2 lockstep items (package.json paths; playwright.config globalSetup/reporter; the 66 `./fixtures.js` imports; the \u26a0 output-paths/qr-camera symmetric-drift pair; the \u26a0 `tools/fit-audit.mjs:27`/`menu-fit.mjs:32` swallowed dynamic imports; \u26a0 `f1-track-accuracy.spec.js:18` `./data/`; \u26a0 manual/ imports + README; the test-groups:154 + docs-integrity:343 regex widenings and flat-readdir walks; test-coverage-audit tool+test scans and fixture-consumer-audit readSpecs; \u26a0 ci.yml sweeps filter; span-kinds:46/docs-integrity:217 hardcoded reads; the TESTING.md/CLAUDE.md/README count-phrase triple). Nothing here contradicts R2; the ordering rule and per-file additions below **extend** it.\n\n**Ordering (audit feed-in): merge \u2192 regroup-rename \u2192 move \u2192 split.** The three merges in 1d delete `hud-audit.spec.js`, `ui-desktop.spec.js`, and `manual/inspect.spec.js` *before* the move (a merged file must never be moved and then deleted \u2014 it would churn the R2 lockstep edits twice), so the move relocates **109** specs, not 111. The 16 foundation renames also land before the move for the same reason (one path rewrite per file, not two). The splits (1b) land *after* the move, inside `tests/specs/`, so their new filenames never exist at the old paths.\n\nPer-file mapping:\n\n| Destination | Files |\n|---|---|\n| `tests/specs/` | All `*.spec.js` (109 post-merge): the 16 renamed `*-foundation.spec.js`, and every other root spec named in the package.json groups \u2014 smoke, dev-tools, logging, headless-api, obs-act-edge, new-hooks, data-lifecycle, telemetry-compare, assets-api, persistence, wake-lock, camera{,-hooks,-driving-hooks,-tuner}, map-hooks, world-physics, longitudinal, elevation-tracks, projection, active-aero, understeer-cue, physics-characterization, physics-fixes, collision{s,-deep,-ai-fixes}, drift, offtrack, debris, race-control, aero-zones, tracks-walls, autopilot, audit, parts-* (all), car-effects, carview-parts, custom-team, garage-aero, presets, sliders, steering, gamepad, touch-steer, tilt-pipeline, steer-migration, ui-audit, ui-button-touch, ui-scale, hud-layout, menu-survey, menu-keyboard, menu-baseline, tracks-visual, props-over-road, terrain-over-road, f1-track-accuracy, scenery-kits, instanced-draw, webgl-probes, lighting-ab, image-grade-visual, lighting-tuner-grade, material-shimmer, tlx-probes, audio-smoke, music-library, season, time-trial, career, quali, agent-view, agent-drive-bench, agent-determinism, multiplayer-* (8), output-paths.spec.js |\n| `tests/unit/` | All 49 `*.test.mjs` + `shared-track-foundation-characterization.test.cjs`: the 34 tooling-fast suites, 7 net-unit suites, 4 sweeps suites, service-worker, webgpu-lifecycle, agentview-api-contract |\n| `tests/helpers/` | The 7: `fixtures.js`, `track-helpers.js`, `qr-camera.js`, `f1-api-mock.js`, `output-paths.js`, `global-setup.js`, `live-reporter.js` |\n| `tests/data/` | `f1-circuit-reference.geojson` (stays) |\n| `tests/manual/` | stays: `README.md`, `blank-scan.spec.js` (post-merge), `circuits.js`, `galleries/` |\n\n**Extensions the per-file audit adds to R2's lockstep list:**\n\n14. **Snapshot dir moves with its spec**: `tests/specs/menu-baseline.spec.js-snapshots/` \u2192 `tests/specs/menu-baseline.spec.js-snapshots/` in the same `git mv` commit \u2014 Playwright resolves snapshot paths relative to the spec file, and these are the repo's ONLY golden baselines (CLAUDE.md); a missed move reads as \"baseline missing\", which `toHaveScreenshot` under `--update-snapshots` would silently re-bless. Same rule pre-arms `tests/specs/tracks-visual.spec.js-snapshots/` \u2014 the skip gate at tracks-visual:27-30 keys on that (spec-relative) dir existing, so the gate keeps skipping correctly after the move with no edit.\n15. **`tests/physics-baseline.json`** sits flat in tests/ (verified on disk) and is in no R2 bucket: move to `tests/data/physics-baseline.json` and update `physics-characterization.spec.js`'s read plus whatever tool regenerates it (grep `physics-baseline` across tools/ in the same commit \u2014 the characterization gate being unable to find its committed baseline must fail loudly, and the spec's header says the baseline's presence is what makes the gate live).\n16. **`tests/manual/` internals**: `manual/circuits.js` and `manual/galleries/` stay put, but R2 item 7's manual/ fix-ups shrink by one file (inspect.spec.js is deleted by the 1d merge before the move).\n17. **Group-taxonomy lockstep**: the \u00a71 package.json membership edits and the R2 path rewrites touch the same ~28 script lines \u2014 land \u00a71's regroup/dedupe **before** the R2 move commit so the move is a pure path rewrite, and `test-groups.test.mjs` / `test-coverage-audit.test.mjs` adjudicate each step separately.\n18. **New-group guards**: `test:foundation` and `test:gallery` each need a `docs/TESTING.md` \u00a72 row (test-groups:66-74 fails otherwise) and their member files satisfy `test:audit` by being explicitly named/globbed.\n\n## 3. Change-aware CI\n\nDesign constraints taken from `ci.yml` as read: guards ~25 s / sweeps 15m12s / smoke 14m24s are **measured** budgets (run 31023778264) and `timeout-minutes` must stay above them; a timed-out job reports `cancelled`, not `failed`, and that once hid a dead deploy behind \"0 failures\"; SwiftShader on a shared runner measured **90\u2013115 s/test** (smoke-step comment), serial one-worker with `--timeout=240000` is the proven-stable shape; `concurrency` cancels **only** `pull_request` runs; pages.yml gates deploy via `workflow_call` + `needs: ci`, so jobs must always run and report (step-level conditions only, never job-level `if` \u2014 the sweeps filter comment states this explicitly).\n\n### The `selected` job (additive, non-deploy branches)\n\n```yaml\n  selected:\n    name: Change-selected groups (advisory on feature branches)\n    # Never part of the deploy gate: pages.yml needs guards/sweeps/smoke, not this.\n    # Skipped entirely when ci.yml runs via workflow_call from pages.yml.\n    if: github.event_name != 'workflow_call'\n    runs-on: ubuntu-latest\n    # BUDGET (state and re-measure per the header's rule): setup ~4 min\n    # (npm ci + playwright install chromium), node-only groups ~2 min,\n    # browser step capped at 25 min below => 31 min worst case. 35 > 31.\n    timeout-minutes: 35\n    steps:\n      - uses: actions/checkout@v4\n        with: { fetch-depth: 0 }   # --since needs the base commit reachable\n      - uses: actions/setup-node@v4\n        with: { node-version: \"20\", cache: npm }\n      - run: npm ci\n      - name: Resolve diff base + fallback check\n        id: base\n        ...\n      - name: Node-only selected groups\n        timeout-minutes: 5        # measured: tooling-fast ~30 s, net-unit <1 s\n        ...\n      - name: Browser selected groups (fixed budget)\n        if: steps.base.outputs.fallback != 'true'\n        timeout-minutes: 25       # 12 tests x 115 s = 23 min; cap above it\n        env: { APEX_WORKERS: \"1\" }\n        ...\n```\n\n**Diff refs (exact, per event):**\n- `push`: base = `${{ github.event.before }}`, head = `${{ github.sha }}`. Copy the sweeps filter's fail-safe ladder verbatim (ci.yml:196-217): empty or all-zero `before` (new branch/first push), `git cat-file -e \"$BEFORE^{commit}\"` failing (force push), or `git diff` erroring each set `fallback=true`. **Fail safe, never fail open** \u2014 an unresolvable diff must not be read as \"nothing changed\".\n- `pull_request`: base = `${{ github.event.pull_request.base.sha }}`; compute `MB=$(git merge-base \"$BASE\" HEAD)` (the checkout is the merge commit, so merge-base keeps unrelated base-branch churn out of the diff) and use `MB` as the ref. If merge-base fails \u2192 `fallback=true`.\n\n**Selection**: run `node tools/pick-tests.mjs --since \"$REF\"`. `--since` does exactly `git diff --name-only <ref>` against the checked-out tree (pick-tests:120-121), which in a clean CI checkout is base\u2192head. Parse the `test:<group>` lines from the \"group(s) to run\" block. Partition:\n- **Node-only groups, run directly, uncapped** (their package.json commands are `node --test` or pure tools): `tooling-fast`, `net-unit`, `service-worker`, `webgpu-lifecycle`, `agent-contract`, `audit`. **Exclude `sweeps`/`tooling`** \u2014 the sweeps job already owns that work with its `NODE_OPTIONS: --max-old-space-size=6144` heap ceiling (job-level, to reach prop-clipping's execFileSync child); re-running it here without that env would reproduce the documented 4 GB SIGABRT-as-`cancelled` failure. Guards-job overlap (tooling-fast/audit run twice) costs ~30 s and is accepted for locality of failure.\n- **Browser groups, fixed budget**: install Chromium only (`npx playwright install --with-deps chromium` \u2014 the smoke job's rationale). Budget rule, derived from the measured 90\u2013115 s/test and the 35-min job cap: **at most 2 selected browser groups AND at most 12 total tests** (12 \u00d7 115 s \u2248 23 min, inside the 25-min step cap with margin; margin-that-thin-is-a-coin-flip is the header's own lesson). Count with `npx playwright test --list <group's spec paths> | tail -1` before running. Run serial: `APEX_WORKERS: 1` (playwright.config.js's documented override; CI default `workers: 2` is the proven oversubscription signature) with `-- --timeout=240000`. Take groups in pick-tests' sorted output order until the budget is hit.\n- **Overflow**: if selection exceeds 2 groups / 12 tests, run what fits, and write the remainder to `$GITHUB_STEP_SUMMARY` as `run locally: node tools/test-bg.mjs <groups>` \u2014 do **not** fail. This job is advisory; the three-job gate below stays the authority.\n\n### Fallback-to-full-gate rule\n\nIf the diff (or an unresolvable diff, per the fail-safe ladder) touches any **infra path**, the selected job sets `fallback=true`: the browser step is skipped with a printed reason and the node-only step runs only `tooling-fast` + `audit`. Coverage then rests entirely on the unconditional three-job gate, which is already running on the same push. Infra paths:\n\n```\npackage.json  package-lock.json  tools/manifest.cjs  tools/pick-tests.mjs\ntools/run-playwright.mjs  tools/test-bg.mjs  .github/**  playwright.config.js\ntests/helpers/**          (pre-R2-move: tests/{fixtures,track-helpers,qr-camera,\n                           f1-api-mock,output-paths,global-setup,live-reporter}.js)\nindex.html  sw.js  version.json      (index.html's script block IS the load\n                                      order; a ?v/version.json bump invalidates\n                                      every cached page \u2014 gate at file granularity)\n```\n\nRationale: these files change what selection *means* (pick-tests RULES, group scripts, project partition, fixtures every spec loads, the shell the server serves), so a selector reading them to route its own validity would be circular. This list also matches R2 item 10's obligation to update ci.yml's sweeps filter regex to `tests/unit/(...)` when the move lands \u2014 do the selected-job path list and the sweeps filter in that same commit, or the sweeps filter fails open.\n\n### Coexistence with guards / sweeps / smoke\n\nUnchanged, byte-for-byte, as the deploy gate: `pages.yml` keeps `needs: ci`, ci.yml keeps `workflow_call`, and guards/sweeps/smoke keep their measured budgets (10/35/30 `timeout-minutes`) and step-level conditionality. `selected` is **additive on non-deploy branches only** \u2014 it is excluded from `workflow_call` runs so a slow advisory job can never delay or cancel a deploy, and it is deliberately *not* listed in any `needs:`. The sweeps job's geometry filter and the selected job share the pick-tests-derived worldview but stay independent implementations, per the sweeps comment's fail-safe doctrine.\n\n### Failure-mode notes (the header's demands, restated as obligations on this design)\n\n- **Every step states its `timeout-minutes` next to a measured figure**, and the header rule applies: change what a step runs \u2192 re-time it \u2192 change the number. The 25-min browser cap sits above the computed 23-min worst case for the same reason smoke's 30 sits above 14m24s: thin margins produced `cancelled` verdicts nobody read as failures.\n- **No cancel-in-progress on push** \u2014 the existing `concurrency` block (`cancel-in-progress: ${{ github.event_name == 'pull_request' }}`) already encodes the fifteen-consecutive-cancelled-runs lesson; `selected` adds no concurrency group of its own and inherits it.\n- **`cancelled` \u2260 `failed`**: any step in `selected` that can exceed its cap must overflow-to-summary rather than run long; a budget overflow is a *reported decision*, never a timeout.\n- **Step-level `if`, job always reports** \u2014 same pattern as the sweeps filter, so a future `needs:` consumer of `selected` (if it ever becomes gating) inherits working semantics.\n- **Heap ceiling stays with the sweeps job**: `selected` must never run `test:sweeps`/`test:tooling`; the OOM story (road-under-floor SIGABRT at 4088 MB on run 31016070670, surfacing as `cancelled`) belongs to the job that carries `NODE_OPTIONS=--max-old-space-size=6144`.\n- **Worker discipline**: browser steps pin `APEX_WORKERS=1` + `--timeout=240000` \u2014 the smoke step's own controlled experiment (125.2 s timeout contended \u2192 110.6 s pass alone) is the evidence; two SwiftShader contexts on a shared runner measure the runner, not the code.",
 "feasibilityGaps": [
  "BROKEN workflow_call GUARD (blocking): `if: github.event_name != 'workflow_call'` never fires \u2014 in a reusable workflow, github.event_name is inherited from the CALLER's triggering event ('push' when pages.yml runs on the deploy branch, 'workflow_dispatch' on manual deploys); 'workflow_call' is a trigger name, not a runtime event_name value. The condition is always true, so `selected` runs inside every pages.yml-called CI run. Workable discriminators exist (github.ref == 'refs/heads/claude/f1-game-project-26h3ng', or a workflow_call input with a default) but the design names neither.",
  "FALSE 'not in any needs:' SAFETY CLAIM (blocking, compounds gap 1): pages.yml's `ci` job (pages.yml:30-31, `uses: ./.github/workflows/ci.yml`) succeeds only if EVERY job in the called workflow succeeds \u2014 a reusable-workflow caller aggregates all jobs, so 'deliberately not listed in any needs:' protects nothing. With gap 1, a failing selected job blocks deploy, and a selected job that hits its 35-min cap reports `cancelled` \u2014 reproducing byte-for-byte the invisible-deploy-outage mode ci.yml's header documents (lines 42-47). (A job-level `if:` skip WOULD be safe \u2014 skipped counts as success \u2014 but the design's skip condition doesn't work, per gap 1.)",
  "UNCOVERED EVENTS: the diff-ref table specifies push and pull_request only. Via pages.yml the workflow also runs under workflow_dispatch (pages.yml:6) and (per gap 1) push-to-deploy-branch; the sweeps ladder the design claims to copy 'verbatim' handles non-push events by RUNNING EVERYTHING (ci.yml:204 run_all), an action that has no analogue in selected \u2014 behavior for these events is simply unspecified.",
  "FAIL-SAFE DOCTRINE INVERTED, AND MISLABELED: sweeps' fail-safe on an unresolvable diff is to RUN the expensive work (ci.yml:191-195); selected's fallback SKIPS the browser step and runs only tooling-fast+audit \u2014 i.e. it runs LESS when it knows less. Acceptable for an advisory job, but the design quotes the 'fail safe, never fail open' doctrine while doing the opposite, and the claim 'coverage then rests entirely on the unconditional three-job gate' overstates: guards/sweeps/smoke never covered career/parts/steering/ui/etc. groups in the first place, so on fallback those selections are covered by nothing.",
  "FIRST-PUSH FALLBACK IS THE COMMON CASE: github.event.before is all-zeros on the first push of a branch (and the old, usually-unreachable sha on force-push \u2014 the ladder's cat-file rung catches that). In this repo every task lands on a fresh claude/<topic> branch, so the branch-creation push \u2014 often the push carrying the whole change \u2014 always takes fallback and the selected job degrades to duplicating guards. A better base exists and goes unused: pick-tests' own default mode (lines 124-128) merge-bases against origin/main (which exists on this remote), and the true default branch is the deploy branch claude/f1-game-project-26h3ng, reachable under fetch-depth: 0. The design never attempts either.",
  "PICK-TESTS OUTPUT PARSING TRAPS: (a) the zero-match message 'no rule matched \u2014 run `npm run test:fast`' (pick-tests:146) contains the literal token test:fast \u2014 an unanchored grep for test:<group> selects test:fast for e.g. a spike/- or .claude/-only diff (neither is on the infra list, so fallback does not shield this); (b) 'no changed files \u2014 nothing to run' (line 137) and the batching lines (`node tools/test-bg.mjs api behaviour`, line 166) must also be excluded; (c) there is no --json/porcelain flag \u2014 lines 152-153 are a human-oriented, unversioned format (`test:${g.padEnd(14)} reason`) that no test asserts, so CI becomes a silent consumer of prose. The design says 'parse the test:<group> lines from the block' without pinning any of this.",
  "BUDGET vs REAL GROUP SIZES MAKES THE BROWSER STEP VACUOUS: measured in artifacts/logs, test:api = 193 tests and test:agent = 126; test:tiny = 71 (ci.yml:249); career ~80 and agent-view ~90 by the design's own counts (pre-split). Nearly every browser group pick-tests emits exceeds the 12-test cap ALONE, and RULES route `tiny` (71 tests) on every js/css edit. For the flagship case \u2014 game.js \u2192 api, behaviour, circuit, physics, tiny \u2014 the alphabetically-sorted order (pick-tests:150) front-loads api's 193 tests, so under 'take groups until the budget is hit' the step runs nothing and dumps everything to the summary. The rule is also ambiguous: stop at first overflow, or skip-and-continue to a smaller group? Either way the design never states a minimum useful selection or any per-spec granularity, so the job's headline capability barely ever executes.",
  "RETRY/TIMEOUT INFLATION BREAKS THE 23-MINUTE ARITHMETIC: playwright.config.js:88 pins retries:1 in CI, and the design sets --timeout=240000. A timing-out test therefore costs up to 240 s x 2 = 8 min, not the 115 s the budget uses; two-three such tests exceed the 25-min step cap with zero passing tests around them. The cap fits only the all-pass case, so precisely when the selection finds a real regression (or the runner degrades), the step dies on timeout-minutes and the job reports `cancelled` \u2014 violating the design's own obligation that 'a budget overflow is a reported decision, never a timeout'. Smoke survives the same exposure only because 30 min vs 9 specs leaves ~3 timeout-retries of headroom; 25 min vs 12 tests leaves ~1.",
  "TEST-COUNT STEP AS WRITTEN DOESN'T RUN: 'npx playwright test --list <group's spec paths> | tail -1' needs the group's spec paths, which exist only inside package.json script strings (including globs like tests/physics-*.spec.js) \u2014 the design names no extraction mechanism. `npm run test:<group> -- --list` would inherit them through tools/run-playwright.mjs (which appends argv verbatim to `playwright test`), at the cost of pointlessly starting its static server; raw npx with hand-extracted paths is a second, unasserted package.json parser. Also `tail -1` on 'Total: N tests in M files' is plausible but unverified against this reporter config (three custom reporters, playwright.config.js:134-140), and the double-project concern is safe only because RENDER_SPECS/testIgnore are exactly complementary \u2014 an invariant the counting step now silently depends on.",
  "INFRA GATING x BUMP-CACHE CONVENTION: index.html + version.json are on the infra list at file granularity, and the repo's hard rule (CLAUDE.md) is that every shippable change's LAST commit bumps ?v=N in index.html AND version.json. So the final push of essentially every change sets fallback=true \u2014 the browser step never runs on exactly the commits that get reviewed and merged; advisory coverage exists only on intermediate pushes.",
  "THE 1a DEDUPE BREAKS ITS OWN 'UNION IDENTICAL' CLAIM (selection interaction): dropping collision*/drift/offtrack from test:behaviour removes those specs from every game.js / physics-consts.js selection \u2014 RULES route `collision` only from debrisworld/incidentsim (pick-tests:82), so after the change no selected group covers collisions, collision-deep, collision-ai-fixes, drift, or offtrack for driving-model edits (test:barriers has no rule at all, as the design itself notes). Likewise new-hooks leaving test:api removes it from js/render/assets.js (RULES:48) and js/data/ (RULES:89) selections, which route api-only. The CI selected job inherits these holes on its highest-value trigger.",
  "FALLBACK PATH IS PURE DUPLICATION: on fallback the node-only step runs tooling-fast + audit \u2014 exactly what the guards job already runs unconditionally in the same workflow run (ci.yml:114-117), plus a second ~2-4 min npm ci to do it. Zero added coverage; the design frames it as a coverage story rather than the no-op it is. Minor, but it means the job's most common outcome (see gaps 5 and 10) is spend with no signal.",
  "UNADDRESSED GROUPS IN THE BROWSER PARTITION: RULES emit `visual` for js/render/glx|gfx edits and `ab` for shaders/lighting \u2014 test:visual (tracks-visual) is deliberately skip-gated and would burn one of the 2 group slots plus --list-counted tests that will all skip at runtime (the gate is a runtime dir check, invisible to --list); lighting-ab.spec.js is double-billed inside test:webgl AND test:ab, so a shader edit (RULES:45 \u2192 webgl+ab) re-runs it \u2014 the design de-dupes six other double-billings but not this one, and its browser partition never classifies visual/ab/tiny/baseline at all.",
  "MINOR ARITHMETIC/CLAIM NITS: (a) 'Node-only selected groups, run directly, uncapped' contradicts the same step's timeout-minutes: 5 two lines up; service-worker/webgpu-lifecycle/agent-contract/audit are unmeasured there (guards' 25 s total suggests it's fine, but the header rule the design invokes demands a stated measurement). (b) The 35 = 4+2+25 budget omits the diff-resolve, pick-tests, and per-group --list invocations (seconds each \u2014 fine, but the design's own doctrine is to state them). (c) On a branch with an open PR, push and pull_request runs both fire with different concurrency groups (ci-refs/heads/... vs ci-refs/pull/...), so selected's browser spend doubles per push \u2014 pre-existing for other jobs but selected adds up to 2x25 min of it. (d) A backward force-push whose old sha is still reachable passes the cat-file rung and yields a reversed diff (the reverted files) \u2014 conservative in direction, but worth knowing the ladder does not catch it."
 ],
 "perFileCount": 162,
 "perFile": [
  {
   "file": "tests/specs/active-aero.spec.js",
   "purpose": "Failing means the 2026 active-aero mechanic broke: state surface, zone-gated arming, flap travel asymmetry, brake cutoff, the speed/grip trade, or AI usage of X-mode.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/aero-zones.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Exemplary relative assertions with anti-vacuity guards (asserts flap position before comparing runs, on-track checks); note it runs in BOTH test:physics and test:behaviour so it executes twice when both groups run.",
   "weakSpots": "None vacuous; the trade tests (L214-261) partially duplicate aero-zones' 'downforce traded for top speed' block, which measures the same trade against physState constants."
  },
  {
   "file": "tests/specs/aero-zones.spec.js",
   "purpose": "Failing means the FIA activation-zone model broke: zone minimums, Monaco's zero-zone case, pace-slider invariance, inZone/zoneAhead wrap handling, overtake's opening-lap gate, or the per-wing trade scaling.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/active-aero.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Every assertion is a real invariant (flicker-proof whole-trail overtake gate at L150, envelope derived from constants not literals); the 133s test.slow wing-sweep (L224-278) is expensive but self-justifying with anti-vacuity ordering checks.",
   "weakSpots": "The 3-reload wing-sweep test (L224-278) is the single most expensive test in the file and is the main reason the file needs its own budget; nothing vacuous."
  },
  {
   "file": "tests/specs/agent-determinism.spec.js",
   "purpose": "Failing means seeded replay broke \u2014 some code path draws Math.random() into the sim or reset() leaks episode state, invalidating every A/B comparison and the drive bench.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Byte-identical digest comparison with both anti-vacuity halves (car actually drove >50m at L67; different seed differs at L78); exactly the guard the repo's seeded specs depend on.",
   "weakSpots": "None; the eval(episodeScript) pattern is unusual but sound."
  },
  {
   "file": "tests/specs/agent-drive-bench.spec.js",
   "purpose": "Failing means the agent world-view fields stopped carrying actionable driving signal (a semantic regression shape tests cannot see), or a prescribed-racing-line field leaked back into the payload.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/agent-view.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "The task-success bench (relational > 1.5x naive, >300m) is the one test that catches meaning-level regressions; recent logs show it actually failing on a live branch, i.e. it bites.",
   "weakSpots": "L99 margin (1.5x, 300m) is a judgment call vulnerable to physics retunes \u2014 the comment acknowledges it; the honest-geometry block (L108-158) partially duplicates corner-table checks in agent-view.spec.js."
  },
  {
   "file": "tests/specs/agent-view.spec.js",
   "purpose": "Failing means the agent-facing JSON contract broke somewhere across world()/trackInfo()/scene()/describe()/query()/render()/carView()/survey()/rollout()/atmosphere()/agentHelp() \u2014 payload shapes, typed errors, geometric invariants, or size budgets.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/unit/agentview-api-contract.test.mjs",
    "tests/specs/agent-drive-bench.spec.js"
   ],
   "verdict": "split",
   "verdictDetail": "1857 lines and ~90 tests spanning at least five separable surfaces (world/trackInfo contract, render rasters, scene/describe/query drill-down, carView, survey/rollout) \u2014 splitting into 3-4 files would let pick-tests and test-bg schedule a fraction of it per edit instead of the whole 30+ minute monolith.",
   "weakSpots": "Assertions are uniformly strong; the weakness is granularity, plus a few tripwire budgets (agentHelp <6000 bytes L1554, drive payload <4500 L305) that fire on wording changes rather than regressions \u2014 deliberately, per their comments."
  },
  {
   "file": "tests/unit/agentview-api-contract.test.mjs",
   "purpose": "Failing means the agentview/agentview-raster module split silently dropped or resurrected a surface method \u2014 the class of bug that otherwise only throws the first time an agent calls the untested name.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [
    "tests/specs/agent-view.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Cheap VM-loaded freeze of CURRENT/REMOVED/CONSTS plus a re-coupling regex guard; complements (does not duplicate) the behavioural spec, and pins the removed aliases from both sides.",
   "weakSpots": "None; the source-regex check at L102-108 is mildly brittle to refactors but its failure message explains itself."
  },
  {
   "file": "tests/unit/albert-park-foundation.test.mjs",
   "purpose": "Failing means someone edited js/circuits/albert_park.js away from its authored foundation contract: racing-space coordinates, essentially-flat terrain, full-lap dressing exclusions, or the typed water/ground helpers.",
   "assertionQuality": "mixed",
   "costTier": "trivial",
   "overlaps": [
    "tests/unit/baku-migration.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "A legitimate per-circuit characterization pin in test:tooling-fast; same pattern as baku-migration, so if more circuits gain foundation tests a shared parameterised suite would be tidier, but two files does not yet justify the churn.",
   "weakSpots": "L43-46 asserts source-text regexes (waterSurface(/groundPatch(/no groundPlane() \u2014 brittle to renames and proves call presence, not behaviour; thresholds (terrainOuter>=90, rise<=0.75) are authored choices, not derived invariants."
  },
  {
   "file": "tests/specs/assets-api.spec.js",
   "purpose": "Failing means the baked asset pack's runtime path broke: load/upload state, matTex clamping, the ON-by-default contract, procedural fallback on unload, GL-error-free rendering at full mix, credits traceability, or the diag()/stats() debug surface.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/unit/assets-pack.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "Runtime complement to the static pack test; the 'still renders' check honestly asserts the GL error channel (L127-128) instead of pretending pixels prove shader health.",
   "weakSpots": "L109-129 cannot detect an all-black-but-error-free frame (acknowledged in its own comment); layer-count bounds 10..16 (L42-43) are loose by design."
  },
  {
   "file": "tests/unit/assets-pack.test.mjs",
   "purpose": "Failing means the committed pack or bake tooling rotted: MAT table drift from TrackGeom, a non-CC0 licence, budget overrun, malformed filmstrip PNGs, broken bake-model/toGLB/zip round-trips, or bad addMesh placement math.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "overlaps": [
    "tests/specs/assets-api.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Outstanding static guard: verifies against the real shipping modules in VM, the game's own GLB loader, and the system unzip rather than transcribed copies.",
   "weakSpots": "L413 silently passes when unzip is absent on the box (env-conditional vacuity for that one test); L422 registers a process-exit cleanup handler per run (harmless leak)."
  },
  {
   "file": "tests/specs/audio-smoke.spec.js",
   "purpose": "Failing means GameAudio errors at boot, the sound-toggle stopped re-issuing race music with the correct track index, or real WebAudio unlock/engine synthesis broke after a user gesture.",
   "assertionQuality": "mixed",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Tests 2 and 3 are genuinely strong (stubbed startMusic asserting exact call args; centroidHz>50 proves audible synthesis); the boot smoke is weak by design and fine as the file's namesake.",
   "weakSpots": "L4-24 asserts only 'global defined + no filtered console errors' \u2014 a silent audio failure that logs nothing passes; the filter list (L17-21) could miss differently-worded errors."
  },
  {
   "file": "tests/specs/audit.spec.js",
   "purpose": "Failing means an audited edge-case regressed: grid-settle NaN/launch, wrong-way false positives, lap-counter symmetry over the line, rear-end speed injection, barrier clipping at Suzuka, tan/grip finiteness under abuse, or the opt-in road-follow assist.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Each test pins a documented historical bug with invariant-style assertions (lap given back and re-timed, L74-151, is a model regression test); worth its circuit-group cost.",
   "weakSpots": "Magnitude thresholds are tuned rather than derived (maxAbsX<20 L29, maxOver<0.5 L181, hw*1.6 L228, 0.7 ratio L255) \u2014 justified in comments but would need retuning after physics changes."
  },
  {
   "file": "tests/specs/autopilot.spec.js",
   "purpose": "Failing means the car can no longer be driven hard by a closed-loop controller without NaN-ing, clipping a barrier, or stalling \u2014 the end-to-end drivability/safety floor for steering and physics changes.",
   "assertionQuality": "mixed",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/agent-drive-bench.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "The asserted floor (finite, maxWall<1, distPct>40/30) is deliberately loose because completion is unachievable by the naive controller; it still catches the failures it exists for, and the rich metrics it computes are the tuning instrument even though only logged.",
   "weakSpots": "L196/L216 progress floors (40%/30% in 150s) are arbitrary and PACE-coupled (mitigated by pinning pace=1 at L36); most computed metrics (lapTime, avgSpeed, jitter, offFrames) are console.log'd (L192, L211-212), never asserted."
  },
  {
   "file": "tests/unit/backend-surface-parity.test.mjs",
   "purpose": "Failing means WGX or TLX stopped declaring a name GLX publishes \u2014 the descriptor-copy install would then leave a live-but-broken inherited GLX function that defeats every feature test (the bug that bit twice).",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Source-scanning is fragile in principle but the >=40-key floor (L82-91) is exactly the anti-vacuity tripwire that makes it trustworthy; superb failure message.",
   "weakSpots": "Scanner depends on indentation conventions of the three files (L61-63); the floor test would let the scanner miss a minority of keys undetected, though the parity diff then errs toward false positives, not silence."
  },
  {
   "file": "tests/unit/baku-migration.test.mjs",
   "purpose": "Failing means Baku's authored contract broke: racing coordinates, the two curated dressing exclusions, the castle-crest elevation profile, required landmarks, near-full street barrier tightness, or the seafront staying flat within the undulation envelope.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "overlaps": [
    "tests/unit/albert-park-foundation.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "Builds the real track twice (day/night) in a VM and asserts derived invariants \u2014 trend-vs-envelope (L124-139) is a model of how to assert 'flat' in an undulating world; keep as-is.",
   "weakSpots": "L49-66 deepEquals the exact exclusion zones and elevation tuple, so any legitimate re-curation of Baku must edit the test in lockstep (characterization by intent, per the eb0b6b3 comment)."
  },
  {
   "file": "tests/specs/camera-driving-hooks.spec.js",
   "purpose": "Failing means the v196 __apex hooks regressed: orbit/cinematic/carOrbit fov plumbing and clamps, cinematic outside-of-corner placement, or the setSpeed/spin/nudge state mutators and their clamps/guards.",
   "assertionQuality": "mixed",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/camera-hooks.spec.js"
   ],
   "verdict": "strengthen",
   "verdictDetail": "Real substance exists (cinematic az-sign vs curvature L75-105, clamps, no-player guards) but several asserts are dead or under-claim their titles, and 24 tests each paying a full page load + fixed 3s sleep (~20-42s apiece, ~11 min total measured) is a lot of box time for hook plumbing \u2014 consolidate loads and fix the asserts.",
   "weakSpots": "L191-193: before/after headings computed and never asserted (the known dead computation); L209-217: title claims 'zeroes vLat and yawRate' but only vLat is asserted (L216); L22-44 and L120-132 fov tests mostly echo the input back; imports from @playwright/test not ./fixtures.js so failures arrive without apex-state attachments; L12-17 fixed 3s setTimeout instead of waitForFunction."
  },
  {
   "file": "tests/specs/camera-hooks.spec.js",
   "purpose": "Failing means the composition hooks broke: dolly/roadside placement, tourShots coverage and corner framing, previewCam's non-invasive mode solver, view()'s whole-track aerial, or orbit's underground clamp.",
   "assertionQuality": "mixed",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/camera-driving-hooks.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Most tests assert real semantics (tourShots lap-ordering and az magnitude L77-84, orbit-above-roadY L127-136, previewCam not moving the car L88-108); one weak test and the fixed-sleep load pattern keep it from 'strong'.",
   "weakSpots": "L19-33 dolly test never verifies the eye landed at the requested (frac, lateral, height) \u2014 only eye\u2260target and the debug flag, so a dolly that ignores its arguments entirely still passes; L9-16 fixed 3s sleep per test; console.log noise stands in for some verification."
  },
  {
   "file": "tests/specs/camera-tuner.spec.js",
   "purpose": "Failing means the CAMERA TUNER broke its core invariant \u2014 a knob moves only its own camera by exactly the tuned amount, untuned modes keep the shipped framing \u2014 or the clamp/persist/reset flow or pause-menu panel regressed.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Exact-delta assertions (eye +2m, dist +3m, fov -5 at L89-95), cross-mode isolation (L119-136), and a full panel walkthrough \u2014 the isolation invariant is precisely what would rot silently without this.",
   "weakSpots": "L11-16 fixed 3s sleep in the loader; otherwise none of note."
  },
  {
   "file": "tests/specs/camera.spec.js",
   "purpose": "Failing means the 13 player camera modes broke: the mode list/cycling/wrap, persistence, a mode collapsing onto another's vantage or failing to draw, the grid world-pose seed that stops the chase rig jumping at lights-out, or the C-key cycle.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "The vantage-distinctness redesign (L71-98) is a well-reasoned honest trade \u2014 13 distinct camState vantages plus one real pixel capture \u2014 documented with measured SwiftShader costs; keep exactly as written.",
   "weakSpots": "The single screenshot proves 'draws something >1KB' (L98), not that each mode draws correctly \u2014 an accepted, documented limitation."
  },
  {
   "file": "tests/specs/car-effects.spec.js",
   "purpose": "Failing means visual effect state decoupled from physics: ERS deploying an exhaust flame, brake glow ignoring the heat threshold, the ERS lamp tracking the switch instead of actual deployment, or exhaust flames burning continuously on steady throttle.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Drives real physics to each threshold (steps until brakeHeat crosses brakeGlowThreshold, flat-battery boost hold) rather than toggling renderer flags \u2014 precisely the grounded contract its header claims.",
   "weakSpots": "L20-23 tolerates carEffects being absent by returning null, but every test then fails on the null, so no silent pass; none otherwise."
  },
  {
   "file": "tests/specs/career.spec.js",
   "purpose": "Failing means CAREER broke somewhere across its whole surface: save versioning/migration, GP\u2194career isolation, hub/garage/R&D economy, MY TEAM roster and wages, objectives/reputation/rollover determinism, reliability DNFs, save slots, guides, settlement, or sponsors.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "split",
   "verdictDetail": "2049 lines and ~80 tests, many staging a full weekend plus 24-round season sims \u2014 its own comments document repeated 120s-budget fights; splitting into career-core (save/mode/isolation), career-economy (garage/objectives/rollover/sponsors/reliability), and career-ui (hub/slots/modes screen/guide) would let pick-tests target a fraction and stop the budget churn.",
   "weakSpots": "Assertions are consistently excellent (anti-vacuity everywhere, e.g. L1412 'the plan is real, so the equality above is not vacuous'); the one honest coin-flip is the gated test.skip at L1028; L1459-1463 defines addrOf after use inside the same test (works, hoisting) alongside an unused const addr at L1454."
  },
  {
   "file": "tests/specs/carview-parts.spec.js",
   "purpose": "Failing means the standalone tools/carview.html audit page broke: parts-category selectors missing, the frame counter not advancing after state changes, or the effects API not round-tripping onto a rendered frame.",
   "assertionQuality": "mixed",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Adequate guard for a dev tool \u2014 presence checks plus two real frame-advance/state-round-trip tests; cheap enough (no circuit build) to leave alone in test:parts.",
   "weakSpots": "L5 title says 'eight parts categories' while the loop asserts twelve (L9-10) \u2014 stale title; L27-34 is visibility-only (controls could be wired to nothing and pass); no test asserts a rendered pixel changed with the parts."
  },
  {
   "file": "tests/unit/circuit-def-fields.test.mjs",
   "purpose": "Failing means a field authored in js/circuits/<id>.js was silently dropped by the field-by-field copy into Tracks.LIST and reads undefined at every consumer (the pal/sunAzimBias/sceneryTheme bug class).",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Exemplary guard: general hasOwnProperty sweep plus the five once-dropped fields pinned by value, with a justified ENGINE_ONLY escape hatch; cheap (VM buildContext, runs in test:tooling-fast).",
   "weakSpots": "None. hasOwnProperty (not truthiness) at lines 60-63 is exactly right; spa ownPitStraight=false pin at line 109 catches the falsy-copy edge."
  },
  {
   "file": "tests/specs/collision-ai-fixes.spec.js",
   "purpose": "Failing means one of the June 2026 audit fixes regressed: wrong-way threshold/hysteresis, wall penalty on open circuits, rescue-grace reset, rear-end contactT, >6-car separation window, AI banking grip, or Jeddah barrier alignment.",
   "assertionQuality": "mixed",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/collisions.spec.js",
    "tests/specs/elevation-tracks.spec.js"
   ],
   "verdict": "strengthen",
   "verdictDetail": "Most tests are genuinely strong relative regressions (banking grip vs flat-straight reference is model quality), but the wall-scrub test proves nothing and the file is triple-billed (test:collision + test:behaviour globs plus explicit test:barriers), so its 10 race builds across 3 tracks run three times over.",
   "weakSpots": "Line 131: 'speed is scrubbed when pinned against Monza barrier' asserts only speedAfter < 20 after 10 throttle-off frames \u2014 coasting drag alone passes this with the pushIn penalty deleted; it needs a no-wall control run or a magnitude floor. Line 34: the slow-crawl loop breaks the moment speed > 13, so how long the sub-threshold state is actually held is timing-dependent. Line 353 pins hw + barrierGap - 1.1 with an unexplained 1.1 (deliberate pin, but opaque). 'Player stays on banked Zandvoort section' (315-334) substantially re-proves elevation-tracks' zandvoort banked-corner test; '10-car pack digs out' (248-276) re-proves collisions.spec.js jam dig-out at n=10 vs n=5."
  },
  {
   "file": "tests/specs/collisions-deep.spec.js",
   "purpose": "Failing means player-side collision feedback broke: Frenet-space pushes not fed back to world-space player state, interpenetration, energy injection, wall overshoot, or the perpetual-side-rub speed-death regression.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/collisions.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "15 scenario tests each asserting a physically meaningful invariant (push direction, min-gap while alongside, per-frame displacement cap, monotonic prog across the line, maxWallOvershoot < 0.01) \u2014 the model of a behavior suite; uses fixtures.js for failure attachments; the cost (15 race builds, Monza+Monaco) is what it costs.",
   "weakSpots": "Thresholds like hw+9.5 (line 81) and maxAbsX < 20 (176) are stated-as-band rather than derived, but each has an in-file rationale; only mild redundancy with collisions.spec.js's full-pack-no-NaN test."
  },
  {
   "file": "tests/specs/collisions.spec.js",
   "purpose": "Failing means AI-to-AI collision resolution broke: an overlapped pair no longer separates while alongside, a jammed pack cannot dig out, or a full pack NaNs/leaves the track within 10 s.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/collision-ai-fixes.spec.js",
    "tests/specs/collisions-deep.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Three tight tests; the peak-gap-while-alongside fix (documented at lines 28-36) shows the assertion was hardened against exactly the sampling bug that made the old version meaningless; the jam(5) dig-out overlaps collision-ai-fixes' jam(10) pack tests but asserts the extra pairwise non-overlap condition.",
   "weakSpots": "None vacuous; jam dig-out speed floor of 12 m/s (line 73) and on-track bound of 12 m are judgment thresholds with generous margins."
  },
  {
   "file": "tests/unit/comment-citations.test.mjs",
   "purpose": "Failing means a cross-file 'other.js:412' comment citation points past the end of the file it names, or the citation population grew (or the ratchet ceiling was left slack).",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [
    "tests/unit/docs-integrity.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "Existence check + growth ratchet + anti-slack ratchet is the complete idiom; overlaps docs-integrity's cited-line test but deliberately: this one covers css/ and carries the ratchet, docs-integrity sweeps tools/ and tests/ \u2014 the files document each other.",
   "weakSpots": "Comment-detection heuristic (lines 93-95) intentionally under-matches (strings containing // etc.), so the ratchet count is approximate \u2014 acknowledged in-file and the right trade."
  },
  {
   "file": "tests/unit/component-inventory.test.mjs",
   "purpose": "Failing means docs/COMPONENTS.md drifted from css/: a class family exists undocumented, a documented family left, the dead-class list is wrong, or a cross-file shared class is unnamed.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "The fourth test (lines 91-102) closes the exact hole the file's own history documents \u2014 computing shared-class data and never asserting it \u2014 so all four directions of drift are now covered.",
   "weakSpots": "Line 72: dead-class detection uses substring includes() over concatenated js/ + index.html, so a dead class whose name is a substring of any identifier is counted live (misses, never false-fails) \u2014 acceptable for a rot guard."
  },
  {
   "file": "tests/unit/coplanar-faces.test.mjs",
   "purpose": "Failing means a circuit gained same-facing coplanar face pairs \u2014 z-fighting that flickers at every distance and that clip-audit structurally cannot see \u2014 or the baseline went stale.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Correct ratchet semantics (unlisted circuit caps at 0, listed circuit fails on growth, no ALLOW hatch, stale-baseline check both directions); heavy because the sweep rebuilds all 40 circuits (part of the measured 14m20s test:sweeps), mitigated by the shared cached sweep.",
   "weakSpots": "Line 58: roster floor of >= 24 against 40 circuits (confirmed known-already) \u2014 a sweep that silently dropped up to 16 circuits would still pass the floor; the stale-baseline test only protects baseline-listed ids, so a NEW circuit vanishing from the sweep is invisible. Cheap fix: assert against the js/circuits/ file count."
  },
  {
   "file": "tests/unit/css-comments.test.mjs",
   "purpose": "Failing means a CSS comment ended early or never opened, turning prose into a selector prelude and silently dropping the rule after it.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "The prelude-length signature (real max 173 vs failures 275/759, gate at 220) separates cleanly and the rationale for why the gap cannot close is written down; brace-balance companion catches the unterminated-comment case.",
   "weakSpots": "None; the 220 threshold is arbitrary only in the harmless sense \u2014 both populations are measured and far apart."
  },
  {
   "file": "tests/unit/css-layers.test.mjs",
   "purpose": "Failing means a rule fell outside its file's @layer block (stray closing brace or trailing rules), making it unlayered and silently outranking every layered rule in the project.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "strengthen",
   "verdictDetail": "The assertions it makes are exact (depth balance, close-at-last-content, single cascade-order declaration), but its trigger has a hole: the fully-unlayered failure mode it exists to prevent is exempt if the wrapper is simply deleted \u2014 add a roster assertion that every css/ file except tokens.css opens with @layer.",
   "weakSpots": "Line 63: `if (!/^@layer\\s+[\\w-]+\\s*\\{/.test(first)) continue;` \u2014 a stylesheet that loses its opening @layer wrapper entirely is silently skipped, i.e. 100% of its rules become unlayered and no test fires; nothing asserts WHICH files must be layered."
  },
  {
   "file": "tests/unit/css-tokens.test.mjs",
   "purpose": "Failing means a design token in css/tokens.css has no var()/setProperty consumer anywhere \u2014 an invitation for the next author to inherit an unmaintained value.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "No allow-list by design, comment-stripped definitions, JS setProperty consumers counted, and a scan-broke sanity floor (>30 tokens) so an empty result cannot pass vacuously.",
   "weakSpots": "None material; a token consumed only by another unused token's var() inside tokens.css would count as used, but that chain terminates at this same test on the outer token."
  },
  {
   "file": "tests/specs/custom-team.spec.js",
   "purpose": "Failing means the MY TEAM save flow leaks or fails to rebuild GPU resources (decal atlas, cached body meshes) after a repaint, or the livery row's select/edit/delete stopped being independent keyboard buttons.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Sophisticated instrumentation done right: atlases tagged by team (fixing the documented createdTextureIds[0] race), leak check scoped to the pre-save stale set rather than created-vs-freed, and an explicit guard-the-guard assertion (line 113) so the tagging cannot rot silently; measured 132 s for one test under SwiftShader (setTimeout 240s), so heavy is inherent.",
   "weakSpots": "None vacuous; the flow's fragility is UI-path length (two full garage round-trips), not assertion quality."
  },
  {
   "file": "tests/specs/data-lifecycle.spec.js",
   "purpose": "Failing means a stale async response (older year/meeting/session/driver/refresh request resolving late) overwrites newer data-hub state, or the hub's modal/focus contract broke.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Deferred-promise injection resolving responses deliberately out of order is exactly how last-writer-wins races must be tested; loads only the data modules (no game boot), measured 0.6-12s per test in artifacts/logs, so it earns its place in test:api.",
   "weakSpots": "Line 279 waitForTimeout(1100) exists to force a distinct timestamp \u2014 justified but a fixed sleep; setInterval capture (99-101) monkey-patches globals per test, contained."
  },
  {
   "file": "tests/specs/debris.spec.js",
   "purpose": "Failing means the Rapier debris side-world broke a phase-R0/R1 promise: not inert/zero-fetch when opted out, no debris from a real wall hit, seeded episodes not bitwise-replayable, or the pool cap not enforced.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Each test pins a contract with teeth \u2014 rapierFetches === 0 and stepped === 0 for opt-out, maxD < 1e-9 for determinism, live === cap exactly for recycle-oldest \u2014 and load failures throw with the module's own error rather than timing out; 5 race builds plus WASM init makes it heavy.",
   "weakSpots": "None; the wall-hit test's bounded retry loop (lines 114-117) exits on lastImpact rather than sleeping."
  },
  {
   "file": "tests/unit/deploy-staging.test.mjs",
   "purpose": "Failing means the Pages workflow's hand-edited cp allow-list no longer covers something the shipped code can fetch (or stages a path that does not exist) \u2014 the vendor/-404-in-production-only bug class.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Parses the workflow rather than duplicating the list, covers tags, importmap, sw precache, and runtime-built vendor URLs, and checks both directions plus file-level importmap existence; if the workflow refactors away from `cp ... _site/`, staged() goes empty and the first test fails loudly rather than passing vacuously.",
   "weakSpots": "The runtime-URL scan (lines 71-77) only catches vendor/ paths in string literals \u2014 a dynamically concatenated path would slip through, an accepted heuristic bound."
  },
  {
   "file": "tests/specs/dev-tools.spec.js",
   "purpose": "Failing means a window.__apex dev-API method changed shape or stopped mutating game state as documented \u2014 this is the living contract for the surface every other test and agent workflow drives.",
   "assertionQuality": "mixed",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Shape-level typeof checks are the deliberate altitude for a contract file, and it also carries real behavior (finishRace uses live field order, slipFactor drops under braking, freeze round-trips); confirmed ~11-13s per test in artifacts/logs across ~37 tests, and it sits in test:tiny, test:api, and test:fast, so its heaviness is paid often \u2014 worth remembering before adding more tests here.",
   "weakSpots": "Lines 609-619: 'setInput and clearInput work together' is self-described no-assert-beyond-no-crash. Many tests assert only typeof (e.g. camState 237-246, viewState 251-259, probe 363-373, physState 385-395) \u2014 would catch a rename/removal, not a wrong value; acceptable for the contract role. Screenshots at 118 and 320 are gallery side-effects, not assertions."
  },
  {
   "file": "tests/unit/docs-integrity.test.mjs",
   "purpose": "Failing means a mechanically checkable doc claim drifted from the repo: dead file paths in live docs/skills/source comments, wrong suite/circuit/category counts, stale citation line numbers, unindexed tools/skills/docs, or a re-hardcoded season year.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "overlaps": [
    "tests/unit/comment-citations.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "Every check is anchored to a documented past failure and stays strictly in mechanically-verifiable territory; the cited-line check (lines 103-171) overlaps comment-citations' first assertion, but with different scope (tools/tests here, css + ratchet there) and both are trivial-cost, so consolidation would buy little.",
   "weakSpots": "Longest exemption surface in the batch (PLACEHOLDERS, SOURCE_EXEMPT, archive/research skips) \u2014 each entry carries a reason, which is the discipline that keeps it honest; regex path-matching is inherently heuristic but tuned (longest-first extensions, URL lookbehind)."
  },
  {
   "file": "tests/specs/drift.spec.js",
   "purpose": "Failing means the dynamic bicycle tyre model lost its qualitative contract: default stability at the limit, SLIDE loosening the rear, self-alignment on release, grip-limited (not speed-proportional) yaw, or SPEED STEER's lock taper.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Model relative-assertion physics testing \u2014 planted-vs-loose, slow-vs-fast, calm-vs-sharp comparisons with pace pinned so a PACE retune cannot silently change what is measured; lives in test:collision by documented design (docs/TESTING.md line 113) even though it is a tyre-model spec.",
   "weakSpots": "Absolute bands (peakSlip < 45 at line 53, maxAbsX < 60, tail < 1.5 at line 82) are qualitative judgment values, but each states the failure mode it separates from (spin vs wash, fly-off, non-decaying slide)."
  },
  {
   "file": "tests/specs/elevation-tracks.spec.js",
   "purpose": "Failing means slope gravity, banking geometry, or road-following broke on some circuit: descents overspeeding past a measured flat reference, climbs acting as walls, banked corners undrivable, or the banking mesh pivoting wrong.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "The heaviest file in the batch (40 per-circuit tests each building a full circuit, self-documented ~24s each, deliberately sequential single-worker) but the assertions are the best kind \u2014 measured flat-out reference before the scan (with the Monaco traffic bug written up), a legibility pre-assertion at line 272 that fails where the cause is readable, and elevation-race-proofed startup wait; note it runs in BOTH test:physics and test:circuit, so its cost is paid twice when both groups run.",
   "weakSpots": "The 1.35x overspeed ceiling (line 273) and hw+8 road-follow band (line 280) are generous judgment bands; climbGain > 0.5 (line 277) is a weak floor by admission ('just require the car is still moving') \u2014 all rationalized in-file."
  },
  {
   "file": "tests/specs/f1-track-accuracy.spec.js",
   "purpose": "Failing means a game circuit's centreline no longer matches its pinned real-world GeoJSON reference in shape or race direction, or the circuit-to-reference mapping went incomplete.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "One test covering all 40 circuits against a pinned fixture with explicit direction overrides and bidirectional set-equality on the mapping; no track build needed (reads CircuitPaths data), 120s budget; the SVG gallery and report.json are diagnostic side-effects after the real assertions, not instead of them.",
   "weakSpots": "None; MAX_SHAPE_ERROR lives in the shared validator (itself unit-tested by track-accuracy-validator.test.mjs), and the failure list is asserted empty, not logged-only (line 109)."
  },
  {
   "file": "tests/unit/fixture-consumer-audit.test.mjs",
   "purpose": "Nominally: failing means the fixture-import detection regex broke; in practice it is the only CI-run trace of the fixture-adoption ratchet, and it does not enforce the ratchet.",
   "assertionQuality": "weak",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "strengthen",
   "verdictDetail": "Two synthetic unit tests of fixtureImportViolations prove the regex on toy input and nothing about the repo \u2014 and I verified nothing runs the tool's CLI (no test:* script, no CI step; ci.yml runs test:tooling-fast and test:audit only), so the FLOOR=31 adoption ratchet and the four load-bearing FIXTURE_CONSUMERS that docs/TESTING.md claims are 'ratcheted so it cannot go backwards' are actually unenforced; add tests calling fixtureImportViolations(readSpecs()) === [] and adoption(readSpecs()).uses >= FLOOR so the guarantee runs in test:tooling-fast.",
   "weakSpots": "Whole file (lines 5-19): synthetic two-entry Maps only; never imports readSpecs/adoption/FLOOR; the real-world enforcement path is dead code from CI's perspective."
  },
  {
   "file": "tests/specs/gamepad.spec.js",
   "purpose": "Failing means the W3C standard-mapping gamepad contract broke: steering dead zone, digital d-pad lock, trigger/face pedals, edge-triggered latches, disconnect/blur/hidden release, or the menu-navigation mapping (including the native-<dialog> cancel seam and the driving/menu mode split).",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Mocked getGamepads + real Input.poll exercises the actual seam; the menu-nav half tests the exact non-obvious behaviors (untrusted-key Escape does not close a <dialog>, bumpers must not also shift gears) with a driving-unaffected regression guard; most tests need no race build so the file stays reasonably priced.",
   "weakSpots": "Fixed sleeps at lines 233, 252, 275, 280 (waitForTimeout 250-400ms) are flake-prone on a loaded box but back real assertions; the held-repeat test (334-358) depends on wall-clock exceeding 450ms between polls, which a very slow evaluate round-trip could theoretically violate on the no-repeat step."
  },
  {
   "file": "tests/specs/garage-aero.spec.js",
   "purpose": "Proves the GARAGE X-MODE demo actually eases the flaps, moves real wing geometry by a visible amount, and re-aims the camera at the rear wing \u2014 failing means the button visibly does nothing again (the exact bug that shipped).",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Every assertion is behavioral and monotone (ease progression, close-faster-than-open, mm of leading-edge travel, camera dist/az), driven via __apex.garageStep because rAF never fires headlessly; 6 tests boot the page but never build a circuit. Grouped in test:parts.",
   "weakSpots": "The dist<3.2 camera thresholds (lines 86, 97) are magic numbers, but the file documents why (135mm projects to ~10px at whole-car framing); no genuinely weak asserts."
  },
  {
   "file": "tests/specs/headless-api.spec.js",
   "purpose": "Contract of the RL-style headless loop (headless()/obs()/act()/reset()) \u2014 failing means an agent driving via obs/act gets wrong shapes or physics that don't respond to inputs.",
   "assertionQuality": "mixed",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/obs-act-edge.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Core is strong and behavioral (throttle raises speed, brake lowers it, n=10 advances further than n=1, reset places within \u00b150m), but each of 25 tests re-boots and rebuilds monza \u2014 api.log shows 17-55s per test, several minutes wall; trimming the three trivial toggle tests into one would cut real cost.",
   "weakSpots": "Confirmed known: 'returns false when player not yet initialised' (lines 290-303) accepts BOTH outcomes so it can only fail on a throw. Lines 22-45 are three separate boots for trivial getter/setter round-trips. The big obs test (64-131) is mostly typeof/shape checks \u2014 fine for a contract, but the position/motion values are only checked as numbers, not for plausibility (speedKph cross-check line 73 is the exception)."
  },
  {
   "file": "tests/unit/hooks-documented.test.mjs",
   "purpose": "Ratchet: no new __apex hook may ship absent from docs/DEBUG-HOOKS.md, and the UNDOCUMENTED grandfather list cannot rot in either direction \u2014 failing means the dev-API reference stopped being trustworthy.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Textbook ratchet with both anti-rot directions asserted (stale entries at 72-77, ghost entries at 79-84); pure fs reads, milliseconds, in test:tooling-fast.",
   "weakSpots": "hookNames() regex (line 54) depends on 2-space indentation of the api literal in js/game/apex.js \u2014 a reformat would silently shrink the hook set, though the UNDOCUMENTED-gone test would likely trip on it."
  },
  {
   "file": "tests/specs/hud-audit.spec.js",
   "purpose": "Screenshot gallery of the HUD across 4 steer modes x 2 orientations with a minimal 'it actually rendered' assert \u2014 failing means the HUD or the mode's on-screen controls didn't appear at all.",
   "assertionQuality": "weak",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/hud-layout.spec.js",
    "tests/specs/ui-audit.spec.js"
   ],
   "verdict": "merge",
   "verdictDetail": "Merge into tests/specs/hud-layout.spec.js (or fold the shots into the ui-audit gallery): its only assertions (assertHud, lines 39-45 \u2014 #hud visible plus one steering control visible) are a strict subset of what hud-layout proves per mode/orientation with real geometry, yet each of 8 tests pays a reload + bahrain build; the screenshots are the only unique value.",
   "weakSpots": "shot() (lines 30-33) is the real payload and asserts nothing; assertHud checks only visibility of #hud and '#btn-steer-left, #btn-throttle' .first() (line 43) \u2014 a broken layout, overlapping controls, or a blank canvas behind a visible DOM HUD all pass."
  },
  {
   "file": "tests/specs/hud-layout.spec.js",
   "purpose": "No touch control may overlap another, no HUD readout may sit on a tap target, and everything must stay inside the safe area across notched/small phones in both orientations \u2014 failing means a shipped layout collision like the AERO-chip-over-BRAKE bug.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Measures real geometry (circle-aware clash test for arc buttons, conservative rects for readouts, safe-area insets injected because headless Chromium reports them as 0) across 24 combos plus a desktop hide check; e-ui.log shows 27-48s per test so it is genuinely expensive, but every dollar buys a real assertion.",
   "weakSpots": "HUD_LANDSCAPE_ONLY (lines 38-55) deliberately stops asserting a measured 8.7px portrait collision because #rotate-device covers it \u2014 documented and defensible, but it means portrait .hud-top/.hud-gaps/minimap overlap is permanently untested; count>3 floor (line 125) is a loose liveness check."
  },
  {
   "file": "tests/unit/image-grade-shaders.test.mjs",
   "purpose": "The HDR grade math must be identical across the JS reference model, the GLSL post shader, and the WGSL mirror (zone weights, toe/shoulder, guards, composite order) \u2014 failing means the backends' grades drift apart or a control goes non-finite.",
   "assertionQuality": "mixed",
   "costTier": "cheap",
   "overlaps": [
    "tests/specs/image-grade-visual.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "The behavioral tests (neutral preservation 115-127, all 65k min/max combos finite 129-151, channel isolation 153-165) run against a local JS reimplementation (hdrGrade, lines 39-55) and only bind to shipped code via the exact-source regex pins in tests 66-113 \u2014 brittle to harmless reformatting but genuinely enforcing GLSL/WGSL formula parity, and image-grade-visual covers the end-to-end pixels; keep as the fast half of that pair.",
   "weakSpots": "Tests 115-165 cannot catch a bug in the actual shaders except through the regex pins; the pins themselves (e.g. lines 72-87, 93-107) fail on any whitespace/reorder refactor even when behavior is preserved \u2014 change-detector risk, mitigated by the visual spec."
  },
  {
   "file": "tests/specs/image-grade-visual.spec.js",
   "purpose": "The grading sliders must change actual rendered pixels in the correct tonal range/channel, extremes must not kill the canvas, and five representative track/tod/weather conditions must keep broad tonal range without clipping \u2014 failing means the grade is cosmetic-in-name-only or a shipped condition clips.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/unit/image-grade-shaders.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "Signed and relative pixel deltas with count floors (darkCount>1000) guard against vacuous blank captures, and the histogram gates on five real conditions are the only thing standing between a lighting retune and shipped black-crush; serial mode + 180s timeouts + 6 distinct circuit builds make it one of the most expensive files in test:webgl, justifiably.",
   "weakSpots": "Thresholds are judgment calls: 2x dominance ratios (194, 204, 214-215), clip fractions 0.08/0.03 and p95-p05>45 (250-252) \u2014 reasoned but tunable; the discard-first-frame pattern (172, 186) papers over render-cache settling with no assertion that it settled."
  },
  {
   "file": "tests/unit/import-models.test.mjs",
   "purpose": "tools/import-models.mjs must emit AX26 bytes that js/render/assets.js can parse, with palette-atlas colour baked into vertex colour \u2014 failing means CI-imported models arrive corrupt or flat grey.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Builds a hand-rolled PNG+glTF fixture, runs the real importer as a subprocess, and byte-parses the output exactly as the consumer does (magic, vert/index counts, height normalisation to 12m, red/green palette sampling, MAT id on every vertex); one spawn, sub-second, in test:tooling-fast.",
   "weakSpots": "None material; cleanup deferral via process.on('exit') inside finally (line 115) is odd but harmless."
  },
  {
   "file": "tests/specs/instanced-draw.spec.js",
   "purpose": "The GLX consumer of TrackGraph.batches(): a real Monza payload uploads to GL instance buffers, frustum culling packs correctly, and instancing state does not black out ordinary draws \u2014 failing means scenery instancing silently broke in the browser half that track-graph.test.mjs cannot see.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/unit/track-graph.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "Explicitly de-vacuoused (the line 53-57 comment converts what used to be a skip into a hard failure on zero batches), the culling test uses raw planes so it cannot drift with the projection, and the overlap with track-graph.test.mjs is a documented division of labour (maths vs wiring), but it pays 3 Monza builds with 180s waits.",
   "weakSpots": "Lines 17-24 and 100-103 are typeof-only existence checks \u2014 castShadowInstanced is asserted to exist, not to actually draw a shadow; line 115's 20KB screenshot-size heuristic for 'scene not black' is a proxy (reasoned: blank frames compress to a few KB)."
  },
  {
   "file": "tests/unit/light-presets.test.mjs",
   "purpose": "Every one of the ~1,921 shipped lighting preset knobs must name a live TUNE_DEFS id \u2014 failing means a knob rename silently orphaned shipped looks (the resolution falls back to defaults without any error).",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Guards a real silent-divergence failure mode between two files nothing else connects, with scan-broke floors (ids>100 at line 54, settings>500 at line 67) so a regex slip cannot go green; pure node, milliseconds, in test:tooling-fast.",
   "weakSpots": "tuneIds() scrapes id: \"...\" by regex from lighting.js source (line 40) \u2014 an id built dynamically would be invisible, though the size floor would catch wholesale breakage."
  },
  {
   "file": "tests/specs/lighting-ab.spec.js",
   "purpose": "The A/B harness knob catalog cannot rot against its source files, and the live lighting invariants hold (weather() applies lighting not just wetness, night fog glows, night light budget and exposure floors) \u2014 failing means a shipped lighting regression or a silently stale A/B harness.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "The catalog-integrity test is a cheap exact-once pin including the silent-patch FREEZE_FLICKER trap, and the live tests each encode a named shipped bug (weather-changed-nothing, fog grey-wash) with waits on actual state rather than sleeps where it matters; Singapore/Qatar/Vegas night scenes under SwiftShader make it expensive (180s timeouts).",
   "weakSpots": "Absolute pins exposure 0.90 / floodEmit 0.78 (lines 119-120) fail on any deliberate retune \u2014 intended as a catalog, but they make retunes noisy; fog-glow 1.1x region-mean threshold (line 104) and the fixed 2200/1100/3000ms sleeps (lines 28-31, 101) are empirically tuned to the SwiftShader box."
  },
  {
   "file": "tests/unit/lighting-campaign.test.mjs",
   "purpose": "The offline lighting-campaign tooling behaves: the 800-condition matrix enumerates uniquely, config validation rejects bad shards/cameras, capture retry/cleanup lifecycle is leak-free, merges reject duplicates/gaps, and metrics/gates classify clipping correctly \u2014 failing means a multi-hour campaign run produces wrong or lost data.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Genuine unit coverage of failure modes that matter for a long-running batch tool (retry pages closed, original preserved, blocked-after-3, duplicate/missing condition rejection), using clever data-URI config mutation to test validators; pure node with two ephemeral HTTP servers, seconds, in test:tooling-fast.",
   "weakSpots": "Lines 315-328 assert capture.mjs's source by regex (export names, doesNotMatch /3456/) \u2014 change-detector-ish; the minimalProfile trace assertions (lines 175-180, 225-233) pin exact call sequences, so a behavior-preserving reorder of the search fails them."
  },
  {
   "file": "tests/specs/lighting-tuner-grade.spec.js",
   "purpose": "The IMAGE & COLOUR tuner tab exposes all 16 grading controls in the right sections, and values clamp, persist across reload, reset to shipped, and export \u2014 failing means the panel or its persistence pipeline broke.",
   "assertionQuality": "mixed",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "The second test is real behavior (clamp 9 to 1 and 0.1 to 0.5, gainB survives reload, reset lands on LightPresets['*'] values, export JSON contains the value); two tests, one bahrain build plus a reload, sits reasonably in test:webgl alongside its image-grade siblings.",
   "weakSpots": "First test (lines 26-34) is pure presence \u2014 heading text equality and input visibility, no behavior; the reset comparison (lines 51-59) computes 'shipped' from the same LightPresets global the app reads, so a preset-loading bug that broke both sides identically would pass."
  },
  {
   "file": "tests/unit/load-order.test.mjs",
   "purpose": "index.html, tools/carview.html, sw.js and game.js's BACKEND_FILES must all agree with tools/manifest.cjs (script order, CSS order, uniform ?v=, deferred files accounted for, HARD_EDGES ordered) \u2014 failing means a hand-edited shell diverged from the single source of truth, the exact failure that once shipped vendor/ to Pages.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Every assertion is an exact structural equality against the manifest, including both anti-rot directions (missing files AND dead manifest entries, line 78-79) and the three-way DEFERRED agreement; pure fs, milliseconds, load-bearing for the whole no-build-step architecture.",
   "weakSpots": "Regex extraction of BACKEND_FILES (line 109) and sw.js's optional Set (line 121) depends on exact declaration shapes \u2014 a formatting change fails loudly rather than silently, which is acceptable."
  },
  {
   "file": "tests/specs/logging.spec.js",
   "purpose": "js/log.js semantics in a real page: loads first, retention follows the raised console level, records are flattened strings not live references, namespace tag printed once, filters work and bad specs don't throw \u2014 failing means the diagnostic facility became quietly useless (or a leak).",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Each test maps to a documented way the facility can fail without throwing, uses the consoleLines fixture to assert what actually printed, and the monaco-build test proves the ring explains a real build; 6 tests, one circuit build, measured 15-65s each in api.log, in test:api and test:tiny.",
   "weakSpots": "The track-build test (lines 67-76) only asserts count>0 and that scenery-or-track appears among namespaces \u2014 loose, but deliberately so since build content varies."
  },
  {
   "file": "tests/specs/longitudinal.spec.js",
   "purpose": "Longitudinal physics ordering (brake < coast < throttle), grass drag, speed-sensitive steering, slope sanity on Spa, and lap-counter wrap at start/finish \u2014 failing means the driving model's basic force balance or lap accounting regressed.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Model relative-assertion practice: PACE pinned so bounds mean something, peak-before-corner instead of the old wrong final-speed read, measured flat top speed as the descent reference instead of a VMAX literal, and the lap-wrap test opts into roadFollow explicitly with the reasoning written down; 6 tests with monza boots plus a spa build carrying 600+ jump/step probes, in test:physics.",
   "weakSpots": "The 20<peak<150 band (lines 66-67) and 1.35x descent margin (line 170) are wide by design \u2014 they catch catastrophe, not drift; climbGain>5 (line 171) similarly coarse."
  },
  {
   "file": "tests/manual/blank-scan.spec.js",
   "purpose": "No circuit renders a near-blank frame at any of 25 lap positions (camera inside scenery / staring into void) \u2014 failing names the circuit and lap fractions where the chase cam sees nothing.",
   "assertionQuality": "mixed",
   "costTier": "heavy",
   "overlaps": [
    "tests/manual/inspect.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "The one real assert (blanks toEqual [], line 62) does catch its target failure, and the file is deliberately excluded from discovery (40 circuits x 25 SwiftShader frames); keep as the asserted manual scan, ideally absorbing inspect's sheet output (see that file).",
   "weakSpots": "'Blank' is defined as PNG < 80,000 bytes (line 22) \u2014 a compression proxy that drifts with viewport and content (a uniformly-lit but wrong frame passes; a legitimately dark night frame could false-positive); the culled count (lines 43-49) is computed and only console.logged, never asserted."
  },
  {
   "file": "tests/manual/inspect.spec.js",
   "purpose": "Emits a 5x5 per-circuit contact sheet for human visual review (roof-over-racing-line class of bug that a bright frame hides) \u2014 it gates nothing and cannot fail short of a crash.",
   "assertionQuality": "vacuous",
   "costTier": "heavy",
   "overlaps": [
    "tests/manual/blank-scan.spec.js"
   ],
   "verdict": "merge",
   "verdictDetail": "Merge into tests/manual/blank-scan.spec.js: it contains zero expects by design (imports only test, not expect), and its capture loop (25 frames, jump+snapCam, ~230ms settle, lines 37-45) is byte-for-byte the same as blank-scan's \u2014 one pass could assert blanks AND write the sheet, halving a 40-circuit SwiftShader run whenever both are wanted.",
   "weakSpots": "No assertions anywhere (entire file); honest about it in the header ('it emits images for a human, it does not gate anything'), so the only question is whether the duplicate capture pass earns its keep \u2014 it doesn't."
  },
  {
   "file": "tests/specs/map-hooks.spec.js",
   "purpose": "Contract of __apex.mapPts()/trackBounds() (normalized picker outline points, world span, centerFrac) \u2014 failing means the minimap/picker data hooks broke.",
   "assertionQuality": "mixed",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "strengthen",
   "verdictDetail": "Strengthen: the [0,1] bounds and span checks are real but the comment's north-up claim (lines 31-33) is asserted only as y-span > 0.5 \u2014 orientation itself is untested (a flipped map passes) \u2014 and the orbit() call (line 43) has no assertion after it, so 'usable with orbit' only fails on a throw; assert a known-frac landmark's map position and that camState changed after orbit.",
   "weakSpots": "Line 32-33: north-up orientation claimed, only span asserted. Line 43: orbit(b.centerFrac,...) result never checked. Line 17: fixed 3000ms sleep instead of waiting on info().track. Lines 37-38: spanX/spanZ>100 are loose sanity floors. Variable nullPts (line 12) documents an inverted historical expectation."
  },
  {
   "file": "tests/specs/material-shimmer.spec.js",
   "purpose": "The baked tarmac must not add more than 2x the frame-to-frame temporal noise of the procedural look while driving (the 'road is boiling' risk the asset pack carries) \u2014 but only when APEX_SHIMMER=1 is set.",
   "assertionQuality": "mixed",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "The measurement design is genuinely good (identical motion at matTex 0/1 so the ratio isolates the textures; anti-blank guard line 107 and motion floor line 108 prevent vacuous green) but the line 89 env-gate means it runs in no group by default and its own comment admits it has never completed \u2014 keep as the documented opt-in it is (same policy as tracks-visual), with the honest caveat that until someone runs it on real hardware once it proves nothing.",
   "weakSpots": "Line 89: test.skip(!process.env.APEX_SHIMMER) \u2014 never exercised in CI or any test:* group except test:shimmer, which nothing invokes automatically; header admits 'it has never been run to completion'; the 2.0 ratio (line 114) is untested against any real measurement."
  },
  {
   "file": "tests/specs/menu-baseline.spec.js",
   "purpose": "Six blessed pixel baselines (title/select/garage x phone-landscape/desktop) catching the IDENTITY half the geometry audit can't see \u2014 failing means the menus' colour/type/spacing changed and a human must bless it.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/menu-survey.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "toHaveScreenshot against committed snapshots (the repo's only golden baselines, per CLAUDE.md) with the render loop stopped via headless(true) so the baseline can actually match; deliberately capped at six so review stays honest rather than rubber-stamped; no circuit builds but 1440x900 SwiftShader captures need the 60s timeout.",
   "weakSpots": "maxDiffPixelRatio 0.01 (line 71) is ~5,000 pixels at phone size \u2014 a small accent element could change entirely inside the tolerance; baselines are SwiftShader-platform-locked (documented, lines 19-22), so any runner change invalidates all six at once."
  },
  {
   "file": "tests/specs/menu-keyboard.spec.js",
   "purpose": "Failing means desktop menu input is broken: wheel redirection to the circuit list, arrow-key nav vs car steering, modal top-layer ranking, dialog inertness, and the Escape-is-back contract.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Every test pins a documented real defect (z-index:auto modal ranking, div-dialog focus leak, Escape resuming races) with precise both-directions assertions; artifacts/logs/e-ui.log even shows the Tab-escape test catching a live failure (1 failed at 08:28:06), proving it is real signal.",
   "weakSpots": "Minor only: sleep-based settling via waitForTimeout(400/300/250) throughout (lines 175, 227, 272, 311, 364...); test at line 165 depends on waitForTimeout(400) for selection to land; the e-ui log failure on the line-265 test needs triage (regression or flake) but the assertion itself is sound."
  },
  {
   "file": "tests/specs/menu-survey.spec.js",
   "purpose": "Failing would mean a settings/HUD state could not even be reached and screenshotted -- it is a gallery generator, not a regression net.",
   "assertionQuality": "vacuous",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/ui-audit.spec.js"
   ],
   "verdict": "strengthen",
   "verdictDetail": "Deliberate screenshot companion to ui-audit (confirmed mostly screenshot-only), but one-line DOM asserts would make each captured state verifiable at near-zero cost instead of trusting the picture.",
   "weakSpots": "Tests 30/33/34/35/36/40/41/42/43 contain zero expects beyond implicit waits (lines 57-62, 78-84, 86-99, 101-105, 108-115, 132-165); cycleTo at line 61/81 is not asserted to have landed on TILT/MANUAL; test 34 never asserts music/sound actually toggled off (lines 92-97); test 43's campicker wait swallows failure with .catch(() => {}) (line 162), so a broken picker still screenshots green; only tests 31/32 (lines 67, 74) assert anything."
  },
  {
   "file": "tests/unit/module-size.test.mjs",
   "purpose": "Failing means a big module (game.js, apex.js, agentview.js, car3d.js, tracks.js) grew past its ratchet ceiling, or a ceiling drifted >400 lines above its file and stopped ratcheting.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "A two-sided ratchet (over-ceiling AND stale-slack both fail) that cannot be gamed silently; exactly what a line-count guard should be, and it runs in milliseconds in test:tooling-fast.",
   "weakSpots": "None material; the ceilings are policy numbers by design, with the raise/lower protocol documented in the header."
  },
  {
   "file": "tests/specs/multiplayer-lobby.spec.js",
   "purpose": "Failing means the VS FRIEND lobby screen/flow is broken: real RTCPeerConnection constructibility, invite links/QR/share fallbacks, junk-code refusal, and the ids-not-multipliers wire-profile security claim.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/unit/net-transport.test.mjs",
    "tests/unit/net-qr.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "The rtc()-constructs test closes a documented blind spot the fake-transport suite created, and the lobbyMods liar test is a genuine anti-cheat assertion; the deliberate delegation of code-format and QR correctness to the net-unit suites is documented per test, so the overlap is layering, not duplication.",
   "weakSpots": "Line 99: not.toHaveText(\"\") passes on any text including an error; line 222 asserts an environment property (!navigator.share) that a headless-Chromium update could flip independent of this code; lines 287-288's QR structural check (odd, 21+4n) is a shape check only -- real decode correctness lives in net-qr.test.mjs as stated."
  },
  {
   "file": "tests/specs/multiplayer-npeer.spec.js",
   "purpose": "Failing means the rival is keyed by a local grid index again instead of a cross-peer identity: wrong car posed, seat fallback firing, unknown packets routed onto somebody, or both humans gridded in the same box.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/multiplayer-session.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Asserts exactly the surface multiplayer-session cannot see (wire id == driverId, slotFallback === null proving the exact-seat arm fired, unknown-id drop) and its header explicitly defers behavioural coverage to that file, so the pairing is intentional and non-redundant.",
   "weakSpots": "Line 77-79: the unknown-packet test only asserts the one existing buffer count is unchanged -- it cannot see a packet mis-routed onto a car with no buffer, though carAt inspection before/after partially covers that; four full monza builds for four tests is the cost of the seam."
  },
  {
   "file": "tests/specs/multiplayer-roles.spec.js",
   "purpose": "Failing means the human/local role split regressed: a promoted car does not get the bicycle model, a non-local human reads the local stick, per-car mods leak from the local player, or fed-input and local-input trajectories diverge.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "The fed-vs-local equivalence test (toBeCloseTo at 3 decimals under identical seed) is the load-bearing Phase 0 invariant with no other coverage anywhere, and the local-stick isolation test uses opposite-lock inputs as a proper control.",
   "weakSpots": "Line 145: strong-brakes bound (strong < weak - 1) is a directional threshold, arbitrary by 1 m/s but robust; asAi.vLat === 0 (line 80) encodes an implementation fact (AI is kinematic) that a future AI physics upgrade would legitimately break -- the comment says so, which is fine."
  },
  {
   "file": "tests/specs/multiplayer-room.spec.js",
   "purpose": "Failing means the waiting room rules regressed: guest editing the race, START before double-ready, settings starting the race, GO not starting it, screens surviving lights-out, or the friend-quali gating breaking.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Every test asserts a rule with both a positive and negative half (settings arrive AND race did not start; ready toggles on AND off), and the lights-out test's empty-open-screens sweep is a strong whole-DOM invariant; the GO tests must build circuits, which is inherent, not waste.",
   "weakSpots": "Line 135/189/224: 60s timeouts on circuit builds make these the slowest specs in test:net (self-documented with test.slow()); line 101/103 ContainText(\"FER\")/(\"MCL\") depends on team-code rendering conventions, mildly brittle to copy changes."
  },
  {
   "file": "tests/specs/multiplayer-scan-cancel.spec.js",
   "purpose": "Failing means abandoning a QR scan leaves the camera live (privacy) -- CANCEL, lobby CLOSE, or tab backgrounding failing to tear down getUserMedia tracks.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/multiplayer-scan.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Must stay a separate file from multiplayer-scan because the fake camera is a Chromium launch argument (blank feed here so mid-scan is a stable state, not a race) -- the header explains why merging would make the teardown-under-test unreachable.",
   "weakSpots": "Line 76-78 fakes visibilitychange via defineProperty on document.hidden, which tests the handler wiring rather than real browser backgrounding -- acceptable, since real backgrounding is untestable headless; liveTracks reads only the video element's srcObject, so a stream cloned elsewhere would evade it (no evidence the code does that)."
  },
  {
   "file": "tests/specs/multiplayer-scan.spec.js",
   "purpose": "Failing means the camera-scan path is silently dead: getUserMedia, video, canvas readback and jsQR no longer land the decoded invite code in the box, or the camera outlives a successful decode.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/multiplayer-scan-cancel.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "A genuinely end-to-end pipeline test (real Y4M of a code produced by the shipped encoder, real decoder script fetch) covering a failure class the header correctly notes is always silent; only two tests, both necessary.",
   "weakSpots": "45s decode budgets (lines 60, 68) are load-dependent generosity, mitigated by serial mode; the second test re-runs the whole first scan just to check track release -- could fold into one test, but serial isolation after a decode is cheap insurance."
  },
  {
   "file": "tests/specs/multiplayer-seats.spec.js",
   "purpose": "Failing means two players can hold the same seat again: the guest not yielding (or not being told), the garage handing out a TAKEN chip, team-switch landing on an occupied seat 0, or seat logic leaking into solo play.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/parts-setup-ids.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Covers both halves (garage prevention, room resolution) plus the solo-untouched property, with the awaitPeer poll correctly closing the loopback delivery race its own comment describes; the parts-setup-ids overlap (line 213) is a deliberate cross-reference asserting the solo behaviour did NOT change.",
   "weakSpots": "Lines 89-90 pin driver display names (Verstappen/Hadjar) so a roster update breaks this test for non-logic reasons; localStorage seeding depends on the js/game.js:186 read noted in the comment -- a boot-order change would invalidate the fixture silently."
  },
  {
   "file": "tests/specs/multiplayer-session.spec.js",
   "purpose": "Failing means the game side of a net session is wrong: authority rule (rival posed, never simulated), extrapolation along s, AI handback on drop, shared lights-out instant, lamp sequencing, adopted classification/flags, or contact pushing only the owned car.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/unit/net-snapshot.test.mjs",
    "tests/unit/net-session.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "Exemplary: virtual-clock driven, every negative assertion has a control (a stepped AI car proving the sim ran, lamp-dwell sample counts proving no backfill), and most tests cite the real field bug they pin; the net-unit overlaps are unit-vs-integration layering, not duplication.",
   "weakSpots": "Sixteen monza builds make this the heaviest file in test:net; lines 154-156 extrapolation window (4-6 m for an expected ~5 m) and line 208 lossy-link floor (0.04 of a lap) are tuned-but-defensible bounds; line 380's named computation (startPending || state===race) accepts two paths, slightly loosening what 'named' proves."
  },
  {
   "file": "tests/specs/music-library.spec.js",
   "purpose": "Failing means the bring-your-own-music library or Spotify backend regressed: upload/decode/persist/dedupe/remove in IndexedDB, backend delegation, or Spotify making any network contact while unconfigured.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Real decodable WAVs, reload-based persistence proof, request-interception for the Spotify dormancy claim, and the token-drop-on-clientId-change regression are all behaviour-level assertions with pageErrors checked in every test.",
   "weakSpots": "Line 340's waitForTimeout(1500) makes the 'no deferred network' claim a time-budget assertion (inherent to proving a negative); lines 110-113 pin the exact builtin playlist ids, so adding a shipped song breaks this test for a benign reason."
  },
  {
   "file": "tests/unit/net-qr.test.mjs",
   "purpose": "Failing means the shipped QR encoder produces symbols a real camera cannot read -- every assertion round-trips through jsQR, an independent third-party decoder.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "overlaps": [
    "tests/specs/multiplayer-lobby.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "The independent-decoder design defeats the self-consistency trap the header names, and the quiet-zone negative test turns a rendering constraint into an executable fact; the lobby spec's QR test only checks which string is handed to the encoder, so the split of concerns is clean.",
   "weakSpots": "Line 70's version <= 14 scannability bound is a judgment call (stated as such); real-camera optics are obviously out of scope -- jsQR is the right proxy."
  },
  {
   "file": "tests/unit/net-rendezvous.test.mjs",
   "purpose": "Failing means the room-code client broke: code alphabet/normalisation, the offer/answer swap protocol against a contract-faithful relay, soft-failure reporting (down/unreachable/empty/taken), or the seal/open encryption that makes a public broker safe.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Runs the real module against a real in-process HTTP server implementing worker/rendezvous.js's contract, so status codes and JSON shapes are exercised for real, and every failure mode gets its own typed, message-asserted outcome; the GCM tamper and topic-leak tests are genuine security assertions.",
   "weakSpots": "Timer-driven tests (lines 142, 192, 207: 200-300 ms setTimeouts) add a couple of wall-clock seconds and a slim flake window under extreme load; line 102's uniqueness check (>495/500 distinct codes) is statistical but effectively deterministic for the alphabet size."
  },
  {
   "file": "tests/unit/net-sdp.test.mjs",
   "purpose": "Failing means the compact invite codec corrupts a rebuild: lost ICE facts, missing CRLF termination (the Chrome-refusal regression), candidate-type mangling, partial packs, or the relay-candidate-dropped-by-budget bug.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [
    "tests/unit/net-transport.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "Pinned against SDP captured verbatim from a real gathered RTCPeerConnection rather than invented, with the relay-survives-the-budget test documenting a measured field failure; the CRLF overlap with net-transport covers two different modules (pack/unpack vs normaliseSdp) that both shipped the same bug class.",
   "weakSpots": "Line 82's size bound (<100 bytes) would fail on a legitimate format extension, but that is the stated intent (growth must be deliberate); nothing vacuous."
  },
  {
   "file": "tests/unit/net-session.test.mjs",
   "purpose": "Failing means clock sync, channel routing, or liveness broke: offset/RTT recovery across skewed clocks, ping vs state separation, events surviving loss on the reliable channel, the shared-stall-is-not-a-disconnect fix, or handler-throw containment.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "overlaps": [
    "tests/unit/net-transport.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "Deterministic virtual-clock pair with deliberate 10 s skew and the RIVAL DISCONNECTED field bug pinned as its own test including the forgiveness-must-not-hide-real-death counter-assertion; the loss overlap with net-transport is layered (transport proves the loss model, session proves survival on top of it).",
   "weakSpots": "Numeric tolerances (lines 68-69, 75-76, 94: <5 ms, <3 ms, <25 ms) are tight but justified by the 1 ms pump documented at line 48; no weak assertions found."
  },
  {
   "file": "tests/unit/net-snapshot.test.mjs",
   "purpose": "Failing means the 13-byte wire codec or the interpolation buffer silently corrupts a rival's pose: quantisation, signed speed, truncation, lap-wrap shortest-path, along-road extrapolation with cap, out-of-order handling, predict-vs-sample, and lap+s crossing the line together.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [
    "tests/specs/multiplayer-session.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "The prog-monotonic-across-the-line walk and the reversing-loses-a-lap test close exactly the composite gap the file's own comment describes letting a real bug through, and the 13-byte size pin makes bandwidth growth a deliberate act; ms-fast and dense.",
   "weakSpots": "None material; the multiplayer-session overlap is intentional (unit codec/buffer here, integrated pose-on-a-car there)."
  },
  {
   "file": "tests/unit/net-transport.test.mjs",
   "purpose": "Failing means the loopback wire model, ICE/TURN configuration, or the handshake codec broke: deterministic latency/loss, reliable-channel ordering, no dead hardcoded relay, coturn credential derivation, build-mismatch/unknown-build refusal, CRLF-preserving SDP normalisation, fragment-only invite links.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [
    "tests/unit/net-sdp.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "The foundation the whole net stack's testability rests on, plus real regression pins (unknown-build 0===0 waved through, the retired Open Relay credentials, HMAC derivation checked against node:crypto independently); global fetch/localStorage stubbing is scoped with try/finally and fresh module loads where order-dependence threatened.",
   "weakSpots": "Line 133 asserts OPEN_RELAY === undefined -- an absence-of-name check that a rename would satisfy while reintroducing the behaviour (the behavioural tests around it mostly cover that); line 173-176's fresh-module reload pattern is the right fix for the memoisation leak it documents but shows the module has hidden global state the tests must dance around."
  },
  {
   "file": "tests/unit/net-trystero-api.test.mjs",
   "purpose": "Failing means the vendored Trystero surface or our use of it drifted: setter-vs-method shapes, targeted send options, room leave, importmap/bare-specifier completeness, .nojekyll/.mjs deployability, rejection-vs-down distinguishability, and bad relay-override sanitisation.",
   "assertionQuality": "mixed",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Most of it is regex-over-source-text, which its own line-47 comment admits once passed while the code was broken -- but for a dynamic-imported vendored ESM island that no node harness can execute, shape-pinning both sides (vendor AND our usage) is the honest available check, and the relayUrls() test at line 217 does drive real code.",
   "weakSpots": "Lines 34-36, 49-55, 70-79, 197-203: assert.match/doesNotMatch on source text can pass while behaviour is broken by anything the pattern does not capture (e.g. a second unmatched call site); line 180-183's 400-char window around 'relay failure from' is fragile to vendor formatting; the importmap/file-existence tests (105-160) are structural but genuinely fail-able and load-bearing for deploy."
  },
  {
   "file": "tests/specs/new-hooks.spec.js",
   "purpose": "Failing means either an __apex hook contract regressed (timing/sectorState/lapHistory/fieldState/aiPlace/setEnergy/setLap/trackProfile/obs().gear) OR one of eight circuits' foundation diagnostics (elevation, walls, groundY gaps, model manifests, vertex budgets) drifted -- two unrelated concerns in one file.",
   "assertionQuality": "mixed",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/headless-api.spec.js",
    "tests/specs/terrain-over-road.spec.js",
    "tests/specs/tracks-walls.spec.js",
    "tests/unit/shared-track-foundation-characterization.test.cjs"
   ],
   "verdict": "split",
   "verdictDetail": "Split lines 509-877 ('shared track foundation diagnostics' + 'Madrid track foundation migration' -- Silverstone/COTA/Miami/Jeddah/Singapore x2/Madrid-at-300s/Shanghai x2 builds) into a foundation spec under test:scenery or test:circuit, leaving the hooks contract here -- doubly urgent because new-hooks.spec.js is listed in BOTH test:api and test:hooks in package.json, so today the entire heavy foundation half runs twice per full sweep (api.log shows even trivial hook tests costing 15-77 s each).",
   "weakSpots": "Hooks half: lines 33-51 and 198-213 are typeof-only shape checks (catch removal, not wrong values); line 292's 'some frac near 0.8' could coincidentally pass on a 22-car grid. Foundation half is actually the stronger set (specific measured bounds with re-measurement provenance at lines 446-472, 733-744, 804-819) but has no business in a hooks-contract group; also imports @playwright/test directly rather than ./fixtures.js, so it skips the pageErrors fixture the rest of the suite gets."
  },
  {
   "file": "tests/specs/obs-act-edge.spec.js",
   "purpose": "Failing means the headless RL control loop breaks at the edges: act(n=0) semantics, reset at the lap seam, scan finiteness/wrap at s ~= total, done-flag lifecycle, and NaN sweeps around the full lap on four circuits.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/headless-api.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Genuine boundary coverage the happy-path headless-api.spec.js does not attempt (seam fractions to 0.9995, wrong-way done semantics, 20-position NaN sweep), and the seam bugs it hunts are exactly the silent-corruption class worth paying browser cost for; the monaco/suzuka/spa parametrised seam tests earn their builds.",
   "weakSpots": "Lines 71-73: +/-50 m window around expected s is loose for a placement assertion; lines 32/43 toBeCloseTo(precision 1) tolerates 0.05 m drift under 'did not advance' (adequate); line 16's waitForTimeout(100) is sleep-based settling; like new-hooks it imports @playwright/test directly and skips the fixtures.js pageErrors net."
  },
  {
   "file": "tests/specs/offtrack.spec.js",
   "purpose": "Off-track/reverse/wrong-way/auto-rescue behaviour and the prog<->s coupling; failing means progress can be cheated, rescue is broken, or grass no longer slows the car.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/physics-fixes.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Exemplary: pace pinned with a written rationale, relative assertions (onSpeed > offSpeed+2), both branches of the rescue contract tested (throttle held vs released); 8 tests each rebuild a circuit so it is heavy but every test earns it.",
   "weakSpots": "None material; absolute metre/m-s thresholds are justified by the pinPace header comment."
  },
  {
   "file": "tests/specs/output-paths.spec.js",
   "purpose": "The gallery path helper's contract (port-scoped dirs, traversal rejection, stable per-suite dirs); failing means every screenshot-producing spec writes to the wrong or an unsafe place.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Pure-node logic inside the Playwright runner (no page), exact-path equality plus throw assertions; owns its own test:paths group and costs nothing.",
   "weakSpots": "None."
  },
  {
   "file": "tests/specs/parts-budget.spec.js",
   "purpose": "The 600cr budget system UI: display, decrement, over-budget gating, and the persisted FREE BUILD toggle; failing means players can over-spend or the budget readout lies.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/parts-persistence.spec.js",
    "tests/specs/parts-physics.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "14 real UI assertions (exact 440 remainder, over class, scaleX fill, persistence across reload) with screenshots as supplements, not substitutes; the cost math itself is double-covered by parts-physics getCost tests, which is acceptable layering.",
   "weakSpots": "Lines 40-41: expect(text).toContain(\"600\") twice \u2014 the second is a copy-paste no-op (presumably meant to check spent vs total separately); parts-persistence line 137-150 re-asserts the same 440-after-reload."
  },
  {
   "file": "tests/specs/parts-catalog.spec.js",
   "purpose": "Setup-panel catalog rendering: all 12 category tabs, option visibility/activation, cost badges, and factory/supplier/signature gating per team; failing means the garage offers wrong or missing parts.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/parts-setup-ids.spec.js",
    "tests/specs/parts-physics.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Real visibility/class/count assertions incl. negative gating (manu_mercedes absent for Red Bull Ford, sig_mclaren_flex absent for Mercedes); the one vacuous test could be dropped into a gallery suite but does not justify churn.",
   "weakSpots": "Lines 233-238 'portrait setup screenshot' has zero expects \u2014 screenshot-only; option-activation tests (lines 103-123) duplicate parts-setup-ids line 55-63 (same high_octane click-makes-active check)."
  },
  {
   "file": "tests/specs/parts-ers.spec.js",
   "purpose": "ERS BOOST must both push and cost at every speed (the taper-reaches-zero + drain-gated-on-deploy>0 regression); failing means BOOST is silently inert or free somewhere in the speed range.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/parts-physics.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Pins both halves of a documented real bug with a 5-speed sweep and a no-boost control run; parts-physics's final ERS test covers per-part scaling, this covers the taper/drain invariant \u2014 complementary, not redundant, though co-locating them is a reasonable future merge target.",
   "weakSpots": "Drain thresholds (>0.05 per 1.5 s) are magic but generous enough to be robust; 4 tests each rebuild monza \u2014 the 4 sweeps could share one page load."
  },
  {
   "file": "tests/specs/parts-factory-presets.spec.js",
   "purpose": "AI car meshes must be built from deterministic team factory presets, never the player's saved setup; failing means the whole grid renders wearing the player's parts.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/parts-physics.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Instruments Car3D.build to capture what was actually rendered for all 11 teams and proves the saved extreme aero did NOT reach the AI mesh \u2014 tests the render path where parts-physics only tests the catalog side.",
   "weakSpots": "None; the poisoned-save setup (saved aero=extreme, factory differs) makes the negative assertion real."
  },
  {
   "file": "tests/specs/parts-liveries.spec.js",
   "purpose": "Livery catalog well-formedness and the finish (gloss/satin/chrome) surface-remap semantics plus the creator round-trip; failing means malformed liveries ship or a finish repaints the wrong faces.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/parts-livery-contrast.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "The remap tests are exactly right (only paint faces move, geometry/colours untouched, chrome->mirror with the rationale written down) and the creator UI round-trip lands in localStorage with the correct absent-finish encoding.",
   "weakSpots": "None material; count>=30 per team (line 65) is a roster floor like coplanar-faces, acceptable."
  },
  {
   "file": "tests/specs/parts-livery-contrast.spec.js",
   "purpose": "Sponsor/crest ink legibility: every livery must be readable on the panel it actually sits on (the white-on-white family of bugs); failing means an invisible wordmark shipped or a decal region moved off its paint.",
   "assertionQuality": "mixed",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/parts-liveries.spec.js"
   ],
   "verdict": "strengthen",
   "verdictDetail": "The geometry probe (decal->backing-surface via sentinel colours) is genuinely strong and catches region/paint drift, but the ink-choice half never calls buildAtlas \u2014 assert against the atlas's real lum/ink helpers (or export them) so a production linearisation regression fails here instead of only in a human's eyes.",
   "weakSpots": "Test 1 (lines 29-40) asserts properties of the spec's OWN inline CONTRAST_FN, not production code \u2014 it cannot catch buildAtlas reverting to gamma-space lum; test 2's inkOn (lines 47-51) re-implements the picker, so it proves legibility is ACHIEVABLE, not that the atlas achieves it; INKED_FOR (lines 240-254) is a hand-maintained mirror of buildAtlas rather than read from it."
  },
  {
   "file": "tests/specs/parts-mesh-cache.spec.js",
   "purpose": "Bounded GPU mesh caches (body/cockpit <=3 keys, wheels <=8 pairs, decal cache <=24) with real eviction and no double-free; failing means a garage session leaks GPU meshes until the tab dies.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/parts-physics.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Instruments real GLX create/free and asserts oldest-evicted/newest-live plus no duplicate frees \u2014 exactly the leak class it exists for; the 240/360 s timeouts and 13 race start/quit cycles are the honest price of exercising the real eviction path.",
   "weakSpots": "Wheel-centre test (lines 400-401) pins y~=0.34 absolute, but that constant is co-asserted as Car3D.AXLES in parts-physics so drift is caught coherently."
  },
  {
   "file": "tests/specs/parts-persistence.spec.js",
   "purpose": "Part selections persist to apex26.parts.<teamId>, survive reload, and are team-isolated; failing means setups silently reset or bleed between teams.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/parts-budget.spec.js",
    "tests/specs/parts-catalog.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Asserts the actual storage payload, the post-reload active class, and cross-team isolation \u2014 each a distinct failure mode; the 440-budget-after-reload test duplicates parts-budget's number but from the persistence side, tolerable.",
   "weakSpots": "None material."
  },
  {
   "file": "tests/specs/parts-physics.spec.js",
   "purpose": "A 69-test omnibus: catalog structure, getMods/getCost/resolveSetup/statMult, the visual-recipe registry, ~40 Car3D/CarMesh geometry contracts, shader parity strings, signatures/factory presets, and ERS battery axes; failing means anything from a wrong price to a detached rear wing.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/parts-ers.spec.js",
    "tests/specs/parts-factory-presets.spec.js",
    "tests/specs/parts-budget.spec.js",
    "tests/specs/parts-mesh-cache.spec.js"
   ],
   "verdict": "split",
   "verdictDetail": "The assertions are individually excellent (mesh-signature uniqueness, per-knob deform probes, exact endplate containment) but 1481 lines under a name that says 'physics' while ~70% is geometry/visual-recipe contracts: split the Car3D/CarMesh geometry tests into parts-geometry.spec.js, move the final ERS-battery test (lines 1415-1481, which reloads and rebuilds monza 3 times) into parts-ers.spec.js, and keep catalog/mods/cost/resolve here \u2014 each test does its own page.goto so 69 boots is also where the runtime goes.",
   "weakSpots": "Lines 69-77 'near-1.0 multipliers' only asserts >0.9 (loose); lines 1090-1104 assert shader source by literal string-containment \u2014 an intentional parity pin but brittle to whitespace; the name misleads pick-tests toward driving-model relevance."
  },
  {
   "file": "tests/specs/parts-setup-ids.spec.js",
   "purpose": "Stable data-cs-cat/data-cs-opt DOM contract, the no-screen-shows-through layering rules, the TEAM tab flows, and the garage preview orbit camera; failing means every parts spec's selectors break or the transparent garage shows the menu through it.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/parts-catalog.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Goes beyond attribute checks to hit-testing z-order (elementFromPoint) and full camera semantics (pan is not zoom, dolly perpendicular to strafe, clamps under a 70-click mash) \u2014 each assertion maps to a named shipped bug; the driver-chip test.skip on single-driver teams is a legitimate guard, not a wrapped expect.",
   "weakSpots": "Line 55-63 duplicates parts-catalog's click-makes-active check for the same high_octane option."
  },
  {
   "file": "tests/unit/perf-governor.test.mjs",
   "purpose": "The adaptive governor must not chase an externally capped clock (30 fps Low Power Mode) to the quality floor, and must revert steps that did not help; failing means capped devices play in needless safe-mode.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [
    "tests/unit/perf-sentinel.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "Runs the REAL tick() via eval of js/game/perf.js against carefully distinguished dt signals (cap vs coupled GPU load vs jitter) and pins the mechanism (floorMs convergence) as well as the outcome; pure node, ms-cheap, correctly in test:tooling-fast.",
   "weakSpots": "None."
  },
  {
   "file": "tests/unit/perf-sentinel.test.mjs",
   "purpose": "Crash strikes expire with the build that earned them (the one-way safe-mode door); failing means devices stay pinned in broken-looking safe mode after the offending build is fixed.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [
    "tests/unit/perf-governor.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "Covers the full matrix \u2014 strike accrual, floor pinning, new-build expiry, mid-race update not a crash, same-build accumulation, manual clear, desktop exemption \u2014 each against real perf.js state transitions; in test:tooling-fast where it belongs.",
   "weakSpots": "None."
  },
  {
   "file": "tests/specs/persistence.spec.js",
   "purpose": "A failing localStorage write (iOS Private Browsing zero quota) must be reported (persistState ok:false, named exception, one loud warning) while the session keeps working from cache; failing means careers vanish silently again.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Injects the real failure mode via addInitScript before boot, asserts through the player-visible surface (real settings, the log ring, console dedup) rather than module internals, and explicitly guards the cache-write half that made the original bug silent; correctly in test:api.",
   "weakSpots": "None; api.log shows 13-22 s per test, dominated by page boot, acceptable."
  },
  {
   "file": "tests/physics-abudhabi-foundation.spec.js",
   "purpose": "Abu Dhabi runtime build contract: elevation profile applied within bounds, Yas Hotel gridshell/bridge spans emitted with >=4.8 m clearance and supported, water present, walls sane, terrain under road \u2014 at day and night; failing means the circuit shipped visibly broken.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/props-over-road.spec.js",
    "tests/specs/terrain-over-road.spec.js",
    "tests/specs/tracks-walls.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "The tests are good, but this file (like its 14 siblings) contains zero driving-model physics \u2014 the physics- prefix exists only to be caught by test:physics's glob, so every driving-model change pays 15 circuit builds; rename to abudhabi-foundation.spec.js and give the foundations their own test:foundation group (TESTING.md documents today's grouping, so update both together).",
   "weakSpots": "The day/night loop (lines 38-124) reruns the ENTIRE contract twice though only emission can differ by tod \u2014 bahrain's pattern (cheap night recheck of models/geometry only) would halve the cost."
  },
  {
   "file": "tests/physics-albert-park-foundation.spec.js",
   "purpose": "Albert Park runtime build: both lake fountains and >=8 lake water models emitted, water geometry valid, spans clear, terrain grounded; failing means the lake or its landmarks vanished from the build.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/unit/albert-park-foundation.test.mjs",
    "tests/specs/terrain-over-road.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "Same regroup/rename as the other foundations (no physics here, glob-named); compact and complementary to its no-browser twin albert-park-foundation.test.mjs which covers the pure-node side.",
   "weakSpots": "Line 49 walls.minOverHw > -1.5 tolerates barriers 1.5 m inside the road half-width \u2014 far looser than bahrain's >=0.8 floor; worth tightening or documenting why Albert Park needs the slack."
  },
  {
   "file": "tests/physics-bahrain-foundation.spec.js",
   "purpose": "Bahrain runtime build: props/glass triangles never overhang the racing surface by >0.2 m (full 1200-node scan), bounded T8/T9 relief, exact required-model roster, night rebuild clean; failing means props sit on the track or required scenery silently vanished.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/props-over-road.spec.js",
    "tests/specs/terrain-over-road.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "Same regroup/rename as the other foundations; its exact (not subset) required-model list with the written justification is the pattern the other circuit specs should copy, and its cheap night recheck is the right way to cover the second tod.",
   "weakSpots": "None material; the in-page O(samples x triangles) scan is expensive but grid-bucketed and inside a deliberate 300 s budget."
  },
  {
   "file": "tests/specs/physics-characterization.spec.js",
   "purpose": "Characterization gate on the driving model: four scripted scenarios must reproduce the committed physics-baseline.json numbers exactly (1e-4); failing means the physics changed \u2014 intentionally or not \u2014 and the diff names where.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/agent-determinism.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "The single most valuable spec for the extraction work game.js is undergoing: baseline is committed (verified present) so the gate is live, header documents the verified-non-vacuous check (LAT_MAX 22->27 fails by name), and one build + reset() keeps it ~25 s; the agent-determinism overlap is explicitly distinguished in the header (within-session vs across-commits).",
   "weakSpots": "The obvious hazard \u2014 regenerating to silence a red run \u2014 is procedural, not a test defect, and the header addresses it head-on."
  },
  {
   "file": "tests/physics-cota-foundation.spec.js",
   "purpose": "COTA shared-foundation migration: Big Red's 15-22 m crest at frac 0.08-0.14, corridor-focused props-over-road scan, required hero/gantry models at day and night, walls sane; failing means COTA's signature terrain or landmarks regressed.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/props-over-road.spec.js",
    "tests/specs/terrain-over-road.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "Same regroup/rename as the other foundations; the double-bounded elevation range with peak location is exactly the shape-not-absence contract the abudhabi header argues for.",
   "weakSpots": "None material; the embedded scan is explicitly documented as the focused form of props-over-road.spec.js."
  },
  {
   "file": "tests/specs/physics-fixes.spec.js",
   "purpose": "Two named physics regressions: world->s projection must not teleport lap distance onto the other leg of a Monaco hairpin, and wall scrub must scale with entry angle; failing reintroduces lap-distance teleports or the flat-stop wall.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [
    "tests/specs/offtrack.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Textbook regression spec: worst-case geometry chosen deliberately (Monaco hairpins), seam-unwrapped continuous monitoring over ~25 s of driving, and a purely relative gentle-vs-hard comparison for the wall; correctly in both test:behaviour and the physics glob.",
   "weakSpots": "The <5 m back-jump bound is loose but defensibly so (real teleports are tens of metres, wobble is <2 m, per the comment)."
  },
  {
   "file": "tests/physics-hungaroring-foundation.spec.js",
   "purpose": "Hungaroring's track-owned foundation contract: racing coordinates, hero models emitted, valley/crest elevation signature, terrain-gap probes, sane walls \u2014 failing means the circuit build regressed.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tracks-walls.spec.js",
    "terrain-over-road.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "Keep the content verbatim, but this proves scenery/terrain/barrier contracts, not the driving model \u2014 the physics-* name puts it in test:physics (run on driving-model edits that cannot break it) while pick-tests routes js/circuits/ edits to circuit+scenery, which never run it; a foundations/track group fixes both directions.",
   "weakSpots": "L59-61 gap probes accept null (missing terrain silently passes, unlike Spa/Monza which assert terrainY not-null); wall thresholds L67-69 are argued in a comment but still judgment calls."
  },
  {
   "file": "tests/physics-imola-foundation.spec.js",
   "purpose": "Imola's foundation: exact dressing exclusions, five named-corner elevation signature (Piratella/Acque Minerali/Rivazza), 480-point shoulder-gap sweep, Senna memorial + Santerno water models, plus a night rebuild \u2014 failing means Imola's build or its authored relief regressed.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tracks-walls.spec.js",
    "terrain-over-road.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "One of the best of the family (uses fixtures' racePage/pageErrors, asserts non-null via shoulderGaps filtering, named-corner elevations); same family-wide misgrouping \u2014 foundation contract living in test:physics via the filename glob.",
   "weakSpots": "L61 tightFrac > 0.3 is a loose floor; 420 s timeout makes it the heaviest single test in the family."
  },
  {
   "file": "tests/physics-interlagos-migration.spec.js",
   "purpose": "Interlagos' migration to the track-owned foundation: 40-46 m relief with the Senna-S drop and box climb, hero models + consolidated reservoir water, a props vertex ratchet, and grounded terrain/finite walls.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tracks-walls.spec.js",
    "terrain-over-road.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "Same family regroup; also the only file split into 4 tests that each reload and rebuild the circuit \u2014 collapsing to one build (as the other circuits do) would roughly quarter its cost.",
   "weakSpots": "L79 vertex ratchet's own comment admits a scenery DROP is invisible to it; L102 minOverHw > -1.5 tolerates barriers 1.5 m inside the road half-width."
  },
  {
   "file": "tests/physics-monaco-foundation.spec.js",
   "purpose": "Monaco's foundation day+night: harbour water pinned by projected AREA (not tessellation), tunnel-roof count, overhead clearance >= 4.8 m, required-only suppression rule, elevation signature and tight street walls; a second test re-runs a full prop-over-road corridor scan capped at 1.4 m.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "props-over-road.spec.js",
    "tracks-walls.spec.js",
    "terrain-over-road.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "Regroup with the family, and note the second test (L140-220) is a hand-copied duplicate of props-over-road.spec.js's scan whose 1.4 cap is identical to that file's BASELINE.monaco \u2014 redundant protection that could be dropped or kept only as fast per-circuit feedback.",
   "weakSpots": "Second test duplicates ~80 lines of the all-circuit audit; its 1.4 m cap can never disagree with props-over-road.spec.js:52, so one of the two is dead weight."
  },
  {
   "file": "tests/physics-montreal-foundation.spec.js",
   "purpose": "Montreal's island foundation day+night: flat-terrain contract (<= 1.3 m relief), casino footbridge with two physically grounded support pillars probed vertex-by-vertex, river water present, walls tight for a street-style circuit.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tracks-walls.spec.js",
    "terrain-over-road.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "The support-pillar probe (L96-102) is a genuinely clever grounded-geometry assertion; same family-wide misgrouping out of test:physics.",
   "weakSpots": "L71 dressingExclusions toHaveLength(7) is a brittle count pin that fails any legitimate exclusion edit; L109-110 gap probes accept null; only 4 ground probes at 2 fracs is sparse next to siblings."
  },
  {
   "file": "tests/physics-monza-foundation.spec.js",
   "purpose": "Monza's foundation: Roggia dip / Lesmo crest, Sopraelevata banking models + flyover clearance, and bank-corrected terrain-over-road edge probes measured against the ROAD SURFACE (not the centreline) \u2014 failing means geometry or the banking reference regressed.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tracks-walls.spec.js",
    "terrain-over-road.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "The banking-corrected probe (L50-61, with explicit not-null at L99) is the model the null-tolerant siblings should copy; regroup with the family out of test:physics.",
   "weakSpots": "None material \u2014 thresholds are measured and documented in-file."
  },
  {
   "file": "tests/physics-qatar-foundation.spec.js",
   "purpose": "Qatar's foundation night+day: flat-by-GRADIENT contract (max |dy/ds| < 3.5% with measured rationale), the leanest per-circuit props vertex cap in the suite (310k), hero models, and day/night lighting deltas (stars, floodlights, ambient).",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tracks-walls.spec.js",
    "terrain-over-road.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "Exemplary threshold hygiene (every number carries its measurement); same family regroup \u2014 and its own comments note the values were measured via the headless verify-track harness, evidence this spec could run browser-free like albert-park-foundation.test.mjs.",
   "weakSpots": "day.walls is collected but never asserted (only night's, L120-122); L123 gap probes accept null."
  },
  {
   "file": "tests/physics-redbull-foundation.spec.js",
   "purpose": "Red Bull Ring's alpine foundation day+night: 55-65 m relief with peak/low in the right fractions, slope cap, hero models (bull plaza, wing), near-total barrier coverage \u2014 failing means the alpine build regressed.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tracks-walls.spec.js",
    "terrain-over-road.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "Solid family member; regroup out of test:physics with the rest.",
   "weakSpots": "L57 waitForTimeout(100) is a dead wait after evaluate already returned; L83-85 gap probes accept null; only two hero models named."
  },
  {
   "file": "tests/physics-spa-foundation.spec.js",
   "purpose": "Spa's foundation: the 100-104 m relief and Raidillon climb, explicitly non-null terrain feet at named landmarks, a bank-corrected road/terrain/props corridor scan, and day+night landmark/overhead-clearance checks.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "terrain-over-road.spec.js",
    "props-over-road.spec.js",
    "tracks-walls.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "Regroup with the family; test 3 (L52-147) is a per-circuit copy of the all-circuit corridor scans \u2014 its own comment (L73-74) admits terrain-over-road.spec.js now does the same thing with the banking fix, so that test is a merge candidate into the shared audit.",
   "weakSpots": "Test 3 duplicates ~90 lines of scan machinery already living in terrain-over-road/props-over-road; day/night loop doubles the build cost for assertions that only need the model diagnostics."
  },
  {
   "file": "tests/physics-suzuka-foundation.spec.js",
   "purpose": "Suzuka's foundation: the figure-8 crossover bridge (raw-def pin plus the source-to-racing s-transform 0.4298 -> 0.8173), esses/Degner/crossover relief, eleven required models including both footbridge tower pairs.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tracks-walls.spec.js",
    "terrain-over-road.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "The transform and relief checks are the real value; regroup with the family \u2014 and consider dropping the verbatim raw-def pins, which are change-detectors of authored data that already broke once on a deliberate retune (the L50-63 comment is the scar).",
   "weakSpots": "L46-63 pins the exact elevations/bridges arrays from js/circuits/suzuka.js \u2014 restating source data, fails every intentional retune; L71-73 gap probes accept null."
  },
  {
   "file": "tests/physics-vegas-foundation.spec.js",
   "purpose": "Vegas' foundation: Sphere-sector dip, both renamed Bellagio lake basins typed as water, overhead spans >= 4.8 m clearance, near-total street walls and night lighting present \u2014 failing means the street-circuit build regressed.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "tracks-walls.spec.js",
    "terrain-over-road.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "The lake-rename comment (L65-74) is model documentation of why both new ids are pinned; regroup with the family out of test:physics.",
   "weakSpots": "L96 waitForTimeout(300) arbitrary settle; L109-110 gap probes accept null; walls test samples ground at only one frac (0.35)."
  },
  {
   "file": "tests/physics-zandvoort-foundation.spec.js",
   "purpose": "Zandvoort's foundation: exact four-model required list (justified in-file), a full prop/terrain corridor-clearance scan, walls, and a night rebuild \u2014 failing means the dune-circuit build or its required-model set changed.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "props-over-road.spec.js",
    "terrain-over-road.spec.js",
    "tracks-walls.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "Regroup with the family; its propClearance helper (L16-183, ~170 lines) is a third hand-copied instance of the props-over-road scan (after Monaco's and Spa's) asserting the same TOL the all-circuit audit already enforces for Zandvoort \u2014 extract to track-helpers.js or drop.",
   "weakSpots": "L13 waitForTimeout(1200) arbitrary settle; the clearance scan duplicates the shared audit verbatim, so its passing adds nothing props-over-road.spec.js does not already guarantee."
  },
  {
   "file": "tests/specs/presets.spec.js",
   "purpose": "The RELAX/STANDARD/PRO preset bundles: relative ordering on every feel axis (survives retunes), active-state marking, persistence, and manual-edit-drops-to-custom \u2014 failing means presets stopped forming a coherent ladder or stopped persisting.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Textbook relative assertions (the header documents the hardcoded-constants version this replaced); correctly grouped in test:steering.",
   "weakSpots": "None \u2014 ordering-based by design."
  },
  {
   "file": "tests/specs/projection.spec.js",
   "purpose": "Tracks.project() is the faithful inverse of the (s, lat) -> world mapping, with error bounds derived from node spacing and the +right sign convention \u2014 failing means the world-space physics migration's coordinate bridge broke.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Genuinely a physics-group spec (unlike the foundation family) with justified tolerances; the only nit is that all three tests stage a race through four UI clicks when __apex.race()+go() (or the loadTrack fixture) would do, and 84 sequential page.evaluate round-trips in test 1 could be one evaluate.",
   "weakSpots": "startLiveRace's UI walk is slow ceremony unrelated to what is asserted; per-point evaluate calls add avoidable wall time."
  },
  {
   "file": "tests/unit/prop-clipping.test.mjs",
   "purpose": "Ratchet on prop-vs-prop interpenetration across all 40 circuits (the third axis after road and ground guards): a new circuit or new defect reads 0-cap and fails, growth over baseline fails, and a stale over-generous baseline fails.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Model ratchet: roster-derived count floor (L65-67 \u2014 explicitly fixing the frozen >=24 mistake coplanar-faces still has), shared baseline JSON with the tool's own --gate so they cannot disagree, and an anti-slack test; correctly in test:sweeps as pure Node (~90 s cached across both tests).",
   "weakSpots": "The Silverstone cap knowingly carries a detector limitation (documented L39-49) \u2014 acceptable, but it means same-assembly crowdMound growth under that cap is invisible."
  },
  {
   "file": "tests/specs/props-over-road.spec.js",
   "purpose": "All-circuit audit that no prop/glass triangle sits 0.2-5 m above the racing line, with per-circuit caps documenting justified overheads \u2014 failing means scenery intruded into the racing space somewhere on the fleet.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [
    "prop-clipping.test.mjs",
    "terrain-over-road.spec.js"
   ],
   "verdict": "strengthen",
   "verdictDetail": "The main audit is strong and irreplaceable (observed 616 s, budget raised to 1500 s for good reason), but the standalone 'Shanghai uses the default clean prop-clearance baseline' test (L57-59) only asserts a literal in this same file and can never catch a runtime regression \u2014 the real Shanghai check already runs inline at L144-145; delete the vacuous one.",
   "weakSpots": "L57-59 vacuous self-referential constant test; L47 miami_note as a string entry inside a numeric BASELINE map is a fragile convention (harmless today because lookups are by track id)."
  },
  {
   "file": "tests/specs/quali.spec.js",
   "purpose": "The full qualifying feature: session staging, deterministic sim model, grid = classification car-for-car, no leakage into GPs or later rounds, standing-start flying-lap rules, BACK semantics, and the one-off QUALIFYING LAP setting \u2014 failing means a real player-facing quali regression.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Every test encodes a specific historical bug (orphaned car references, round-1 grid reuse, quali order leaking into GP) in its comments; correctly in test:modes/test:career, with test.slow() on the two double-circuit tests.",
   "weakSpots": "L66-68 spread bounds (0.4-12 s) are deliberately loose; nothing vacuous."
  },
  {
   "file": "tests/unit/quick-validate.test.mjs",
   "purpose": "Unit-tests the pure helpers of tools/quick-validate.mjs: failure-message mapping and that a false camera() result reads as failed while an object reads as success \u2014 failing means the refactor gate would mislabel probe outcomes.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Small and correctly scoped (the tool lazily imports playwright precisely so this can stay pure-node in test:tooling-fast); coverage is narrow \u2014 the obs/light validity branches of evaluateLiveProbe are untested \u2014 but the cams-false case it does pin is the one that historically lies.",
   "weakSpots": "No test for probeFailures' error-truncation (slice(0,5)) or for obs/light being marked false on non-finite values \u2014 cheap additions if the tool ever churns."
  },
  {
   "file": "tests/specs/race-control.spec.js",
   "purpose": "The browser-only slice of race control: the layer is on by default with a coherent GREEN, the enabled flag survives a real localStorage round trip (guarding the '0'-parses-truthy storage-format bug), and per-sector hazards are reportable.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "A deliberate, documented complement to race-control.test.mjs (the header records why the injected-host-flag Playwright approach was wrong), keeping only what needs a real page; correctly in test:debris.",
   "weakSpots": "None \u2014 each test states why it cannot live in the VM suite."
  },
  {
   "file": "tests/unit/race-control.test.mjs",
   "purpose": "The caution state machine in a VM: thresholds (worst-sector YELLOW vs total VSC/SC), raise-fast/hold-before-lower hysteresis, drop-on-disable, both storage formats, guest-adopts-host-verbatim, broadcast-on-change-only, leader's-lap OVERTAKE gating, inert states.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Exactly the extraction payoff its header describes \u2014 millisecond-fast, direct-drive hazard pictures; correctly in test:tooling-fast.",
   "weakSpots": "L116-126 'a stuck hazard cannot neutralise the race forever' never exercises the 90 s cap it names \u2014 it asserts the flag STAYS at 95 s under a persistent hazard; the cap-expiry path (flag dropping after the cap once the picture clears) is untested anywhere in the file."
  },
  {
   "file": "tests/unit/road-under-floor.test.mjs",
   "purpose": "Fleet-wide invariant that the flat world-floor slab sits with real daylight below the lowest upward-facing road vertex \u2014 failing means a banked corner's low edge is buried under the floor (the invisible-to-everything-else Zandvoort bug).",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Targets a defect class provably invisible to every other guard (documented L10-17), filters the downward skirt by face normal, and manages memory across the 40-circuit VM sweep with env.trim(); correctly in test:sweeps.",
   "weakSpots": "The -0.05 daylight epsilon is a judgment call, but a z-fighting slab is exactly what it exists to forbid."
  },
  {
   "file": "tests/unit/scenery-api-contract.test.mjs",
   "purpose": "Freezes the 107-member scenery(api) surface handed to every circuit's scenery callback; failure means a buildProps refactor dropped/renamed/added a member that 24 circuit files depend on.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "weakSpots": "Third test (line 76) silently passes with `if (!def) return` when no reversed/source-coordinate circuit exists \u2014 a conditional no-op today only because such circuits do ship; if they were all removed the wrapped-api path would go untested with no signal.",
   "overlaps": [
    "tests/specs/scenery-kits.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Exactly the right shape for a contract freeze \u2014 sorted-list + count tripwire + real build diff of Object.keys, done headlessly in seconds; the landmarkKit/circuitKit membership overlap with scenery-kits.spec.js is one level up and harmless."
  },
  {
   "file": "tests/specs/scenery-kits.spec.js",
   "purpose": "Proves the shared landmark/circuit kits are bound into the scenery api with the right themes, and that five representative circuits actually emit validated kit facilities with clean diagnostics; failure means kit wiring or a track's kit usage broke.",
   "assertionQuality": "mixed",
   "costTier": "heavy",
   "weakSpots": "The 5-track parameterized loop (lines 65-94) passes themeName into page.evaluate (line 82) but destructures only [id] and NEVER asserts the theme \u2014 the test titled 'emits validated street kit facilities' never checks the theme is street. emitted.length >= 2 (line 86) is a low floor for 'facilities'. Vertex bound uses `!entry.vertices || entry.vertices <= 50000` (line 88) so a missing vertices field vacuously passes.",
   "overlaps": [
    "tests/unit/scenery-kits.test.mjs",
    "tests/unit/scenery-api-contract.test.mjs"
   ],
   "verdict": "strengthen",
   "verdictDetail": "The binding-contract half is good; make the per-track tests earn their titles by asserting the resolved theme name (one extra field in the evaluate return) and consider whether five full circuit builds are needed when the kit logic itself is unit-covered in scenery-kits.test.mjs."
  },
  {
   "file": "tests/unit/scenery-kits.test.mjs",
   "purpose": "Unit-tests SceneryThemes resolution and the LandmarkKit/CircuitKit helpers in a node VM: emission bounds, spec validation, failure propagation, fail-closed deps; failure means a kit helper regressed its validation or emission contract.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "weakSpots": "The worst-case emission cap pins an exact 58 (line 179), which will churn on any intentional geometry change \u2014 a bound would be less brittle; otherwise the rejection matrices and failure-propagation cases are unusually thorough.",
   "overlaps": [
    "tests/specs/scenery-kits.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Dense, cheap, and every assertion maps to a real defect class (unbounded emission, NaN geometry, throwing on bad deps); the method-list overlap with the spec is the unit/integration split working as intended."
  },
  {
   "file": "tests/specs/season.spec.js",
   "purpose": "Season mode end-to-end: seasonMode flag, races not ending at the quick-race 360 s cap, standings button/panel after round 1, and the legacy-points-to-stable-identity migration; failure means season progression or points identity broke.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "weakSpots": "The 10/25/57-lap trio (lines 55-66) runs three full menu-flow boots to prove one lap-count-independent mechanism \u2014 one or two would do. Fixed waitForTimeout sleeps (lines 79, 81, 88, 99, 102) are flake-prone under SwiftShader. rows count > 0 (line 111) merely restates the first().toBeVisible() on line 109.",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "The migration test alone (custom:0 gets 50 pts, NEW never appears in the store but does on the results table) justifies the file; trimming the 3-lap parameterization to one case would cut two full heavy boots for zero coverage loss."
  },
  {
   "file": "tests/unit/service-worker.test.mjs",
   "purpose": "VM-harnessed sw.js lifecycle semantics: install fails on essential-asset failure, tolerates optional failures, cache writes gate responses/waitUntil, and activation only deletes old caches after a complete install; failure means the PWA could serve a broken or half-written cache.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "weakSpots": "None material \u2014 the deferred-put technique genuinely proves ordering (responses pending until writes land) rather than just outcomes; the fake caches API is faithful enough for what is asserted.",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "This is the only test of sw.js's actual behavior (load-order only checks the precache seed list), it tests race-condition ordering that a browser test could not do deterministically, and it costs milliseconds."
  },
  {
   "file": "tests/unit/shared-track-foundation-characterization.test.cjs",
   "purpose": "Characterizes the shared track foundation across every circuit: startFrac/reverse point transforms, anchor() grounding profiles (the 0.3 m sink + bank fallback), recordBarrier full-lap semantics, place() suppression vertex deltas, and all-40-track mesh finiteness plus the 24/16 roster split; failure means the shared foundation refactor changed pinned behavior.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "weakSpots": "BANK_BOUND = 3 m (line 226) is deliberately generous \u2014 a half-metre grounding regression in the raycast-miss fallback would pass; documented and accepted in-file. The suppression test's exact +24/+36 (lines 313-314) is properly tight after the summing fix described at line 277.",
   "overlaps": [
    "tests/unit/coplanar-faces.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "Runs at node speed (~2m40s, no browser) yet covers every circuit; the roster assertions (LIST 40, SEASON 24, no classic in SEASON, lines 353-355) overlap coplanar-faces' >=24 floor but here they are exact, which is the version that catches a classic leaking into the championship."
  },
  {
   "file": "tests/unit/silent-catch.test.mjs",
   "purpose": "Ratchet on bare `catch (e) {}` blocks in js/ (ceiling 167) plus an anti-slack check that the ceiling stays within 15 of reality; failure means silent-swallow catches are multiplying or the ratchet stopped ratcheting.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "weakSpots": "The brace-matching parser (lines 56-73) can miscount if a catch body contains an unbalanced brace inside a string literal \u2014 unlikely in this codebase and an off-by-one would not defeat the ratchet's purpose. The comment escape hatch is a design choice, argued convincingly in the header.",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "A well-designed ratchet: two-sided (ceiling AND slack), aimed at the codebase's documented most-repeated defect shape, with an actionable failure message naming the worst files."
  },
  {
   "file": "tests/specs/sliders.spec.js",
   "purpose": "Every steering pause-menu slider is wired (direction, label, persistence) AND genuinely changes car behavior \u2014 including the three PACE-envelope regressions (dial\u2192gear mapping, full-gearbox reach, manual-gearbox limiter) and the simplified-control fan-out; failure means a slider is dead or PACE re-broke the speed envelope.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "weakSpots": "Label assertion is only loLabel !== hiLabel (line 62) \u2014 a label showing the wrong value but changing would pass. Behavior multipliers (1.2x line 86, 1.15x line 98, +5 m/s lines 136/154) are somewhat arbitrary but relative and documented. The tilt end-to-end gap is explicitly declared un-runnable headless (lines 293-299) rather than faked.",
   "overlaps": [
    "tests/specs/steering.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "One of the best-annotated specs in the suite \u2014 each behavior test names the exact bug it pins (traffic-pinned measurement, coast-drag decay, the 73.5 m/s manual clamp) and asserts through the real sim; the racing-line overlap with steering.spec.js is wiring-vs-physics, not duplication."
  },
  {
   "file": "tests/specs/smoke.spec.js",
   "purpose": "The boot gate: page loads without WebGL/console errors, the menu-to-race flow works, __apex is live, rendering produces a non-blank frame, and the HUD updates; failure means the game fundamentally does not boot or race.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "weakSpots": "'minimap canvas has content' (lines 212-219) only checks boundingBox width > 0 \u2014 layout, not content; a never-painted minimap passes. HUD speed asserts only > 0 (line 209) not \u2248288 for an 80 m/s jump. The >5000-byte screenshot heuristic (lines 162, 175) is deliberate and documented as a non-blank check, not pixel regression.",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "This is test:tiny's backbone and per artifacts/logs runs 13-128 s per test under SwiftShader; strengthen the minimap test to sample pixels (the telemetry-compare spec already has the getImageData pattern) next time it is touched."
  },
  {
   "file": "tests/unit/span-kinds.test.mjs",
   "purpose": "Keeps agent-view.spec.js's SPAN_KINDS list bidirectionally in sync with every noteSpan() call site in js/track; failure means the agent-view spec would either reject a real span kind (circuit-dependent flake) or list fiction.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "weakSpots": "Source-scrape by regex (line 39) misses a noteSpan call whose kind is a variable rather than a string literal \u2014 currently none exist, and the >=4 floor (line 55) would catch the scrape going completely blind.",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "A deterministic, free guard replacing a probabilistic browser failure whose message pointed away from the cause \u2014 exactly the kind of meta-test that earns its file."
  },
  {
   "file": "tests/specs/steer-migration.spec.js",
   "purpose": "Pins the STEER_SCHEMA migration ladder: each step runs exactly once per version, touches only its own keys, the v3 pace regrid is injective and log-nearest, and current-schema stores are never re-reset (the data-loss bug); failure means a migration would silently overwrite player choices.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "weakSpots": "Reads STEER_SCHEMA out of served source (lines 56-61) \u2014 a fragile-looking regex, but self-failing if it stops matching. The v3 regrid test does 10 reloads (test.slow, lines 190-218), the file's one expensive member; DEFAULT constants at line 177 (wheelbase 3.2333, tiltCutoff 1.1987) are pinned literals that a deliberate default retune would churn, though only as inequality anchors.",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Model migration testing: every direction of the ladder invariant is covered, the assertions distinguish store-level from physics-level effects, and the comments record why each case exists."
  },
  {
   "file": "tests/specs/steering.spec.js",
   "purpose": "The player heading model's core physics contracts (default = nothing steers the car, driver authority, expo curve, symmetry, racing-line PULL/PUSH) plus four real input-latch regressions (keyup-while-focused, lostpointercapture, ghost pointerId, retargeted pointerup); failure means steering physics or throttle-release handling regressed.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "weakSpots": "First test's corner filter (`if (Math.abs(off.before.k) < 0.012) continue`, line 107) can shrink the sample, backstopped only by checked > 0 (line 124). Expo bounds 8-35% (lines 211-212) and symmetry 15% (line 238) are wide but relative. The keyboard-latch describe block (lines 292-405) is input-module territory, not heading-model physics.",
   "overlaps": [
    "tests/specs/touch-steer.spec.js",
    "tests/specs/sliders.spec.js"
   ],
   "verdict": "split",
   "verdictDetail": "Move the four keyboard/pedal latch tests (lines 292-405) into touch-steer.spec.js or an input.spec.js \u2014 they test js/game/input.js with no race needed, and splitting them off the heavy startLiveRace file lets them run at page-boot cost; the physics half stays as-is."
  },
  {
   "file": "tests/specs/telemetry-compare.spec.js",
   "purpose": "Data-hub TELEMETRY multi-lane comparison with a stubbed F1API: three lanes with legends and a genuinely painted map canvas, cross-session R/Q lane badges surviving a session switch, lane removal; failure means the compare UI flow broke.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "weakSpots": "None material \u2014 the getImageData painted-pixel check (lines 88-93) is the pattern smoke.spec.js's minimap test should copy; per artifacts/logs the whole file runs in ~8 s.",
   "overlaps": [
    "tests/unit/telemetry-trace.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "Fast because it setContent's a bare hub instead of booting the game, and the assertions are all user-visible outcomes; the overlap with telemetry-trace is UI-vs-math and correct."
  },
  {
   "file": "tests/unit/telemetry-trace.test.mjs",
   "purpose": "The telemetry map's math defenses in pure node: stray-sample rejection without truncating real excursions, gap detection, non-degenerate bounds, dropout-row filtering in F1API, and the playback dot completing exactly one forward lap on its OWN lap length; failure means session maps would rescale or dots would lag/jump.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "weakSpots": "None material \u2014 each test names the field bug it pins (the Hungary map mismatch, the num(0) !== null filter bug, the borrowed-path tMax scaling regression) and the sweep-angle technique properly handles wrap-around.",
   "overlaps": [
    "tests/specs/telemetry-compare.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "A model unit suite: real regressions, deterministic synthetic laps, milliseconds of cost, and it tests internals (_dropStrays, _locAt) the browser spec could only observe indirectly."
  },
  {
   "file": "tests/specs/terrain-over-road.spec.js",
   "purpose": "No terrain or road-mesh triangle sits above the racing surface (the green-wedge/mound bug class), checked geometrically across all 40 circuits plus targeted Hungaroring/Red Bull Ring/Mexico/Madrid probes; failure means a circuit ships with geometry poking through the track.",
   "assertionQuality": "mixed",
   "costTier": "heavy",
   "weakSpots": "Line 246: road overs are flagged only when `> TOL && < 1.5` \u2014 a road face 2 m above the racing line on ANY track passes silently, because the crossover exemption is a blanket magnitude cut instead of being gated on CROSSOVER_TRACKS (which line 203's frac-distance filter already handles for the three known tracks). The all-circuits test carries a 25-minute budget (test.setTimeout(1500000), line 134). Hungaroring/RBR gap === null acceptance (lines 32, 50) is correct, not vacuous.",
   "overlaps": [
    "tests/specs/props-over-road.spec.js"
   ],
   "verdict": "strengthen",
   "verdictDetail": "The terrain half genuinely catches its bug class, but restrict the >=1.5 m road exemption to CROSSOVER_TRACKS so a gross road-over-road defect on the other 37 circuits cannot hide behind the Suzuka figure-8 carve-out."
  },
  {
   "file": "tests/unit/test-coverage-audit.test.mjs",
   "purpose": "Asserts every test file in THIS repo belongs to a topical test:* group (plus two logic checks of the auditor itself); failure means a test file exists that nothing runs.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "weakSpots": "None \u2014 the file's own header documents the vacuity trap it climbed out of ('a unit test of a checker is not the check') and the real-repo assertion (lines 54-70) now includes .test.cjs, closing the gap that hid shared-track-foundation-characterization for months.",
   "overlaps": [
    "tests/unit/test-groups.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "The orphan-file guard belongs in the suite everyone runs, and the >100-file sanity floor (line 63) protects the scan itself; the adjacency with test-groups.test.mjs is two different invariants (group membership vs docs/config sync), not duplication."
  },
  {
   "file": "tests/unit/test-groups.test.mjs",
   "purpose": "Keeps the test taxonomy honest: pick-tests rules point at real scripts, every source dir routes somewhere, docs/TESTING.md matches the groups and file counts, manual suites stay out of discovery, and the RENDER_SPECS project partition has no silently-never-run specs; failure means the routing that tells agents what to test has drifted.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "weakSpots": "The docs count regexes (lines 96-98) depend on exact phrasing in TESTING.md but self-fail with 'no longer states a count' if the phrasing changes, which is the safe direction. Note the header still says '98 specs' (line 4) while the count assertions are live \u2014 the prose is stale, the checks are not.",
   "overlaps": [
    "tests/unit/test-coverage-audit.test.mjs"
   ],
   "verdict": "keep",
   "verdictDetail": "The --project/RENDER_SPECS partition checks (lines 117-163) guard a genuinely nasty silent-pass failure mode Playwright itself will never report; every assertion here has a documented drift incident behind it."
  },
  {
   "file": "tests/specs/tilt-pipeline.spec.js",
   "purpose": "Every stage of the tilt steering pipeline (dead zone subtraction, MAX_TILT map, steerToTilt inverse, 1.6x slew asymmetry, calibration from raw angle, One-Euro smoothing) plus the live-deviceorientation-vs-simTilt anti-drift guard; failure means tilt steering regressed or the live/harness paths re-diverged.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "weakSpots": "None material \u2014 magnitudes come from the Input getters or are asserted as orderings, the two hard-coded literals (full lock 1, release 1.6x) are argued as THE contract, and the counter-intuitive slew-binds-on-snap behavior (lines 419-445) is pinned so the small-step smoothing test stays meaningful.",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Model coverage of a previously untested default input mode; the final wall-clock test is the only non-deterministic member and it exists precisely to police the shared-helper contract that stops the two paths drifting again."
  },
  {
   "file": "tests/specs/time-trial.spec.js",
   "purpose": "Time Trial mode: TT flags/HUD, ghost recording monotonicity and reverse-sample rejection, sector-split timing at the S/F wrap and S1 announce, and the TT results panel with leaderboard persistence after CLEAR GHOST; failure means TT ghosts or sector timing broke.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "weakSpots": "Fixed waitForTimeout sleeps (lines 30, 44, 181-184, 194) are flake-prone. The sector-announce test (lines 145-171) reads #announce textContent that 'persists even after hiding' \u2014 if a later announce (e.g. RECOVERED) overwrites it despite the low-speed precaution, the /S1/ match fails for the wrong reason; it mitigates but depends on auto-rescue not firing.",
   "overlaps": [
    "tests/specs/season.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "Eleven monza boots is real cost, but each test asserts a distinct behavior with a named failure mode (formation-lap S3 suppression, the ghost/leaderboard separation); the overlap with season.spec.js is only the shared finishRace/results plumbing."
  },
  {
   "file": "tests/specs/tlx-probes.spec.js",
   "purpose": "The opt-in three.js/TSL backend: descriptor-copy install onto GLX, per-milestone subsystem state (shadows, post/HDR, godrays, sky night gate, FX, skids, chunked culling with source-array frees, env probe, gpuTimer contract, mesh-build routing) with a console/GL-error scrape on every test; failure means TLX drifted from GLX parity or a milestone regressed.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "weakSpots": "Screenshot >5000-byte checks (lines 67, 117, 267) are the same coarse non-blank heuristic as smoke, but here they are always paired with state-hook assertions. Waits are on state functions rather than sleeps (good), yet several 60 s waitForFunction timeouts on singapore street builds under SwiftShader make this one of the slowest files per test.",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Asserting exact expected-idle states (lamp arms === 0/idx -1 on a day race, gpuTimer {supported:false} on SwiftShader) rather than only expected-active ones is what makes this parity suite trustworthy; webgl-probes.spec.js is its designed GLX mirror, not an overlap."
  },
  {
   "file": "tests/specs/touch-steer.spec.js",
   "purpose": "Canvas touch steering is an anchored proportional drag (not the old binary screen-half rule), arrows ramp like keys, and pedals report analog travel that reaches the physics; failure means touch/pedal input semantics regressed.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "weakSpots": "None material \u2014 assertions are orderings and bounds ('a sensitivity retune must not rewrite this test', line 69), each regression case names its mechanism (the Map.set identifier-ordering bug at lines 121-131), and no race boot is needed.",
   "overlaps": [
    "tests/specs/steering.spec.js"
   ],
   "verdict": "keep",
   "verdictDetail": "This is the natural home for steering.spec.js's input-latch describe block if that split happens \u2014 same module, same page-boot-only cost model, same fixtures import."
  },
  {
   "file": "tests/unit/track-accuracy-validator.test.mjs",
   "purpose": "Failing means tools/track-accuracy-validator.mjs's shape comparator or mapping validation broke \u2014 the tool that judges circuit geometry against reference shapes would accept reversed/distorted layouts or miss unmapped circuits.",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Four tight unit tests of the comparator (translation/scale invariance, reversal, distortion, mapping diffs) with exact expected outputs; only a square fixture, but sufficient for the invariants asserted.",
   "weakSpots": "Minor: single square fixture only; shapeError < 0.02 (line 14) is a soft bound, though MAX_SHAPE_ERROR is exercised properly at line 25."
  },
  {
   "file": "tests/unit/track-foundation.test.mjs",
   "purpose": "Failing means the track engine's coordinate/space contracts broke \u2014 TrackSpace racing/source frac reversibility, node-rotation permutation, TrackSurface monotonic terrain rails, or TrackModels' atomic group commit/rollback and vertex-budget diagnostics.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Real invariants asserted exactly (permutation via Set size, atomic rollback via deepEqual of buffers, monotone rails); the Miami test pins curated data with a written regression rationale, which is deliberate rather than brittle.",
   "weakSpots": "The Miami dressing-exclusion deepEqual (lines 63-67) pins exact curated s0/s1 values, so any legitimate scenery retune fails it \u2014 accepted brittleness, but it makes this 'foundation' file partly a circuit-data test; it covers three unrelated modules plus one circuit in one file."
  },
  {
   "file": "tests/unit/track-graph.test.mjs",
   "purpose": "Failing means the scenery model library/node graph broke its core contract \u2014 record-once/replay-many, guarded-emitter suppression, bake() reconstruction, instance-matrix equivalence with replay, or NODE_COLOR tint sharing \u2014 i.e. instanced rendering would diverge from the baked world.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Exemplary unit suite: the instance-matrix-vs-replay equivalence test (lines 232-262) and the partial-suppression/non-uniform-scale fallbacks pin exactly the failure modes tools/graph-parity.cjs cannot see; every assertion is exact or 1e-6-bounded.",
   "weakSpots": "None material."
  },
  {
   "file": "tests/specs/tracks-visual.spec.js",
   "purpose": "Failing would mean a per-circuit chase-cam render pixel-diverged from a golden baseline \u2014 but no baselines are committed, so today it can only skip.",
   "assertionQuality": "vacuous",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Deliberately skip-gated (known-already, confirmed at lines 27-30) with a self-documenting path to activation (--update-snapshots) and zero cost while dormant; the file body itself carries no assertions \u2014 all delegated to track-helpers.js describeTrack.",
   "weakSpots": "Entirely inert as shipped: test.skip fires unconditionally because tests/specs/tracks-visual.spec.js-snapshots/ does not exist; if baselines are ever generated the suite becomes heavy (40 circuits x LAP_FRACTIONS renders)."
  },
  {
   "file": "tests/specs/tracks-walls.spec.js",
   "purpose": "Failing means a circuit's driving boundary went insane \u2014 NaN walls, collapsed track (minB<=1), runaway boundary (maxB>=60), barriers deep inside the tarmac, street circuits losing their tight walls, or ramming an edge producing non-finite/unbounded positions.",
   "assertionQuality": "mixed",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "strengthen",
   "verdictDetail": "The invariants are real and would catch genuine wall regressions, but test 2 (lines 37-53) rebuilds all ~40 circuits to assert on only 5 street circuits \u2014 fold it into test 1's loop or build just those 5, roughly halving the file's circuit-build cost.",
   "weakSpots": "Thresholds minB>1 / maxB<60 / minOverHw>-1.5 (lines 30-33) are broad sanity bounds, not tuned values; test 2's whole-grid build asserts nothing for non-street tracks; the driving test's `if (r.skip) continue` (line 89) silently drops a track whose race() failed rather than failing on it."
  },
  {
   "file": "tests/specs/ui-audit.spec.js",
   "purpose": "Nominally: failing means a menu/screen flow can no longer be navigated (locator timeouts); in substance it is a 63-shot screenshot gallery generator for human review.",
   "assertionQuality": "vacuous",
   "costTier": "heavy",
   "overlaps": [
    "tests/ui-desktop.spec.js",
    "tests/specs/menu-survey.spec.js"
   ],
   "verdict": "regroup",
   "verdictDetail": "Confirmed 0 expect() calls across ~63 executions at 13-108s each (e-ui.log) \u2014 it dominates test:ui's wall time while only failing on navigation timeouts already covered by asserted specs; move it to an on-demand gallery group so test:ui runs the specs that can actually fail on a UI regression.",
   "weakSpots": "No expect() anywhere \u2014 the imported expect (line 5) is never used; every test ends in shot() (lines 31-34); several tests bypass real navigation by force-toggling hidden (lines 116, 186-189), so even the navigation-smoke value is partial."
  },
  {
   "file": "tests/specs/ui-button-touch.spec.js",
   "purpose": "Failing means a touch/pause interaction regressed \u2014 the Escape one-step-back ladder, photo-mode stick latch/cancel/foreign-pointer bugs, LOOK-stick overlap at scaled UI, steer-mode-change layout stability, calibrate-button state, the GAS button, or HOW TO PLAY completeness.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Every test pins a documented, once-real bug with an observable proof (nub transform, camState eye, getBoundingClientRect equality); note the known-already item is outdated on this branch \u2014 the throttle-button expect at line 461 is now unconditional, with the old count()-guard documented as the bug.",
   "weakSpots": "cycleToPauseSteerMode (lines 45-53) does not assert it reached the target mode, so a broken cycle would surface as a confusing downstream failure; the file's name has drifted \u2014 half of it is lighting-tuner/photo-mode lifecycle, a plausible future split."
  },
  {
   "file": "tests/ui-desktop.spec.js",
   "purpose": "Nominally: failing means five screens can no longer be reached at iPad/desktop viewports; in substance it is a 10-shot screenshot gallery with zero assertions.",
   "assertionQuality": "vacuous",
   "costTier": "heavy",
   "overlaps": [
    "tests/specs/ui-audit.spec.js"
   ],
   "verdict": "merge",
   "verdictDetail": "Merge into ui-audit.spec.js (or whatever gallery group it lands in) as two more viewport rows \u2014 it is a strict subset of ui-audit's screens (main menu, select, howtoplay, HUD, pause) at different sizes, costing 25-71s per shot (e-ui.log) for no assertable outcome.",
   "weakSpots": "0 expect() calls in the file; the only failure modes are waitFor timeouts; does not even import expect."
  },
  {
   "file": "tests/specs/ui-scale.spec.js",
   "purpose": "Failing means a player-selectable UI/HUD scale (80-150%) paints a control off-screen, clips it behind an unscrollable ancestor, introduces horizontal scroll, or the two scale sliders stop being independent.",
   "assertionQuality": "strong",
   "costTier": "moderate",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "The clipping-ancestor-aware PROBE (lines 91-133) measures reachability rather than raw geometry, assertions are containment/relative by design, and both orientations are covered after a documented portrait-only escape; the one circuit build is deliberate and headless-optimized.",
   "weakSpots": "toTitle() (lines 61-69) force-hides overlays instead of navigating, so a broken back-navigation path would not be seen here (acceptable \u2014 documented as a cost tradeoff); the HUD test skips missing/hidden clusters silently (line 264), so a cluster that stops rendering entirely passes."
  },
  {
   "file": "tests/specs/understeer-cue.spec.js",
   "purpose": "Failing means the front-axle-saturation haptic fires when it shouldn't (gentle steering, below the speed floor, off-track), stays silent when it should fire, machine-guns past its cooldown, loses a channel, or stops encoding saturation depth in its pulse rate.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Model spec for behavioural testing: rig-verification before every silence assertion, positive controls in the same page, relative depth ordering instead of absolute thresholds, and a correctly-argued pace-invariance test that deliberately avoids pinning a bug.",
   "weakSpots": "Only the +2-tick slack on maxGap (line 304) and the grip-sweep runtime (11 drive episodes in one test, lines 318-323) are debatable; every constant is cited to the cue block it mirrors."
  },
  {
   "file": "tests/unit/vstd-invariant.test.mjs",
   "purpose": "Failing means someone added, moved, or refactored a bare `.speed` vs numeric-literal comparison in js/game.js or js/game/* without justifying it \u2014 the PACE-invariant escape that has shipped real bugs twice (A5, A13).",
   "assertionQuality": "strong",
   "costTier": "trivial",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "A well-built ratchet: matcher unit tests, an allow-list keyed to the exact source line so refactors force re-justification, staleness detection both directions, a count pin against byte-identical duplicates, and a guard that the SUSPECT register cannot decay into a second allow-list.",
   "weakSpots": "None material; the `why.length > 20` check (line 327) is a soft floor but every current row far exceeds it."
  },
  {
   "file": "tests/specs/wake-lock.spec.js",
   "purpose": "Failing means the screen wake lock stops being held for a race's duration or released on exit \u2014 request on race start, release on finish AND on the quitToMenu path, re-acquire on visibility return, and silent degradation when the API is absent or rejects.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Exact-sequence toEqual assertions on the event log distinguish the two release paths and the re-acquire path; the defineProperty mock rationale (assignment silently fails on the getter-only accessor) is exactly the trap that would otherwise make these tests hang or pass vacuously.",
   "weakSpots": "None material; individual tests run 17-59s (api.log) purely because each boots a race \u2014 the platform behaviour under test genuinely requires it."
  },
  {
   "file": "tests/specs/webgl-probes.spec.js",
   "purpose": "Failing means a renderer API/lighting contract broke \u2014 the dynamic player shadow using a stale transform, floodlights not rising at night or not clearing by day, the reported light count exceeding the 32-light uniform sizing, or the mobile STANDARD tier spamming GL errors again.",
   "assertionQuality": "mixed",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "strengthen",
   "verdictDetail": "The shadow-transform interception and mobile-tier GL-error tests are strong and pin real bugs, but two tests are shape/typeof-only and the '32-light cap' reads lightState()'s report rather than what the shader received \u2014 fold the weak probes into the strong tests or read the actual upload if a hook exists.",
   "weakSpots": "'hdrMode() returns a boolean' (lines 27-34) is typeof-only and nearly cannot fail meaningfully; 'lightState() returns expected shape' (lines 79-98) asserts structure, not values; the day-settle wait is hedged with .catch (lines 105-110, acceptable since the final night>day compare is real); the cap test (lines 138-150) never reads the UBO, as its own comment admits."
  },
  {
   "file": "tests/unit/webgpu-lifecycle.test.mjs",
   "purpose": "Failing means the frozen WGX backend leaks or corrupts GPU resources on resize/allocation failure (partial textures not destroyed, old targets destroyed prematurely, retry-storm on persistent failure), or its packed uniform lanes drift from the GLX-parity offsets.",
   "assertionQuality": "strong",
   "costTier": "cheap",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "The fault-injection harness (failNextTexture/View/BindGroup) tests transactional cleanup the browser could never exercise deterministically, and the byte-lane assertions pin exact uniform offsets with correct f32-representability handling; worth keeping even though WGX is deferred/frozen \u2014 it guards exactly the kind of silent rot a frozen backend accumulates.",
   "weakSpots": "Several assertions regex-match source comments rather than behaviour (lines 262-268 'off 352', lines 363-365) \u2014 they pin documentation, which is deliberate but weaker than the harness tests around them."
  },
  {
   "file": "tests/specs/world-physics.spec.js",
   "purpose": "Failing means the Cartesian world-space bicycle model broke its observable contract \u2014 errors during driving, progress no longer tracking speed, inverted steer sign, no-input runs-wide going to the wrong side or road-follow not helping, RESPONSE slider dead, or AI cars stuck/off-track after launch.",
   "assertionQuality": "strong",
   "costTier": "heavy",
   "overlaps": [],
   "verdict": "keep",
   "verdictDetail": "Relative assertions throughout (sign of drift vs sign of k, on<off, high>low*1.15), PACE pinned with a written reason, and the AI test's per-car-delta rewrite documents measured values against its 100 m floor \u2014 the thresholds that exist are all justified in-file; runs in both test:physics and test:behaviour, which is group-level duplication worth trimming when groups are reorganized.",
   "weakSpots": "ds > 35 (line 51) and minGain > 100 (line 168) are absolute metre thresholds, but both carry measured rationale and pinned pace; the corners() slice(0,12) sampling (line 89) could theoretically yield zero qualifying corners on a re-splined Monza, though r.length>0 (line 98) would fail loudly rather than pass silently."
  }
 ]
}