# Apex Tools MCP Week-4

**Goal:** Wrap the remaining read-only tree CLIs from the week-3 leftover list.

**Add:** `apex_select_recall`, `apex_cache_bump_only` (exit 1 = classified, `ok:true`), `apex_rotate_markings_check` (never `--write`), `apex_startline_snap`, `apex_startline_probe`, `apex_aero_zone_turns`.

**Still not wrapped:** `wgx-gallery` (batch Chromium), `graph-parity` (needs `BASE=`), HTTP `:3713`, chrome-devtools occupancy gap.
